# CHAPTER CLOSE — align-II: The Ruling & the Epistemics (2026-08-16)

> The second chapter of THE ALIGNMENT PROGRAM (`AUDIT_2026_08_16_GEOMETRIC_ALIGNMENT.md`;
> the master plan's Arcs R + E). Fourteen slices, `1f17d47..6c0d3d7` + this close. The
> chapter's two charges are DISCHARGED: operator judgment has a carrier, a keyed durable
> home, a receiving surface, and a recording verb (Arc R); six epistemic seams that held
> knowledge in prose, packed strings, or silent defaults now hold it as values (Arc E).

## 1. The claim/witness map

Every chapter-open claim, with its executable witness — the close's first ritual item.

| # | Claim (CHAPTER_ALIGN_II_OPEN.md) | Witness |
|---|---|---|
| 1 | Operator judgment is a VALUE (`OperatorRuling<'subject>`; anchored basis; no Pending — absence is pending) | A53 LIVE via M16 citations; `RulingStoreTests` (7 store laws) |
| 2 | The ruling store is keyed replace-by-key, fail-closed, atomic (NOT a fifth LedgerSpec — standing ruling) | `RulingStoreTests` round-trip / replace-by-key / malformed refusal / pending-by-absence |
| 3 | Tightening overrides carry their ruling attribution; the posture stops severing it | `ConfigProvenanceTests`; `TighteningBindingTests` (+7); `EstatePostureTests` activeWithProvenance |
| 4 | Per-index rulings are expressible (adopt ONE promotion, refuse another); proposals carry identity | `UniqueIndexRulesTests` (+4); `SuggestConfigGatingTests` (+3) |
| 5 | The abstain is first-class; `enforces` means enforce; the dead `EvidenceMissing` token lives | `NullabilityRulesTests` / `ForeignKeyRulesTests` / `UniqueIndexRulesTests` behavioral pins (moved with why-comments) |
| 6 | Rulings render on their findings — record + render only; the K9 correspondence is recordable end-to-end | `EstateTests` reception laws; `EstateSinkClaimsTests` K9 end-to-end; `EstateBoardViewTests` rich-lens law |
| 7 | The verb records through the same carrier (the 10-step checklist) | `MovementSurfaceTests` planRule laws; the A53 citation set |
| 8 | The 26-set wire walk derives from ONE contract (count, order, names, dispositions, leading columns) | `OssysProductionWiringParityTests` contract laws; the live extraction canaries |
| 9 | The bundle projection's erasures are enumerable — nothing lost in silence (A54) | A54 LIVE via M16 citations; `BundleErasureTests` (6 laws); the armed comprehensive canary |
| 10 | How a read was scoped is a value; the totality gate reads the type; the S13 gate compares subsumption | scope laws in `OssysProductionWiringParityTests` (32-shape space); R12 codec laws; `SinkStoreTests` axes-named skip |
| 11 | The journal decode is the render's full inverse; an unreadable ledger degrades NAMED; first-witness is a trichotomy | the id law beside T19 (all 12 domain cases); `JournalReading` laws; `PhysicalClaimTests` trichotomy |
| 12 | Fingerprint readings are three explicit fields; a miss names its axes; old manifests read without re-witnessing | `SinkFreshnessTests` (typed + packed-inverse + old-wire laws); the live UPDATE-moves-Content pin |
| 13 | The finding pedigree is the typed record behind the prose — additive, statement-byte-stable | `EstateTests` pedigree laws (stamp moves no byte; JSON projection; overlay bytes identical) |

## 2. The behavioral ledger

Changes an existing consumer could observe, each with its DECISIONS entry:

1. **II.4** — relaxation-only non-overridden nullability returns `DeclaredShapeCarried`
   (was a false `KeepNullable NoTighteningSignal`); FK abstain likewise; applied/declined
   SETS byte-identical. Two test pins moved with why-comments.
2. **II.4b** — an unreliable unique-index probe decides `EvidenceMissing` (was
   `NoCandidateProfiled`); the distinct operator copy now fires. One pin moved.
3. **II.5** — a ruled DECIDE finding renders the ruling in its lever slot; the ACTION line
   skips answered questions. Only when rulings exist — an empty store is byte-identical.
4. **II.7** — a drifted rowsets script now refuses as `RowsetContractDrift` (located)
   where it previously mis-parsed shifted ordinals. Conforming wire byte-identical.
5. **II.10** — a gappy/reconciled ledger's fabricated "@sync 1" renders "@sync ?"
   (`FirstWitnessedSync.Unknown`); an unreadable ledger degrades NAMED at both
   claim-assembly sites. Healthy journals byte-identical.
6. **II.11** — a freshness miss names its axes; pre-II.11 manifests read through the
   packed-form inverse (no re-witnessing).

## 3. The eight-item close ritual

1. **Claims ⇔ witnesses** — §1 above; every row's witness ran green this close.
2. **Release fast, solution-wide** — `TEST_CONFIG=Release scripts/test.sh fast` PASSED
   (the I.1 dividend held through fourteen more slices).
3. **Full Docker pool** — PASSED with exactly the two named reds
   ({StagedMergeDeployE2ETests, T18CycleBreakCanaryTests} — the transfer-leg pair, out of
   scope per operator ruling 2), confirmed against the failed-tests LIST.
4. **Delta-lint** — 82 = 82 substance-identical across all fourteen slices (chain:
   ii4b → ii5 → … → ii12, each verified).
5. **Law census** — 89 live (A53 + A54 promoted this chapter) / C=6 / D=1; matrix
   regenerated at each promotion; verifiability gate green with zero phantoms.
6. **Named deferrals banked** (triggers in DECISIONS): auto-application of rulings
   (align-II.0 standing ruling; the II.6 verb records only); ruling HISTORY append-only
   (past III.2; `BasisAnchor.SinkEdition` widen); EvidenceDigest basis (first
   `OnEvidenceChange` consumer); ApprovalStore ByProposal wire (proposal-key grain);
   FK-side `NoCandidateProfiled` reason split (operator-copy demand);
   Statement-as-projection (first stored-pedigree re-renderer); `TargetErasure`/A37
   (III-era law work).
7. **Doc currency** — DECISIONS entries landed with every slice (14 entries);
   HANDOFF top letter replaced (forward-looking, Chapter III is the next program);
   the PR description refreshed with the Chapter II table.
8. **Anti-findings** — nothing in the exemplary consent surfaces
   (WriteSignoff/ActConsent/ApprovalWorkflow) moved; no Policy↔OverlayAxis collapse;
   no golden re-record was needed this chapter (every slice byte-identical on healthy
   inputs by design).

## 4. What Chapter III inherits

- The ruling carrier + store + verb + reception are LIVE end-to-end — III's S-track
  (ledger honesty, ChainAdmission) can widen `BasisAnchor` with `SinkEdition` and build
  the append-only ruling HISTORY on the III.2 substrate.
- `AcquisitionScope`/`ScopeAxis` and the manifest's typed readings are the vocabulary
  III.2's `LedgerSpec` work and the S13-era offline program stand on.
- The E-track's fail-closed decodes (journal domain, scope token, packed fingerprints)
  are the worked examples for III.1's typed-instant parses (the `MinValue` lie retires
  the same way).
