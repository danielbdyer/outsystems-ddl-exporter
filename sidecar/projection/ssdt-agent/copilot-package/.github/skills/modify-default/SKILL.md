---
name: modify-default
description: Use when the developer says "change the default value", "new orders should default to Shipped now, not Pending", "stop defaulting this attribute" — changing or removing a DEFAULT constraint. SSDT does a DROP-then-ADD; it ships in place as a single schema change and never backfills existing rows.
---

<!-- Generated by ssdt-agent/scripts/ssdt-agent-package.mjs copilot-apply (the tree carries its own generator; in the source monorepo the tree sits at sidecar/projection/ssdt-agent/). Do not edit by hand; edit the canonical tree or the generator and re-run. -->

The full skill lives in the vendored tree, where its cross-references resolve.
**Read `ssdt-agent/skills/op/modify-default/SKILL.md` (from the repository root) and follow it exactly.**
This file is only the dispatch pointer Copilot matches on; the skill body is in the tree.
