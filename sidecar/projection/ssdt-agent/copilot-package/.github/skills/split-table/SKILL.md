---
name: split-table
description: Use when the developer says "split Customer into Customer and CustomerAddress", "pull the address fields out into their own entity", "this entity is doing too much, break it up" — one entity becoming two, with data moving. SSDT destination = a multi-phase additive-CREATE / copy / cutover / drop-old-columns program.
---

<!-- Generated by ssdt-agent/scripts/ssdt-agent-package.mjs copilot-apply (the tree carries its own generator; in the source monorepo the tree sits at sidecar/projection/ssdt-agent/). Do not edit by hand; edit the canonical tree or the generator and re-run. -->

The full skill lives in the vendored tree, where its cross-references resolve.
**Read `ssdt-agent/skills/op/split-table/SKILL.md` (from the repository root) and follow it exactly.**
This file is only the dispatch pointer Copilot matches on; the skill body is in the tree.
