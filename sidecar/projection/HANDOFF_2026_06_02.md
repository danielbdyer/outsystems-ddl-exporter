# Handoff letter — 2026-06-02 (Wave 6 in-flight; Transfer survey dispatched; the survey-independent 6.A frontier is yours)

To the next agent.

You're picking up **Wave 6 — Substantiating the isomorphism** mid-flight. The
branch tip (`c19b304`) shipped **6.D.1** — `migrate A→B`, the L3 composition
where the live square commutes (T16) — on top of the **6.H** durable-provenance
substrate (Episode / LifecycleStore / ChangeManifest) and the **6.A.10–6.A.12**
diff/ALTER keystones. This session added **one doc only** and changed **zero
code**: `TRANSFER_ISOMORPHISM_SUBSTANTIATION.md`, a companion to
`PRESCOPE_TRANSFER.md`. So your inherited code tree *is* `c19b304` exactly —
run `bash scripts/test.sh fast` at the start to confirm your baseline (the
Wave-4 letter in `HANDOFF.md` flagged a pre-existing `A42 (2.4 canary)`
Docker-pool FK-readback failure; verify that's still the *only* red and is not
yours).

## The one thing to internalize first

**The operator is running a capability survey against the real UAT login, and
the results are not back yet — but you are explicitly meant to keep building
the survey-*independent* code in the meantime.** Do **not** block on the survey,
and do **not** build the survey-*dependent* work (see "Do not build" below).
The survey instrument and what it gates are §2 of
`TRANSFER_ISOMORPHISM_SUBSTANTIATION.md`; read that first (~6 min) so you know
which side of the line each slice sits on.

## Where you are in the spine, and what's buildable now

Wave 6's faithfulness arc (6.A) is the **L1→L2 climb: turn each silent erasure
into a held theorem.** Most of 6.A is **survey-independent** — it's proven
against the *local* ephemeral Docker canary (`Deploy.runWithReadback` /
`runWideCanary`), never the UAT box. That whole frontier is yours.

**Recommended next slice — 6.A.5 (the Decision keystone). I already mapped it;
the coordinates below save you the search:**

> `ReadSide` reconstructs a `Kind` with `Indexes = []` hardcoded and an FK whose
> trust state is never recovered — so a deployed `UNIQUE` index and a
> `WITH NOCHECK` FK **vanish on read-back**. The round-trip claims a fidelity it
> doesn't have. Closing it raises the Decision cell from 1/3 to (with 6.A.8) 3/3
> sub-axes and is likely the same surface as the A42 FK-readback gap.

| What | Where (file:line) |
|---|---|
| `Indexes = []` hardcoded | `src/Projection.Adapters.Sql/ReadSide.fs:877` (in `buildKind`) |
| FK reconstruction, **no `IsConstraintTrusted`** | `ReadSide.fs:912-938` (`buildReference`) — uses `{ Reference.create … with HasDbConstraint = true }` |
| `Reference.create` defaults `IsConstraintTrusted = true` | `src/Projection.Core/Catalog.fs` (~`:995`) |
| The combined multi-batch read | `ReadSide.fs:1106-1175` (`readSchemaCombined`) + the per-result-set `NextResultAsync` walk at `:1211-1251` |
| FK batch (extend with `is_not_trusted`) | batch 4, `ReadSide.fs:1142-1155` |
| Emit side already emits the index + NOCHECK | `SsdtDdlEmitter.fs:416` (index), `:344` (NoCheckFk) |

The slice: add a `sys.indexes ⋈ sys.index_columns` batch (exclude PK-backing /
`is_primary_key=1`), populate `Kind.Indexes`; add `fk.is_not_trusted` to the FK
batch and thread `Reference.IsConstraintTrusted = not is_not_trusted`. Mirror
the existing `readTriggers` / `readCheckConstraints` / `readDefaultConstraints`
idiom.

**Three traps that will cost you an hour each if you don't know them:**

1. **`PhysicalSchema.diff` deliberately does NOT compare indexes or FK-trust**
   (it's skeleton-only; the `IndexesUnreflected` tolerance exists precisely for
   this). So the 6.A.5 witness **cannot** ride `PhysicalSchema.isEqual`. Assert
   on the **reconstructed `Catalog`** directly (`Deploy.runWithReadback`'s
   `Reconstructed` → find the kind → assert `Kind.Indexes` has the unique index
   and `Reference.IsConstraintTrusted = false`). Acceptance name:
   `` ``schema round-trip: a UNIQUE index + a NOCHECK FK survive emit/deploy/ReadSide`` ``.
2. **Smart-constructor default-substitution hazard** (the codec discipline's
   named bomb): in `{ Reference.create … with … }` you MUST set
   `IsConstraintTrusted` explicitly, or it silently inherits the `true` default.
   Same for `Index.create`'s `AllowRowLocks` / `AllowPageLocks` (`true`).
3. **`ReadSide` marks every row-carrying reconstructed table `Modality.Static`**
   (the Wave-4 4.4 trap) — irrelevant to 6.A.5 but it bites profiling code.

**The 6.A faithfulness witness inventory** (what already exists vs. what's open
— grep-verified this session, so you know what NOT to re-do):

| Slice | Witness | Status |
|---|---|---|
| 6.A.1 | transfer drops → non-zero exit | **exists** (`TransferCanaryTests.fs`; `DroppedReferencesExit=9`, `TransferRun.fs:408`) |
| 6.A.9 | `DropFk` surfaces a Warning | **exists** (`DecisionOverlayTests.fs`) |
| **6.A.5** | UNIQUE index + NOCHECK FK survive round-trip | **OPEN — recommended** |
| 6.A.2 | cyclic `AssignedBySink` refused, not mis-keyed | OPEN (~S refusal in `TransferRun.writePlan`) |
| 6.A.3 | composite-IDENTITY refused, not half-captured | OPEN (~S refusal) |
| 6.A.4 | empty-string Text round-trips (or named tolerance) | OPEN |
| 6.A.7 | `Synthesized`-key rename surfaced, not silently re-keyed | OPEN |
| 6.B.1 | `EnforceNotNull` on NULL-bearing column refuses pre-write | OPEN (`dataViolatesTightening`; uses `LiveProfiler`, local) |
| 6.B.2 | renamed column re-pointed by rename map, not ordinal | OPEN |

Any of these is forward progress. 6.A.5 is the keystone (unblocks 6.A.8); the
6.A.2 / 6.A.3 refusals are the quickest (~S each, pure fail-loud in
`TransferRun.writePlan`).

## Do NOT build (survey-dependent — wait for results)

- **Slice D / EXECUTION_PLAN 5.1 — execute against real UAT.** Blocked on
  **OPEN-2** (does the UAT box expose a writable SQL connection at all). The
  survey resolves it. `--execute` stays gated by `PROJECTION_ALLOW_EXECUTE=1`
  (`TransferArgs.fs:21`); R6 unchanged.
- **The set-based `MERGE … OUTPUT` capture (§3 of the new doc).** It is a
  **trigger-gated candidate**, not a task: build it only when survey **P4**
  confirms `MERGE`+`OUTPUT INTO @table` is permitted under the UAT grant **and**
  a real `AssignedBySink` load at volume makes per-row capture
  (`insertCaptureRow`, `TransferRun.fs:101-130`) the measured bottleneck
  (OPEN-5). Until both fire, the shipped per-row `INSERT … OUTPUT` is correct.

When the survey results land, fold them into the disposition decision (P3/P10 →
`PreservedFromSource` vs `ReconciledByRule` vs `AssignedBySink` mix) and the §2
synthesis table; that's what unblocks Slice D.

## Disciplines that will bite (internalize before you write code)

- **Build the whole solution + `bash scripts/test.sh fast` before "done."** The
  only ground truth. Never run the pure and Docker pools as one `dotnet test`
  (OOM on this host) — use `scripts/test.sh` (`fast` / `docker` / `canary`).
- **Closed-DU / record-field change → let the compiler drive the blast radius.**
  Change the source sites, build, read the exhaustiveness/field-missing errors;
  don't hand-hunt.
- **The lint inventory (`scripts/lint-discipline.sh`) exits 1 as a soft floor** —
  it's a pre-existing LINT-ALLOW inventory, not a gate you broke. Confirm your
  new files aren't in the delta; don't chase legacy entries.
- **`TRANSFER_ISOMORPHISM_SUBSTANTIATION.md` is a companion, not canon.**
  `PRESCOPE_TRANSFER.md` + `SurrogateRemap.fs` + `TransferRun.fs` remain
  canonical. Use the locked lexicon (`DECISIONS 2026-05-24`).

## Reading order (~12 min)

1. `TRANSFER_ISOMORPHISM_SUBSTANTIATION.md` §2 (the survey + what it gates) and
   §3 (the capture candidate) — ~6 min.
2. `EXECUTION_PLAN.md` §III Wave 6.A (the faithfulness slices + acceptance
   names) — ~4 min.
3. `AUDIT_2026_05_31_FIVE_AXIS_REDTEAM.md` §2.5 (Decision iso on 1/3 sub-axes —
   the 6.A.5 / 6.A.8 finding, file:line) — ~2 min.
4. `DECISIONS.md` *Active deferrals* index — confirm no survey-gated trigger has
   silently fired.

Hold the spine. The survey is the operator's to run; the faithfulness frontier
is yours to climb while it's out. Pick 6.A.5, make the round-trip *actually*
faithful, and witness it on the reconstructed Catalog — that's one more silent
erasure turned into a held theorem.

— The Wave-6 transfer-reconciliation agent.
