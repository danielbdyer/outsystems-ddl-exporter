# Chapter A.4.7' (prime) open — Compose.run registry-traversal: registry becomes load-bearing for execution

**Status:** OPEN. **Predecessor:** chapter A.4.7 CLOSED 2026-05-16 (commit `bc9fedb`); chapter 4.1.A slice 8 CLOSED 2026-05-17 (commit `3cc4f47`). The TransformRegistry types + filter helpers + four bidirectional property tests + 12 pass `.registered` exports + 1 adapter + 5 strategy registrations are the structural input to this chapter. **Branch:** `claude/review-chapter-close-VnRe8`.

**Plan-of-record spec:** `V2_PRODUCTION_CUTOVER.md` §6.4.7 task 4 (deferred-with-trigger from A.4.7 close; this chapter cashes the deferral). **L3 axiom:** L3-CC-Transform-Totality (A from chapter A.4.7 by metadata totality; this chapter extends to **runtime totality** — the registry as load-bearing execution surface, not just enumeration). **AXIOMS placeholder:** A41 amended candidate (the structural-execution sibling of A41's metadata totality).

**Trigger:** explicit cash-out of `CHAPTER_A_4_7_CLOSE.md` forward signal #3 ("Compose.run registry-traversal refactor"). The named blocker — pass-chaining adapter design for heterogeneous output types — is resolved by axis 1 below (`ComposeState` aggregate). Slice γ.2 forward signal (`let run` private) is rolled into this chapter as slice η since it depends on the new traversal being load-bearing first.

This chapter ships **the registry-driven traversal kernel** + **`Compose.runWithSkeleton`** + **`Compose.run` migration off hand-coded orchestration** + **slice γ.2 (private `let run` + ~328 call-site migration)** + the fifth bidirectional property test (manifest digest round-trip) + slice η's CLI / manifest extension. After this chapter close, the registry is the single load-bearing surface for V2 execution; the hand-coded `Compose.project` sequence retires; pillar 9's structural enforcement extends from metadata to runtime.

## Strategic-frame axes (per DECISIONS 2026-05-15 chapter-open shape)

1. **`ComposeState` aggregate solves heterogeneous output types.** The blocker named in A.4.7 close forward signal #3 — 12 passes split 7-and-5 across `RegisteredTransform<Catalog, Catalog>` (chainable) vs `RegisteredTransform<Catalog, 'DecisionSet>` (TopologicalOrder / NullabilityDecisionSet / UniqueIndexDecisionSet / ForeignKeyDecisionSet / CategoricalUniquenessDecisionSet / UserRemapContext) — resolves via a unified `ComposeState` record carrying the Catalog under transformation + `Option<'DecisionSet>` for each registered decision-set producer. Catalog-rewriting passes update `state.Catalog`; decision-set passes write back into the appropriate Option field; emitters consume the final `ComposeState`. A fold over `TransformRegistry.allInStageOrder` typechecks at `ComposeState -> Lineage<Diagnostics<ComposeState>>`. The decision-set DUs already live in `Strategies/*Rules.fs` which compile before `TransformRegistry.fs`; `TopologicalOrder` and `UserRemapContext` live in their dedicated modules also compiling early — `ComposeState.fs` slots in after `Diagnostics.fs` and before `TransformRegistry.fs` with no compile-order disturbance.

2. **`PassChainAdapter` is the type-erasure boundary.** Each pass's `RegisteredTransform<Catalog, 'Out>` is wrapped into a uniform `ComposeState -> Lineage<Diagnostics<ComposeState>>` step via `PassChainAdapter.liftCatalogPass` (for `'Out = Catalog`) or `PassChainAdapter.liftDecisionPass writeBack` (for `'Out = 'DecisionSet`). The adapter projects the Catalog from state, invokes the typed `Run`, and writes the output back into the appropriate field. Type erasure happens at the adapter boundary, not in `RegisteredTransform` itself — the typed `Run` field stays intact on each pass module's `.registered` export. The single point where `obj`-shape could leak — the adapter — has type-safe `liftCatalogPass` / `liftDecisionPass` constructors taking the typed `RegisteredTransform`, so the type system mediates correctness even though the resulting `PassChainAdapter list` is heterogeneous-Out-erased.

3. **`TransformRegistry.allChainSteps` is the new traversal-ready surface.** Sibling to `TransformRegistry.all` (metadata enumeration), `allChainSteps : PassChainAdapter list` carries the type-erased execution steps in stage order. Populated by slice β: each pass module's `<PassName>.registered |> PassChainAdapter.liftCatalogPass` (or `liftDecisionPass`) reference is added to the list explicitly. F# top-level evaluation order resolves the cross-module dependencies (same precedent as `TransformRegistry.all`, which today is empty). The hand-maintained list is the structural commitment; bypassing the registry becomes impossible once `Compose.run` traverses it.

4. **`Compose.run` becomes a fold over `allChainSteps`.** Today `Compose.project` (Pipeline.fs:92) hand-codes `SsdtDdlEmitter.emitSlices catalog` then `JsonEmitter.emit catalog` then `DistributionsEmitter.emit catalog`. After this chapter: `Compose.run` folds `(ComposeState.initial catalog, Lineage.empty)` through `TransformRegistry.allChainSteps`; each step contributes its Lineage / Diagnostics evidence via the dual-writer primitives; emitters then run against the final `ComposeState` (Catalog + every populated decision-set) via a separate emitter-fan-out step. The hand-coded composition retires; the registry becomes load-bearing.

5. **`Compose.runWithSkeleton` is the same fold with `skeletonView` filter.** Filters `allChainSteps` to entries whose `Sites` contain only `DataIntent` classifications (using the existing `TransformRegistry.skeletonView` helper applied at the metadata join). The bidirectional property test **L3-CC-Transform-Totality: `Compose.runWithSkeleton` emits zero `OperatorIntent` LineageEvents** moves from filter-shape-only (A.4.7 slice θ) to true-execution (this chapter). Skeleton-purity goes from structural enforcement at metadata to structural enforcement at runtime.

6. **Slice γ.2 cashes alongside the traversal.** Once `Compose.run` is registry-driven (slice δ this chapter), the parallel-exposure transition affordance for `<Pass>.run` retires. Slice η of this chapter makes `let run` private in all 12 pass modules and migrates the ~328 remaining call sites (vs the ~80 estimated in chapter A.4.7 close — actual is 4× higher; mechanical-edits precedent from chapter A.0' XXXXL applies: `let private` visibility errors enumerate the worklist; the compiler is the discovery primitive). Carbon-copy events expected: zero (the migration is in-place; consumers swap `<Pass>.run` → `<Pass>.registered.Run` or `(<Pass>.registered config).Run`).

7. **Pillar 9 gains runtime structural enforcement.** A.4.7 made pillar 9 structural at metadata (smart-constructor invariants + four bidirectional property tests against `RegisteredTransformMetadata`). This chapter extends to runtime: an `OperatorIntent` pass that bypasses the registry can't fire because `Compose.run` only invokes registered Apply steps. The skeleton-purity property test (today verifies filter shape) becomes a true execution witness: run `Compose.runWithSkeleton` on a representative canary; assert no `OperatorIntent` LineageEvent fired. The bidirectional sibling — overlay-exercise — verifies every registered `OperatorIntent` step DOES fire under the non-skeleton path on representative input.

8. **A41 amendment cashes at chapter close.** A41's current body (chapter A.4.7 ι) names "Transform registry totality + canonical strongly-typed shape" — metadata-level. This chapter's amendment extends to "Compose.run traverses TransformRegistry.allChainSteps as its execution loop; bypassing the registry is structurally impossible." The verifiability-triangle audit cadence (per `DECISIONS 2026-05-12`) names L3-CC-Transform-Totality as touched (no bucket change — already A from A.4.7 — but the underwriting tightens from metadata totality to metadata + execution totality).

9. **No new `OverlayAxis` variants; no new pass modules; no IR refinement.** This chapter is pure orchestration cash-out. The IR shape lifted by chapter A.0' stays unchanged. The classification vocabulary fixed by A.4.7 slices α / β stays unchanged. The five `OverlayAxis` variants (Selection / Emission / Insertion / Tightening / Ordering) earn the chapter's runtime witness; further expansion stays gated on the Q9 trigger discipline. The 12 passes / 1 adapter / 5 strategies stay unchanged in their substantive logic — only their `let run` visibility (slice η) and their assembly into `TransformRegistry.allChainSteps` (slice β / γ) move.

## Slice plan (8 substantive slices)

Order chosen by **risk × leverage × prerequisite chain**. As with chapters A.0' and A.4.7, slice rows are **design intent at chapter open, not constraints at implementation time**; pillar-8 four-question analysis at implementation may refine.

| Slice | Scope | L3 / structural impact | Risk |
|---|---|---|---|
| **α** | `Projection.Core/ComposeState.fs` ships the aggregate type carrying Catalog + every decision-set Option field (TopologicalOrder, NullabilityDecisionSet, UniqueIndexDecisionSet, ForeignKeyDecisionSet, CategoricalUniquenessDecisionSet, UserRemapContext); `ComposeState.initial : Catalog -> ComposeState`; per-axis `withX` setters. `Projection.Core/PassChainAdapter.fs` ships the type `{ Name; Apply : ComposeState -> Lineage<Diagnostics<ComposeState>> }`; `liftCatalogPass : RegisteredTransform<Catalog, Catalog> -> PassChainAdapter`; `liftDecisionPass : RegisteredTransform<Catalog, 'Decision> -> ('Decision -> ComposeState -> ComposeState) -> PassChainAdapter`. ~6 witness tests in new `ComposeStateTests.fs` + `PassChainAdapterTests.fs`. No registry population yet; no Compose.project change. | Establishes pass-chaining type-system surface | Low — new files only; no consumer reroute yet; mechanical record/module shapes |
| **β** | `TransformRegistry.allChainSteps : PassChainAdapter list` populated with the 7 Catalog-chainable + 5 decision-set passes wired through the slice-α adapter (12 entries). `TransformRegistry.all` populated with the matching 18 `RegisteredTransformMetadata` entries (12 pass + 1 adapter + 5 strategy — adapter and strategies appear in `all` for metadata but not in `allChainSteps` because they're not `Catalog → ComposeState` steps). Two new witness tests: chain steps count = 12; metadata count = 18. The 4 chapter-A.4.7 bidirectional property tests now exercise the populated registry instead of the empty list. | Registry becomes non-empty; totality property tests now have substance | High — risk that some pass's `.registered` Run signature has subtle incompatibility with the lift adapter; surfaces in slice α witness tests on representative inputs |
| **γ** | `TransformRegistry.runChain : ComposeState -> Lineage<Diagnostics<ComposeState>>` ships as the registry-driven traversal kernel. Folds `allChainSteps` via `LineageDiagnostics.bind`; preserves lineage trail ordering per A24 (earliest-first under bind). Tested in isolation (no Compose.project / Pipeline change yet) via 4–6 property tests (idempotence under stable input; T1 byte-determinism; chain output Catalog equals last Catalog-rewriting pass's output; decision-set Option fields populated iff the producing pass fired). | Traversal kernel exists; consumable but not yet consumed | Medium — the writer composition correctness is the property tests' job; testable in isolation before Pipeline.fs change |
| **δ** | `Projection.Pipeline/Pipeline.fs:Compose.project` migrated to invoke `TransformRegistry.runChain` and then run the three emitters against the resulting `ComposeState`. Current behavior preserved: same `Outputs` produced; same test baseline holds. The hand-coded pass sequence retires; emitter-fan-out step remains hand-coded (no emitter is in `allChainSteps` because emitter `'Out` is `ArtifactByKind<'element>`, structurally distinct from the chain). | Compose.project becomes registry-driven | Medium — central seam; full test suite must pass byte-identical; existing `EndToEndPipelineTests` + `CanaryRoundTripTests` are the regression surface |
| **ε** | `Compose.runWithSkeleton : Catalog -> outputDir -> Result<string list>` ships in `Pipeline.fs`. Folds `allChainSteps` filtered via `TransformRegistry.skeletonView` joined on pass name. Skeleton-purity bidirectional property test promoted from filter-shape-only to true-execution: runs `Compose.runWithSkeleton` on a fixture; asserts zero LineageEvent carries `Classification = OperatorIntent _`. | True-execution structural enforcement of skeleton-purity | Medium — runtime enforcement of a previously-structural-only commitment; property test gates merge |
| **ζ** | `osm emit --skeleton-only` CLI flag in `Projection.Cli/Program.fs`; binary toggle per Q8 (per-OverlayAxis flags deferred per consumer-pressure). `ManifestEmitter` extension: `registry.digest : Sha256` field + per-artifact `applied-transforms : (SsKey × OverlayAxis option) list` field. Fifth bidirectional property test ships: **manifest registry digest round-trip** — emit canary, parse manifest, re-emit, assert digest stable; perturb a `Sites` rationale, re-emit, assert digest changed. Uses `Utf8JsonWriter` / sorted-key `JsonNode` per Tier-3 hard-requirement (text-builder-as-first-instinct). | CLI surface complete; manifest extension complete; 5/5 bidirectional property tests green | Medium — CLI is binary toggle (low complexity); manifest extension is structured JSON extension (precedent in chapter 3.7 slice ε); digest property test is the new substantive test |
| **η** | Slice γ.2 cash-out: `let run` made private in all 12 pass modules (`CanonicalizeIdentity`, `VisibilityMask`, `NamingMorphism`, `NormalizeStaticPopulations`, `SymmetricClosure`, `TableRename`, `TopologicalOrderPass`, `NullabilityPass`, `UniqueIndexPass`, `ForeignKeyPass`, `CategoricalUniquenessPass`, `UserFkReflowPass`). Migrate ~328 call sites (production + test) from `<Pass>.run` to `<Pass>.registered.Run` or `(<Pass>.registered config).Run`. F# `let private` visibility errors enumerate the worklist; compiler is the discovery primitive. | Parallel-exposure transition affordance retires; canonical surface only | High — voluminous mechanical migration (~328 sites). Mechanical-edits precedent (chapter A.0' XXXXL `/tmp/fix_*.py`); risk concentrated in test fixtures with config-passing patterns (`NullabilityPass.run catalog policy profile` → `(NullabilityPass.registered policy profile).Run catalog`) — slight reshape, not just rename |
| **θ** | A41 amendment body filled in `AXIOMS.md` per scaffolding discipline (the metadata-totality → metadata-and-execution-totality extension); L3-CC-Transform-Totality underwriting tightened in `PRODUCT_AXIOMS.md`. Chapter-close ritual operated (8 items including L3 audit step + pillar-9 audit + Active deferrals scan + verifiability-triangle audit cadence per `DECISIONS 2026-05-12`). `CHAPTER_A_4_7_PRIME_CLOSE.md` ships with per-slice ledger + meta-codifications + forward signals (mirrors `CHAPTER_A_4_7_CLOSE.md` structure). | A41 amendment cash-out; chapter close | Low — documentation + ritual slice |

**Estimated effort:** ~1.5–2 weeks across slices α–θ. Subtask breakdown (refined at implementation time): ~0.25 week slice α; ~0.25 week slice β; ~0.25 week slice γ; ~0.25 week slice δ; ~0.15 week slice ε; ~0.25 week slice ζ; ~0.4 week slice η (~328 call sites mechanical); ~0.1 week slice θ. Less than chapter A.4.7's 3-week estimate because A.4.7 already shipped the registry types + property tests + filter helpers; this chapter only adds the execution surface + the γ.2 cash-out.

## Out of scope (named exclusions)

- **Emitter-as-chain-step.** Emitters return `ArtifactByKind<'element>` (heterogeneous `'element`); they don't compose into `ComposeState` the way passes do. Emitter-fan-out stays hand-coded in `Compose.project` (post-chain step). A future "Compose-emitter-registry" chapter could unify this if a consumer demands it; today three emitters at one fixed call site doesn't justify the abstraction.
- **Adapter-as-chain-step.** The OSSYS adapter's `RegisteredTransform<RawRowSet, CatalogFragment>` shape doesn't fit `ComposeState -> ComposeState` either (it produces the *input* Catalog from raw OSSYS rowsets, not a transformation OF the Catalog). Adapter stays as the boundary-translation step before `ComposeState.initial`; appears in `TransformRegistry.all` for metadata totality but not in `allChainSteps`.
- **`Profile` threading through the chain.** The current `Compose.project` consumes `Profile.empty`; profile-driven decision-set generation lands when M2 onward threads real profile evidence. Slice α–η operate on `Profile` arguments captured at the factory level (each `<Pass>.registered policy profile` call captures the profile in the Run closure); slice δ's `Compose.run` signature accepts `Catalog -> Profile -> outputDir -> Result<...>` and threads the profile into each factory call.
- **`Policy` consumption in emitters.** A18 amended preserved unchanged. Emitters consume `Catalog × Profile` via `ComposeState`; no emitter consumes Policy. Decision-set passes consume Policy at factory time and emit decisions to `ComposeState`; emitters read decisions, not policy.
- **Composition.fanOut replacement.** Per chapter A.4.7 out-of-scope clause preserved: registry enumerates transformations across the pipeline; fanOut composes strategies within a pass. Different granularities; both preserved.
- **`Policy.fs ↔ OverlayAxis` structural collapse.** Deferred-with-trigger from chapter A.4.7 stays deferred. The current chapter doesn't add Policy-consumption sites; the collapse trigger (call sites consulting both vocabularies) still doesn't fire.
- **Per-OverlayAxis CLI flags.** `--no-tightening`, `--no-selection`, etc. stay deferred-with-trigger per the consumer-pressure principle. `--skeleton-only` is the binary toggle per slice ζ.
- **C# layer adoption.** Per V2 self-containment + F#/C# language-role partition: this chapter is pure F# in `Projection.Core` + `Projection.Pipeline`. No C# adapter project lands.
- **Carbon-copy events.** Zero expected across this chapter; the registry traversal is V2-native (no V1 precedent). `BACKLOG.md` V1 inheritance log remains empty.

## Deferred-out-of-A.4.7' (named, with triggers)

- **Emitter-as-chain-step** — trigger: a fourth emitter lands, OR a consumer needs runtime classification of emitter sites.
- **Adapter-as-chain-step** — trigger: V2 needs to compose multiple adapters (e.g., DACPAC adapter alongside OSSYS adapter) at the registry level.
- **Per-OverlayAxis CLI flags** — trigger: real operator demand for granular overlay toggling.
- **`Policy.fs ↔ OverlayAxis` structural collapse** — preserved deferral from chapter A.4.7.
- **`Compose.run` async-streaming form.** Today `Compose.runChain` is synchronous (F# Core stays sync). If chain-level streaming becomes a perf concern (e.g., 300-table catalogs with 50k rows ramping through 18 passes), an `AsyncStream`-based traversal becomes a chapter 5+ candidate.
- **Sub-strategy fanOut-level registration** — preserved deferral from chapter A.4.7.

## What success looks like

**End of slice α (this session, opening):** `ComposeState` + `PassChainAdapter` types ship with `liftCatalogPass` / `liftDecisionPass` constructors + per-axis `withX` setters. ~6 witness tests cover (a) initial-state shape; (b) per-axis setter idempotence; (c) `liftCatalogPass` round-trip on a constant pass; (d) `liftDecisionPass` round-trip on a constant pass; (e) Lineage trail preserved through Apply; (f) Diagnostics entries accumulated through Apply. Test baseline holds (1226 / 1226 + ~6 new); 0 skipped; 0 build warnings under `TreatWarningsAsErrors=true`; lint count 13 — unchanged from main. No `TransformRegistry.all` population; no `Compose.project` change. The slice-pattern is established for slices β–θ to inherit.

**End of chapter A.4.7' (~1.5–2 weeks; 8 slices):**

- `TransformRegistry.allChainSteps : PassChainAdapter list` populated; `TransformRegistry.all : RegisteredTransformMetadata list` populated; both non-empty.
- `TransformRegistry.runChain : ComposeState -> Lineage<Diagnostics<ComposeState>>` ships as the load-bearing execution kernel.
- `Compose.run` in `Pipeline.fs` migrated to `runChain` + emitter-fan-out; hand-coded pass sequence retired.
- `Compose.runWithSkeleton` ships; skeleton-purity property test promoted from filter-shape to true-execution.
- `osm emit --skeleton-only` CLI flag ships; `ManifestEmitter` carries `registry.digest` + per-artifact `applied-transforms`; manifest digest round-trip property test ships (5/5 bidirectional tests green).
- All 12 pass modules: `let run` private; ~328 call sites migrated to `<Pass>.registered.Run`; canonical surface only.
- A41 amendment body filled; L3-CC-Transform-Totality underwriting tightened from metadata totality to metadata + execution totality.
- Chapter-close ritual operated (8 items + L3 audit + pillar-9 audit + Active deferrals scan + verifiability-triangle audit step).
- **Phase-A exit gate's pillar-9 commitment fully closed:** registry is the single load-bearing surface for V2 execution; bypassing it is structurally impossible.

**Verifiability-triangle audit step (per `DECISIONS 2026-05-12`):** chapter-close L3 audit names L3-CC-Transform-Totality as touched (no bucket change — already A from A.4.7 — but the underwriting tightens). Names any new Bucket-D gaps introduced (expected: zero). Pillar-9 audit names each transformation site whose classification this chapter exercised (slice ε's skeleton-purity property test exercises every registered classification at runtime; no new sites added; no reclassifications).

## Resolved at chapter open

- **`ComposeState` is a record, not an aggregate-root smart-construction.** Construction-time invariants (Catalog smart-constructed via `Catalog.create`; decision-sets smart-constructed via their respective `.create`) hold transitively. No new invariants are introduced at the ComposeState level — the record is a state-carrier, not an aggregate-root. Per `A39` discipline preserved: ComposeState's components carry their own invariants; the state-carrier is opt-in evidence accumulation.

- **`PassChainAdapter` is a record, not a class.** Single Apply field captures the type-erased step. Module-level constructors (`liftCatalogPass` / `liftDecisionPass`) preserve type-safety at construction; the resulting `PassChainAdapter list` is homogeneous-Out per F# type rules. Mirrors `RegisteredTransform<'In, 'Out>` vs `RegisteredTransformMetadata` precedent — typed at construction, erased at aggregation.

- **Slice η bundles slice γ.2 (private `let run` migration).** Chapter A.4.7 close named slice γ.2 as a separately-sequenceable forward signal. This chapter rolls γ.2 into slice η because the migration only makes sense once `Compose.run` is registry-driven (slices β–δ). Sequencing γ.2 before β/γ/δ would land the migration without the structural commitment it serves.

## Open questions to surface during implementation (refine, don't pre-decide)

- **Should `ComposeState` carry an explicit `Profile` field?** Today's `Compose.project` accepts `Catalog` and threads `Profile.empty` to each decision-set pass at the factory call site. After slice δ, `Profile` may live on `ComposeState` for visibility; alternatively it may stay as a factory-time argument captured in each `<Pass>.registered policy profile` closure. Slice α decides empirically — if witness tests need `ComposeState.Profile`, lift it; if not, defer.
- **Naming: `PassChainAdapter` vs `ChainStep` vs `ComposeStep`.** Pillar-8 four-question analysis at slice α implementation: (1) domain concept — a typed pass invocation wrapped to compose into the chain fold; (2) is V2 already naming this — no; (3) concept- or action-shaped — concept (it IS a chain step, not "what it does"); (4) generic-suffix smell test — "Adapter" might trigger the smell; "Step" is concept-shaped and shorter. Slice α may rename to `ChainStep` if pillar-8 four-question fires.
- **`runChain` placement: `TransformRegistry` module vs new `Compose` module in Core.** Two-consumer threshold for emergent primitives: if `runChain` is the only registry-traversing function, it lives in `TransformRegistry`; if a sibling function (e.g., per-stage traversal) earns its place, both move to a `Compose` module. Slice γ defers the decision empirically.
- **Adapter rule count refinement.** Chapter A.4.7 shipped CatalogReader as ONE entry with 6 grouped Sites for ~26 rules. This chapter's `TransformRegistry.all` population includes that one entry; the slice δ migration doesn't disturb the per-rule-as-Sites pattern.

## Discipline anchors

- **Pillar 9 (harvest-dichotomy classification).** No new transformation sites; the existing 18 classifications stay. Chapter validates classifications at runtime (slice ε) rather than at metadata (slices α / β of A.4.7).
- **Pillar 8 (domain-first naming, four-question analysis).** Type names `ComposeState`, `PassChainAdapter`, `allChainSteps`, `runChain` pass the four-question test at chapter open; slice α implementation may refine (see open question above). Pillar-8 deviation discipline applies to slice plan rows.
- **Closed-DU empirical-test discipline.** No new variants; record-extension (ComposeState fields) follows chapter 3.2 close generalization — F# field-missing errors light up at literal-construction sites only; semantic interpretation untouched.
- **Two-consumer threshold for emergent primitives.** Holds for `runChain`, `ComposeState.withCatalog`, etc. — extract only at the second consumer. Slice α's witness tests count as one consumer; slice γ's `runChain` body is the second.
- **A18 amended preserved.** No emitter consumes Policy. `ComposeState` carries decision-sets (evidence) not Policy (intent); emitters consume `Catalog × Profile × decision-sets`, never Policy.
- **A39 preserved.** ComposeState's components carry their own smart-constructor invariants; the state-carrier doesn't re-validate.
- **A41 (the metadata totality from chapter A.4.7) preserved.** This chapter's amendment extends A41, doesn't replace it. Metadata totality + execution totality compose.
- **V2 self-containment preserved.** Zero carbon-copy events expected across this chapter. `BACKLOG.md` V1 inheritance log remains empty at chapter close.
- **Writer-fidelity discipline (chapter-3.1 contribution).** `LineageDiagnostics.tellDiagnostics` and `Lineage.ofValueAndEvents` propagate through `PassChainAdapter.Apply`; manual record-building of `LineageEvent` remains forbidden.
- **Text-builder-as-first-instinct (Tier-3 hard-requirement).** Slice ζ's `ManifestEmitter` `registry.digest` extension uses `Utf8JsonWriter` / `JsonNode` (typed-AST), not `StringBuilder`. Per `DECISIONS 2026-05-10`.
- **AXIOMS scaffolding (S0.F).** A41 amendment placeholder added to `AXIOMS.md` "Amendments scheduled (chapter close)" at chapter open; chapter agent fills the body at slice θ per the chapter-3.1 / A.0' / A.4.7 precedent.
- **Verifiability-triangle audit cadence (`DECISIONS 2026-05-12`).** Chapter-close L3 audit step names L3-CC-Transform-Totality as touched; underwriting tightens; no new Bucket-D gaps expected.
- **Chapter-mid-audit (`DECISIONS 2026-05-19`).** Multi-session chapter; sessions 3–5 trigger a cross-document audit subagent dispatch (CRITICAL / MINOR / OPEN classification; Active deferrals scan required). Likely fires after slice δ lands and slice η begins.
