# Law/outcome coverage — alignment audit workpaper

Auditor A10 · 2026-08-16 · scope: AXIOMS.md, AxiomTests.fs, NORTH_STAR.matrix.generated.md + matrix-status.sh, PRODUCT_AXIOMS.md, DECISIONS.md Active-deferrals index, THE_USE_CASE_ONTOLOGY.md (+ .fitness/.acceptance/.obligations siblings), CONSTELLATION.md §10–§11. READ-ONLY; no builds or test runs.

## 1 The declared outcome space (what the docs+laws claim, compactly)

Four strata declare the space:

1. **L2 algebra** — `AXIOMS.md`: A1–A43 + A45–A49, T1–T19, A-Lifecycle-1..4, plus amendments (A1 four-variant, A6, A12×2, A17, A18, A24, T1×2, T11×2, A41 execution-totality). Headline self-count at AXIOMS.md:26 still reads "A1–A43 generating T1–T16". A44 ("expressible ⇔ reachable") is declared in `THE_CONFIG_CONTROL_PLANE.md:31`, **not** in AXIOMS.md.
2. **L3 operator contract** — `PRODUCT_AXIOMS.md`: ~63 promises across S1–S11 / D1–D10 / I1–I10 / X1–X10 / C1–C10 / CC1–CC6 + CC-Transform-Totality / Boundary×4 / Lifecycle×4. Self-described "constitutional, not analytical" (coverage delegated to the verifiability-triangle audit, refreshed annually).
3. **The ontology** — `THE_USE_CASE_ONTOLOGY.md`: a 21-verb move alphabet (11 structural moves §2.1 + 10 cross-cutting verbs §2.2), 9 proteins (P-1..P-9, §3), the 10-axis master matrix (§4), 16 discriminating predicates P-CMP..P-EXE (§5.0), 3 named design forks (§5.12), the T-I..T-VI totalities. Three sibling passes map code onto it: `.fitness.md` (per-cell ladder verdicts incl. `partial-SILENT-gap`), `.acceptance.md` (adversarial ACs, PASS/PARTIAL/FAIL/ABSENT), `.obligations.md` (obligation census, COVERED/UNCOVERED, phantom-green register).
4. **The deferral space** — `DECISIONS.md` Active-deferrals index (~45 rows, prose triggers) + 23 `H-NNN` Skip stubs in AxiomTests.fs + Bucket-C/D Skip stubs + entry-resident triggers (e.g. the three "decisions owed" forks at DECISIONS.md:20333; the permissions-axis and atomic-envelope triggers living in matrix-status.sh's heredoc, lines 192–212).

The generated projection (`NORTH_STAR.matrix.generated.md`) claims: T-II 84 live / 6 C / 1 D of 122 entries, gate PASS; T-I five-axis ladder L1 5/5 · L2 4/5 · L3 5/5; tolerances 12 named, 3 open.

## 2 The coverage map (law-backed / deferred-with-trigger / silent)

**(i) Law-backed with live witnesses (Bucket A per the file's own per-entry classification):**
~25 L2 axioms + 12 theorems: A1, A2, A4, A6, A12, A15, A18am, A23–A26, A32, A34, A41(+exec), A42, A43, A45, A46, A47, A48, A49; T1, T4, T11–T19; A-Lifecycle-1..4; the espace-invariance corollary; ~14 witnessed L3 entries carried in AxiomTests (L3-S2, S4–S9, S11/A42, X7, C1-parse, C5-preflight, Emission-Logical triad, Boundary-AtomicEmission via `ComposeAtomicWriteTests`); Cluster-B/F laws (A24-stack monad triples, Kleisli H-003, H-050 in-process adjunction, H-052 bidirectional pillar-9, H-054, H-097, H-098, H-008). Notable: **A45/T17/A47/A48 are live laws whose AxiomTests entries are Skip-form prose pointers** (AxiomTests.fs:1606–1662) — live per their named witnesses (verified present: `EstateTests.fs`, `RowFidelityTests`/`FidelityRowsDockerTests.fs`, `ReverseLegCanaryTests.fs`, `ProofManifestTests`), but outside every machine surface (§3 F3).

**(ii) Convention-enforced (Bucket B, structural pin, no discriminating witness):** ~22 axioms — A3, A5, A7–A11, A13/A14/A16, A17, A19, A20, A22, A28–A31, A33, A35, A36, A39, A40 (AxiomTests.fs:194–590). Honest no-op Facts, named as such in the file header (AxiomTests.fs:12–15).

**(iii) Deferred-with-named-trigger:** 9 Bucket-C + 1 Bucket-D Skips (A21, A27, T2, T3, T5–T10 — AxiomTests.fs:349–920); 23 H-NNN stubs each carrying its promotion trigger (H-006..H-099); ~20 open Active-deferral rows (DECISIONS.md:201–247); entry-resident triggers: the three §5.12 forks (DECISIONS.md:20333), the permissions axis ("fires only when a flow must publish grants (the eject)", matrix generated:70), atomic `BEGIN TRAN` (gated on managed-login survey + P7b), slice-ζ byte-stable dacpac (AXIOMS.md:1017–1030).

**(iv) SILENT at map level (claimed in a charter, neither law'd nor stub'd nor indexed):**
- **A44** — a numbered A-series law with 10+ witness files (`MovementIsomorphismTests.fs`, `TighteningBindingTests.fs`, `WriteSignoffTests.fs`, …) but zero AXIOMS.md presence (`grep -c A44 AXIOMS.md` = 0) and zero AxiomTests presence (F2).
- **P-7 eject terminal chain** — the provenance-completeness gate, terminal package, operator-owned declaration (ontology §3 P-7): no `eject` vocabulary in `src/Projection.*` (only `src/Twin.Runtime/*`), no L2/L3 axiom, no Skip stub, no deferral row (decisions-owed #3 covers only the deliverable-shape fork) (F6).
- **Six L3 axioms with "Underwriting. —"** (L3-S10, D7, D10, X5, X10, I10 — PRODUCT_AXIOMS.md:88–290): declared as phase-gated in prose, but with no Skip stub and no index row; visible only to the annually-refreshed prose audit. Named as a known closure (E2/E5) only inside a script comment (verifiability-gate.sh:33–35).
- **The generated T-II table's 31 unclassified entries** — 84+6+1 = 91 of a stated 122; the delta (23 H + 4 live-law pointers + 3 wrap-lost C + 1 comment decoy) appears in no displayed class (F3).

## 3 Findings — deepest first

| ID | Class | One-line claim | Anchor |
|---|---|---|---|
| F1 | M6/M1 | The master matrix's per-cell coverage map (ontology passes 2–4) is ungated prose that has already failed both ways it names (silently-RED cited witness; HOLLOW test), while the gating pattern (M16 / matrix-status) exists and covers only AxiomTests+Tolerance | THE_USE_CASE_ONTOLOGY.obligations.md §0 |
| F2 | M1 | A44 is a numbered law living outside the law registry: zero AXIOMS.md/AxiomTests presence despite 10+ witness files; AXIOMS.md's own headline count is stale twice over | THE_CONFIG_CONTROL_PLANE.md:31; AXIOMS.md:26 |
| F3 | M6 | The generated coverage projection miscounts its own grammar: Bucket-C 6 vs 9 (line-wrap blindness), a comment decoy in the total (122 vs 121), and four LIVE laws (A45/T17/A47/A48) held as Skip-prose pointers invisible to M16, the gate, and the live count | scripts/matrix-status.sh:55–60; AxiomTests.fs:1606–1662 |
| F4 | M2 | L3-CC2 pins "every pass has signature `Catalog -> Policy -> Profile -> Lineage<'output>`" — an implementation shape the legitimate realization outgrew (A41's heterogeneous `RegisteredTransform<'In,'Out>`, six output types); recorded in AXIOMS, never amended at L3 ("V2 Amendments: None today") | PRODUCT_AXIOMS.md:366; AXIOMS.md:1597–1605 |
| F5 | M6 | Deferral triggers are 100% prose even where machine-evaluable now (consumer counts, Bench thresholds, site counts), and the trigger registry is incomplete: the three §5.12 forks + permissions-axis + atomic-envelope triggers live in a dated entry at :20333 and a bash heredoc, not the Active-deferrals index built to catch silent fires | DECISIONS.md:191–199, 20333; matrix-status.sh:192–212 |
| F6 | M1 | The eject — the charter's terminal outcome ("no upstream left"; "the hardest correctness requirement") — has no composed law, no Skip stub, no deferral row; only its ingredients (P-PROV/T13/6.H) are law'd | THE_USE_CASE_ONTOLOGY.md §3 P-7; src/ grep |
| F7 | M2/M6 | The T-I ladder's L1/L3 rungs bind the axis outcome ("a witness exists for this axis") to one hard-coded exact test name per cell inside the generator script; a legitimate alternative or renamed witness cannot speak its axis | scripts/matrix-status.sh:100–104 |
| F8 | M6 | The @ladder tag grammar validates variant↔tag names only; the Axis and Disposition tokens are free strings — a typo'd axis or disposition silently un-caps its axis and the generator OVER-claims, breaking its stated contract | scripts/matrix-status.sh:64–84; Tolerance.fs:25–47 |

### F1 — The ontology's coverage passes are the real coverage map, and nothing gates them (M6/M1)
**Evidence.** `.obligations.md` §0 confesses two live failure classes: "**silently RED** — the cited test does not even pass at HEAD (the 2026-06-02 VO-leak that red-ed the whole `CdcSilenceTests` class while it was cited as COVERED)" and **HOLLOW** (a green test that doesn't discriminate). `.fitness.md` carries per-cell verdicts including `partial-SILENT-gap` ("the cardinal sin"). None of the three passes is generated or gated: `matrix-status.sh` derives only from AxiomTests.fs + Tolerance.fs (:46–49); M16 parses only `citationOf` inside AxiomTests.fs (:111–128). So the finest-grained declared coverage of the outcome space (protein × move × axis cells) rests on hand-refreshed prose that has already drifted in the exact recorded way, while the phantom-Bucket-A defect class was closed for the L2 file only.
**Gap.** The map's honesty machinery stops one stratum above the map's finest claims.
**Candidate reification.** `pass-citation-gate` — an M16-sibling Fact (or script beside matrix-status) that parses every backtick-quoted `File::Test` citation in `.fitness.md`/`.acceptance.md`/`.obligations.md` and asserts name-liveness on the tree; second increment: render the pass-3 PASS/PARTIAL/FAIL/ABSENT tallies into the generated matrix so a COVERED claim over a dead test fails a build, not an annual audit. Contract: *a cell verdict citing a test that does not exist is a red test.*
**Outcome-fluency bought.** The declared outcome space's cell-level coverage becomes machine-honest; the silently-RED class dies at the same gate that killed phantom-Bucket-A. **Effort M.**

### F2 — A44: a law the law registry cannot see (M1)
**Evidence.** `THE_CONFIG_CONTROL_PLANE.md:31` "## 2. The law — `expressible ⇔ reachable` (A44 candidate)"; :208 records its residual stubs RESOLVED to ∅ ("the strongest A44 statement"). Witnesses exist in ≥10 test files (`MovementIsomorphismTests.fs`, `WriteSignoffTests.fs`, `TighteningBindingTests.fs`, `SinkBindingTests.fs`, …). Yet `grep -c A44 AXIOMS.md` = 0 and `grep -c A44 AxiomTests.fs` = 0; AXIOMS.md:26 declares "The current count is **A1–A43 generating T1–T16**" — stale against its own body (A45–A49, T17–T19 present below) and silently skipping the externally-resident A44. CLAUDE.md's load-bearing commitment "Every `AXIOMS.md` change carries its `AxiomTests.fs` entry in the same commit" was never triggered because A44 never entered AXIOMS.md — the number space forked around the discipline.
**Gap.** A fresh agent reading "the canonical L2 surface" (AXIOMS.md's own claim, :17) can never find A44; M16 guards none of its witnesses; the chapter-close AXIOMS↔AxiomTests alignment ritual has no jurisdiction over it.
**Candidate reification.** `A44 lands in the registry` — either the body moves/duplicates into AXIOMS.md with a Fact+citationOf entry (M16 then guards the movement-isomorphism witnesses), or AXIOMS.md carries an explicit "A44 — resident in THE_CONFIG_CONTROL_PLANE.md §2" row plus the AxiomTests pointer. Contract: *every A-numbered law is reachable from AXIOMS.md and carries a gated witness pointer.* Fix the :26 count in the same commit (it is a "restated count" defect by CLAUDE.md §0's own rule).
**Outcome-fluency bought.** The registry's totality claim becomes true; the A-series becomes a closed namespace again. **Effort S.**

### F3 — The generator's grammar is line-anchored grep over wrapped prose; four live laws are machine-invisible (M6)
**Evidence.** `matrix-status.sh:55–59` computes `skips` via `grep -E '\[<Fact\(Skip'` (first attribute line only) then counts `Bucket C|D` within those lines. Verified against the file: 38 first-lines match (including the **comment example** at AxiomTests.fs:16–18 — a decoy the script counts but M16's own parser strips, :117–122); full-text parse finds 37 real Skip attributes of which **9** say "Bucket C" — T2/T7/T10's tokens wrap to continuation lines, so the generated table reports **C=6**. The displayed classification (84/6/1 of "122") leaves 31 entries in no class. Worst members: **A45, T17, A47, A48 — adopted LAWS with live witnesses — encoded as `[<Fact(Skip = "… LAW … witness is LIVE …")>]` pointers** (AxiomTests.fs:1606–1662). They carry no `citationOf`, so M16 does not guard their cited witnesses; the verifiability gate WARNs them as unclassified deferrals; the live count excludes them. This is survival-rule-12's shape ("soft-skips are indistinguishable from passes at summary level") reproduced inside the axiom registry itself. The in-file hand summary (AxiomTests.fs:1666–1700) has drifted too: "41 axioms + 11 theorems = 52 entries", Bucket-A list missing A42/A43/A45+/T12+ — the very block CLAUDE.md §8 says "could be generated".
**Gap.** The honesty projection miscounts, and precisely the newest laws are outside every gate.
**Candidate reification.** (a) Flip the four to `[<Fact>]` + `citationOf` (the existing adopted-law grammar A46/T18/T19/A49 already use, :1706–1756) — M16 then gates their witnesses with zero new machinery. (b) Make the generator multiline-aware and comment-stripping (both idioms already exist in-file at M16, :111–128); display an explicit "adopted-law pointer" and "horizon stub" class so rows sum to total. (c) Generate the bucket summary from the systematic `// Bucket X` tokens, retiring the drifted comment block. Contract: *a live law is countable as live by machine; Skip means only deferred.*
**Outcome-fluency bought.** The T-II projection stops under-counting live laws and mis-stating C; the coverage map's classes become exhaustive. **Effort S.**

### F4 — L3-CC2 states the pass contract against a signature the realization legitimately outgrew (M2)
**Evidence.** PRODUCT_AXIOMS.md:366: "Every pass has the signature `Catalog -> Policy -> Profile -> Lineage<'output>` or …". AXIOMS.md:1597–1605 (A41 slice γ): "The canonical spec's 'unified `Catalog → Catalog`' assumption was **refined at implementation**: passes produce six distinct output types; the `RegisteredTransform<'In, 'Out>` type-parameter shape carries the heterogeneity." The shipped Kleisli arrow is `'In -> Lineage<Diagnostics<'Out>>` with `ComposeState` erasure at the adapter boundary (AXIOMS.md:1641–1672). PRODUCT_AXIOMS.md:468: "V2 Amendments … None today."
**Gap/presupposition.** The L3 law protects an outcome — *every transformation is registered, classified, lineage-carrying, and composable* — but is stated over one uniform signature; the real (and legitimate) realization falsifies the letter while satisfying the outcome. A conformance check written from L3-CC2's text would reject the shipped system.
**Candidate reification.** `L3-CC2 amended (outcome-first)` — restate as: "every transformation site is enumerated as a `RegisteredTransform`, classified DataIntent/OperatorIntent, runs inside the dual writer, and is folded by the registry-driven composer (A41-amended is the underwriting)"; the signature becomes an *example realization*, not the law. First entry in PRODUCT_AXIOMS' empty amendments section, mirroring AXIOMS' amendment discipline. Contract: *L3 laws quantify over outcomes; realizations appear as witnesses.*
**Outcome-fluency bought.** The operator contract survives the next legitimate re-realization (e.g., a second composer) without silent falsity. **Effort S.**

### F5 — The trigger space is prose-only, and the registry misses trigger classes it was built for (M6)
**Evidence.** Every trigger in the Active-deferrals index (DECISIONS.md:201–247) and every H-stub Skip rationale is prose. A large subclass is machine-evaluable today: consumer counts ("0 consumers (session 25)" ×4, :207–210), site counts (H-012 "recurs at ≥3 sites" — its own Skip records a *hand-run* audit: "Audit at 2026-05-22: zero such nested matches found", AxiomTests.fs:1166–1173), perf thresholds (H-006/H-011/H-099 ">50% of wall time / >10s at canary scale" — `Bench` and `bench/baseline-canary.json` exist), scale thresholds (staged-MERGE "≳100k rows/kind", :241). Meanwhile the registry's completeness: the three §5.12 design forks are "named-with-trigger" only inside a dated entry (DECISIONS.md:20333); the permissions-axis trigger ("fires only when a flow must publish grants (the eject)") and the atomic-envelope gate (P7b) live in **matrix-status.sh's hand-written heredoc** (:192–212) and the generated artifact — not the index whose stated purpose is "catches silently-fired triggers" (DECISIONS.md:193–199; CLAUDE.md:93). The hand-stamped "(session 25)" counts are the restated-count defect class CLAUDE.md bans in its own file.
**Candidate reification.** `TriggerProbes` — a test class evaluating the measurable trigger subset (grep-shaped site counts via the M16 self-parse idiom; Bench-threshold probes reading the baseline JSON), failing/WARNing when a deferral's condition holds, i.e. the silent-fire failure mode made structural; plus three index rows (decisions-owed #1–#3) and one row each for permissions-axis and atomic-envelope, pointing at their entry-resident substance. Contract: *a fired trigger is a red (or warned) test, not a hoped-for scan.*
**Outcome-fluency bought.** The deferral half of the outcome space gains the same liveness the law half has. **Effort M.**

### F6 — The terminal outcome (eject) is silent territory (M1)
**Evidence.** The charter's thesis makes the eject the point of the system (ontology §1: "terminates at an eject … the published schema and its complete history are provably, exactly right with no upstream left"; P-7 §3: "**P-PROV is the load-bearing law** … cannot be partially right"). P-7's chain names eject-specific outcomes: the provenance-completeness gate ("verify the full chain reconstructs before publication — the last chance"), the terminal package, the operator-owned provenance declaration. In code: `grep -rln eject src/` hits only `src/Twin.Runtime/*` (the *post*-eject sidecar); no L2/L3 axiom names the composed eject outcome; no AxiomTests stub; no Active-deferrals row (decisions-owed #3 covers only the append-forever-vs-collapsible fork). The ingredients are strongly law'd (T13/6.H durable FTC, refactorlog dedup, codec totality, P-PROV witnesses) — the *composition* is not, and unlike T16 (which got `migrate A B` as its forcing instance) there is no named forcing instance for P-7.
**Candidate reification.** `L3-Eject (candidate) + Skip-with-trigger` — one L3 axiom: "the terminal package reconstructs: `reconstructLatest(genesis, exported chain) = frozen state`, the terminal ChangeManifest is complete, and publication is refused if the chain does not replay" — with an AxiomTests Skip stub whose trigger is "the eject-deliverable fork (§5.12 #3) resolves / P-7's dependent slice opens". Contract: *the terminal outcome is at minimum a named deferral in the executable registry, like every other outcome.*
**Outcome-fluency bought.** The one outcome that "cannot be partially right" stops being the one outcome the coverage machinery cannot see. **Effort S** (naming now; witness later).

### F7 — The ladder binds axis outcomes to hard-coded witness names (M2/M6)
**Evidence.** `matrix-status.sh:100–104`: the five axes' L1/L3 witnesses are exact full test names embedded in the script (`cells='Schema|M3: V2-internal closure …|migrate A B canary: …'`). The honest posture is preserved (a rename ⇒ L0, under-claim), but the *outcome* — "a round-trip witness exists for this axis" — is realized as "this one string exists": a new, stronger, or renamed witness cannot flip its axis without editing the generator, and axis membership is unspeakable from the test side.
**Candidate reification.** `axis self-declaration` — witness tests carry a machine tag (the `@ladder` precedent, already grep-parsed from Tolerance.fs) e.g. `// @axis Data L1` beside the test, and the generator collects tags instead of hard-coding names; the cross-check that each axis has ≥1 L1 and ≥1 L3 tag replaces the name table. Contract: *any test can say which axis outcome it witnesses; the generator aggregates, never enumerates.*
**Outcome-fluency bought.** The five-axis outcome grammar becomes speakable by code; adding a sixth axis (the T-VI note's permissions/transactionality, when their triggers fire) is a tag, not a script edit. **Effort S/M.**

### F8 — The @ladder tag's Axis/Disposition tokens are unvalidated free strings (M6)
**Evidence.** `matrix-status.sh:64–79` cross-checks only variant-name ↔ tag-name alignment. `open_for_axis` (:82–84) matches `$2==ax && $3=="OpenGap"`. A tag `@ladder Foo Schma OpenGap` (typo'd axis) or `@ladder Foo Schema OpenGapp` (typo'd disposition) passes the cross-check while silently detaching the tolerance from its axis cap — the axis flips to "✅ faithful" and the generator **over-claims**, violating its stated contract (":30 The generator UNDER-claims; it never over-claims"). Currently all 12 tags are well-formed (Tolerance.fs:47–249), so the defect is latent.
**Candidate reification.** `tag-token validation` — extend the cross-check: every tag's axis token ∈ the axis set (derivable from the `cells` table / the tag corpus), every disposition ∈ {OpenGap, AcceptedFaithful}; exit 3 otherwise (the existing drift-exit). Two awk lines. Contract: *an unrecognized axis or disposition token is a build failure, not a silent un-capping.*
**Outcome-fluency bought.** The one honesty mechanism whose failure mode is over-claiming loses that mode. **Effort S.**

## 4 Anti-findings (apparent gaps that are correctly out of scope)

1. **No `Move`/`Torsor`/`RowDiff` typed vocabulary mirroring the alphabet.** Consciously refused with discriminating reasons: CONSTELLATION.md §11.2 ("behave like the torsor under discriminating witnesses, never be named like it"), §11.3 (data δ is substrate-fused; the CDC series is its observable; WAVE_6_ALGEBRA §12.3/§12.4). The ontology itself declares the alphabet "*expository* framing (not type names)" (§6.1). And the schema-side grammar IS reified where it matters: `CatalogDiff.ChannelCounts`/`norm` (CatalogDiff.fs:1003–1067) and `ChangeManifest` (move counts + ‖δ‖ + refactorlog xref + CDC count + tolerance residual + applied transforms — ChangeManifest.fs:13–46). The operator's "what did this sprint change?" question has a typed carrier. Not M6.
2. **H-NNN stubs carry no bucket.** By design: the verifiability gate exempts them as "future-feature reservations, not bucketed axioms" (verifiability-gate.sh:18–19). Their trigger discipline is strong (each names bucket-free trigger + prerequisite).
3. **Bucket-B no-op Facts assert nothing.** Deliberate convention-pins, declared in the file header (AxiomTests.fs:12–15); the A/B distinction is the point, not a hole.
4. **T3/T5–T10 held as Skips indefinitely.** Explicitly adjudicated: "explicit re-derivation tests would be ceremony with no marginal lift" (AxiomTests.fs:1693–1695). Honest derived-law posture.
5. **Permissions/Transactionality absent from the five-axis ladder.** Named out-of-ladder *by construction* with the correct category argument ("a non-round-trip dimension is a category error in a round-trip `ToleratedDivergence`", generated matrix:70–72), each with a trigger. The finding that remains is only *where* those triggers live (F5), not that the axes are missing.
6. **PRODUCT_AXIOMS carries no per-axiom coverage status.** By charter: "constitutional, not analytical" (:490); coverage delegated to the triangle audit. The residual (stale Group-Lifecycle claim that A-Lifecycle-4 is "not yet operational", :462, vs its 2026-06-01 promotion at AxiomTests.fs:412; stale "A1–A43 + T1–T16" cross-ref, :474) is drift *within* that charter's own pointers — worth fixing under F2's commit, not a separate finding.

## 5 Already-aligned (exemplary law/outcome alignments)

1. **M16, the citation gate** (AxiomTests.fs:130–161): the registry parses *itself* and fails on any drifted `citationOf` — a verified-once debt retired into a live law about the law file. The exact pattern F1/F3 ask to be extended.
2. **The @ladder OpenGap auto-flip** (matrix-status.sh:16–30): retiring a tolerance variant in code flips its axis to faithful; L2 cannot be hand-marked. Outcome (faithfulness) bound to code state, not prose (modulo F8's latent token joint).
3. **T12's no-cheat discipline** (AxiomTests.fs:954–967; ontology §5.2): the law ships with the input on which a plausibly-named wrong implementation diverges (`applyDiff threads the passed-in catalog … (no-cheat)`) — outcome-first law-writing at its best.
4. **A41 registered ⇔ executed** (AXIOMS.md:1615–1765): the enumeration that states the law is the value the composer folds; bypassing the surface requires bypassing production. A convention promoted to a structure twice (metadata, then execution).
5. **A45/T18/A46/T19/A49 promotion arcs**: candidate-at-chapter-open → Skip-with-named-trigger → trigger fires → live witnesses with `citationOf` (T18/A46/T19/A49 form, AxiomTests.fs:1706–1756). The lifecycle grammar for a law is real and operated — F3 is about the four entries that fell out of it.
6. **`Tolerance` as a closed, fail-closed erasure grammar** (L3-C1; AxiomTests.fs:647–665): the "named, closed erasures" outcome is a DU with a parser that cannot silently widen — the outcome space's *loss* vocabulary is fully speakable in code.
7. **The ladder's anchored full-name matching** (matrix-status.sh:106–113): `grep -F` bounded by backtick delimiters, defeating substring phantoms — the honesty mechanism defends itself against its own cheapest failure.
