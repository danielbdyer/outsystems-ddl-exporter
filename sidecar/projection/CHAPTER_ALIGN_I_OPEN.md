# Chapter open — Alignment I, "The Teleology Chapter": the axis vocabulary rejoins the outcome space

> **Opened 2026-08-16** (operator authorization of the full alignment program — three
> chapters, eight arcs, executed consecutively; the master plan was reviewed and approved in
> plan mode with three operator rulings recorded in `DECISIONS.md` this date). Requirements:
> `AUDIT_2026_08_16_GEOMETRIC_ALIGNMENT.md` Arcs 0 + T, grounded in workpapers a3 (transform
> teleology), a2/a9 (identity/lexicon — the SynthesisConvention and sequence findings), and
> a1 (the extension-marker lift). Axiom candidate **A50** lands with its Bucket-C stub in
> this commit.

---

## 1 — The strategic frame (the axes this chapter is judged on)

**The KPI is teleological truth.** The intent vocabulary (`Classification` /
`OverlayAxis`) is supposed to BE the operator's outcome space — it is what conflict
detection, by-axis registry queries, the manifest's applied-transforms enumeration, and
the policy DSL quantify over. The audit proved it froze while the outcome space moved:
the two youngest operator channels (user matching, bridge retargeting) ride mislabeled as
`Selection` — which structurally blinds `ConflictDetector` to their conflicts — and their
decisions ride the trail as prose `Label`s, invisible to applied/declined egress. At close,
every operator channel is a named axis, every decided axis speaks typed lineage, the
skeleton names what it excludes instead of computing over degenerate defaults, and the two
non-axis correctness defects the audit flagged (three-per-lane sequence identity; the
extension-marker idiom split) are closed.

Judgment axes, in order:

1. **Teleological totality** — `axisOfPolicyAxis` total (A50); `Override`
   total-or-named-refusal; the fired collapse trigger acknowledged and resolved, not
   re-deferred.
2. **Correctness restored** — conflict detection sees identity-plane events; one sequence
   mint across lanes (sink↔live diffs stop fabricating add+remove); one extension-marker
   classifier (whitespace variants stop splitting the estate's answer).
3. **Epistemic honesty at the chain** — preconditions as data (`Requires`/`Produces`);
   the skeleton refuses-or-names instead of silently defaulting topology; classification's
   relationship to config provenance stated as a testable rule.
4. **Byte-discipline** — additive defaults byte-identical; every behavioral change carries
   its DECISIONS entry and named golden re-record; DU appends respect serialization order
   (T1).
5. **Register** — new copy through the Voice catalog; no unvoiced operator-visible line.

## 2 — The wave map (the plan of record)

```
I.0  chapter open + A50 stub                      (this commit)
I.1  SamplePr FS3511 hoists                        (the July-debt fold-in the operator ruled)
I.2  OverlayAxis.Identity + axisOfPolicyAxis       (A50 flips LIVE)
I.3  reclassification of the identity-plane passes (BEHAVIORAL: conflicts can surface)
I.4  SynthesisConvention registry + marker lift    ─┐ identity track
I.5  sequence-lane convergence (escape-hatched)    ─┘ (BEHAVIORAL: one-time re-key)
I.6  ChainStep Requires/Produces + skeleton honesty (BEHAVIORAL: skeleton names exclusions)
I.7  typed AnnotationDetail for the identity passes
I.8  FiringSite registry vocabulary
I.9  config-provenance rule + property
I.10 chapter close (eight-item ritual)
```

Spine I.0→I.1→I.2→I.3, then {I.4→I.5} ∥ {I.6}, then I.7→I.8→I.9→I.10. The master plan
(the approved plan file) carries per-slice file-level specs, the count-pin choreography,
and risks R1–R12; this document carries the frame.

## 3 — Named non-goals (standing for this chapter)

- **No Policy ↔ OverlayAxis structural collapse refactor.** The fired trigger resolves by
  path (a)-lite: `OverlayAxis` stays the canonical intent vocabulary and gains `Identity`;
  `Policy` stays the config aggregate; the total `axisOfPolicyAxis` map IS the
  consult-both-vocabularies answer the deferral was waiting for. Fusing the two types is
  not needed for any audited outcome and would churn every Policy consumer.
- **No new Policy axes.** `Identity` names channels Policy already has; inventing channels
  is feature work, out of an alignment program's scope.
- **The `Supplied<'T>` config-provenance wrapper stays deferred** (I.9 lands the rule table
  + property; the wrapper's trigger is the first mis-wiring the rule catches).
- **`OverlayAxis.Extraction` stays in the deferral index** (its second consumer has not
  materialized; adding it now would be vocabulary without evidence).
- **The 82-site lint disposition and the two transfer-leg Docker reds stay OUT** (operator
  ruling; their own future arcs). The delta-lint gate (82 = 82 substance-normalized) and
  the named-red-set posture ({StagedMergeDeployE2ETests, T18CycleBreakCanaryTests})
  continue unchanged.
- **Sequence rename detection** gains at most a warning extension (I.5 rider); a sequence
  rename CHANNEL is deferred with the existing kind-grain machinery as its trigger.

**Chapter-open findings** (named, not fixed here): none new — this chapter executes an
audit; the audit's workpapers ARE the finding set, and `AUDIT_2026_08_16_GEOMETRIC_ALIGNMENT.md`
§4 carries the correctness-adjacent trio this chapter closes (items 1 and 2) and Chapter
II/III inherit (item 3 is I.6's, closed here too).

## 4 — Where truth lives for this chapter

The execution substance (per-slice files, types, tests, gates, risks) is the approved
master plan; the finding-level ground truth is the audit synthesis + workpapers
(`audits/alignment-2026-08-16/`); the operator rulings are in `DECISIONS.md` (this date).
When they disagree, the workpaper's citation wins on current-code facts, and the code wins
over everything.
