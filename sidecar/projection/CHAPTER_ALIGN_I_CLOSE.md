# CHAPTER ALIGN-I CLOSE — The Teleology Chapter (Arcs 0 + T)

> **Closed 2026-08-16** (same day it opened — eleven slices, one authorization:
> "make a masterful plan to get it done, spare no expense"). The frame is
> `CHAPTER_ALIGN_I_OPEN.md`; the finding-level ground truth is
> `AUDIT_2026_08_16_GEOMETRIC_ALIGNMENT.md` + `audits/alignment-2026-08-16/`
> (a2/a3 were this chapter's primary workpapers). The next chapter is
> **align-II — "The Ruling & the Epistemics"** (Arcs R + E), opening
> immediately per the operator's consecutive-chapters ruling.

---

## 1 — What this chapter claimed, and the witness for each claim

The chapter's charge: the three correctness-adjacent audit defects die, and the
teleology arc (T1–T3) makes the transform registry's vocabulary carry what its
comments carried. Every claim below is LIVE with a same-commit witness.

| # | Claim | Landed at | Executable witness |
|---|---|---|---|
| 1 | The operator outcome space is enumerable — every Policy channel has a designated OverlayAxis; `Override` is total-or-named-refusal, never a silent no-op | I.2 (`b282983`) | **A50** + `PolicyExprTests` (Override(Ordering) is the map's THEOREM; Override(Identity) reaches both identity channels) |
| 2 | The identity plane names itself — userFkReflow / bridgeRetarget / transferReconciliation classify `OperatorIntent Identity`; ConflictDetector SEES them | I.3 (`a2a3728`) | `IdentityAxisRegistryTests` re-anchored; three new H-034 facts (identity events on Selection-removed kinds flag; the two-pronged gate holds) |
| 3 | The synthesis-convention vocabulary is closed — 23 registered conventions, typo-proof mints, grain + reader family queryable | I.4 (`4536a9c`) | **A51** (injectivity + round-trip + the M16-style zero-free-string sweep that CAUGHT TWO conventions every grep missed) |
| 4 | One physical sequence = one identity on every lane — the three-per-lane split dies; legacy tokens parse forever | I.5 (`83b3361`) | A51 prong 4 (legacy-parse-only sweep) + the sequence-grain pin (3 registered / exactly `[OsSequence]` mintable) + SinkDiffView R6 strengthened |
| 5 | Chain assemblies satisfy product preconditions — the skeleton stops computing zero-edge analytics; exclusions are named, voiced diagnostics | I.6 (`a823781`) | **A52** + SkeletonPurityTests (eleven → SEVEN pin; the exact four-exclusion pin; `skeleton.stepExcluded` voiced; producer-derived split) |
| 6 | The skeleton is profile-parameterized — DataIntent's own free variable surfaced (`runSkeletonWith`) | I.6 | SkeletonPurityTests (purity at every profile point) |
| 7 | The identity plane's decisions are typed lineage citizens — applied/declined egress sees user-match + bridge-retarget verdicts; `Label` has a named promotion trigger | I.7 (`1a37a5e`) | `UserFkReflowPassTests` typed pins (byte-identical rendering pinned); `EventProjectionTests` applied/declined split |
| 8 | WHERE a transform fires is a registry fact — seam stand-ins named, F12 dormancy a value, the digest seals the axis | I.8 (`90180b1`) | Bidirectional per-site legs (exact stand-in sets; dormant ∩ live = ∅; Firing-perturbation digest sensitivity) |
| 9 | The config-provenance rule is stated once and pinned per factory; `Supplied<'T>` deferred on a named trigger | I.9 (`ffeaf4c`) | `ConfigProvenanceTests` (completeness: table = chain exactly; drift fails by name with the ruling's why) |
| 10 | The July FS3511 debt is retired — the solution builds Release-wide | I.1 (`e5995d6`) | This close's `TEST_CONFIG=Release scripts/test.sh fast` runs SOLUTION-WIDE (first chapter close that can) |

Laws: **A50, A51, A52** — all landed LIVE same-commit with their AxiomTests facts;
the matrix moved 84 → **87 live** across the chapter (regen verified per touch).

## 2 — Behavioral changes shipped (each named in its DECISIONS entry)

1. **I.3** — new conflicts CAN surface (identity-plane work on Selection-removed kinds
   flags); durable axis strings move for future writes (`identity` where `selection`);
   old rows parse forever.
2. **I.4** — the Espace_Kind marker unifies on Trim + OrdinalIgnoreCase in all three
   lanes (whitespace variants unify; no committed fixture affected).
3. **I.5** — one-time sequence re-key for sequence-bearing estates (episodes /
   catalog.snapshot.json / inputDigest); cross-lane sequence diffs stop fabricating
   add+remove.
4. **I.6** — `--skeleton-only` loses the four silently-degenerate zero-edge analytics
   (absent-with-a-name, never present-and-wrong).
5. **I.7** — identity-plane decisions reach Info-level `transform.applied`/`declined`
   (previously debug `transform.lineage`); trail diagnostic strings byte-identical.
6. **I.8** — the registry digest value moves once (the `firing=` segment).

## 3 — The eight-item ritual, walked

1. **Active-deferrals scan.** Fired and RESOLVED this chapter: the 2026-05-16
   Policy↔OverlayAxis collapse trigger (path (a)-lite, A50 — the type fusion is retired
   as unnecessary, not re-deferred); the `Label` typed-shape trigger (named AND exercised
   in the same chapter, I.7); the F12 dormancy comment (reified as a registry value,
   I.8). Newly DEFERRED, named: `Supplied<'T>` (I.9 — trigger: the first mis-wiring the
   rule table catches); the sequence rename-warning rider (I.5 — trigger: a real
   sequence-rename workflow); X-arc renames and later-arc items per the master plan.
2. **Contract-vs-implementation walk.** The audit's Chapter-I findings (a2-A2-1, a3-F2,
   a3-F4, a3-F5, the fired trigger, the sequence split) each map to a slice above;
   Chapter II/III findings remain in the master plan's spine — none silently dropped.
3. **CLAUDE.md staleness.** §4 survival list re-verified — all fifteen entries still
   true; nothing from this chapter rises to will-bite-you-today (the one candidate, the
   bare-`Identity` Domain/OverlayAxis ambiguity, is compiler-loud FS0001, not silent).
   No Tier-1 reading-order change (the alignment program's operating surfaces are the
   plan file + this close + DECISIONS, all already routed).
4. **README staleness.** `tests/README.md` unchanged and true — the assembly split
   holds; every new test this chapter is a pure `Projection.Tests` file
   (`ConfigProvenanceTests` wired in the fsproj).
5. **HANDOFF scope.** The top letter is rewritten forward-looking: Chapter II (Arcs
   R + E) is the next program; the letter names its spine and the II.1 design ruling
   (keyed ruling store, NOT a 5th LedgerSpec).
6. **Fresh-eye walk.** The chapter-open's judgment axes were re-read against the landed
   code: every axis has a law or a named deferral; the wave map's DAG was honored
   (I.4→I.5 dependency, I.6 after I.2/I.3, X-arc untouched as ruled).
7. **Operating-disciplines currency.** Two disciplines EXERCISED, none new: "audit
   during the work" (A51's first execution corrected the corpus count the audit and the
   grep both got wrong — the law found what greps missed); "the compiler is the sweep"
   (I.8's FS0764-driven fan-out). The delta-lint gate also guarded itself (I.8's Dormant
   trigger text tripped set-assign; reworded before commit).
8. **V1-input-envelope walk.** Not a V1-input chapter (registry/identity/chain
   vocabulary only) — skipped by class.

## 4 — Anti-findings (what was deliberately NOT done)

- **No Policy ↔ OverlayAxis type fusion** — the total map IS the resolution (operator
  ruling at I.0; the collapse refactor retired).
- **No new Policy axes, no auto-application of anything** — vocabulary and carriage
  only.
- **Exemplary surfaces untouched** — WriteSignoff / ActConsent / ApprovalWorkflow /
  CatalogDiff / SinkDisplacement stayed put per the workpapers' anti-findings.
- **The 82 lint sites and the two transfer-leg Docker reds stayed OUT** (operator
  ruling 2); delta-lint held 82 = 82 substance-normalized at every one of the eleven
  commits.
- **The escape hatch stayed cold** — I.5's convergence fit its slice; no deferral
  needed.

## 5 — Verification at close

- `TEST_CONFIG=Release scripts/test.sh fast` — SOLUTION-WIDE (post-I.1), green.
- Full Docker pool — known-red set exactly `{StagedMergeDeployE2ETests,
  T18CycleBreakCanaryTests}` (the two named transfer-leg reds, out of scope per
  operator ruling 2).
- `scripts/verifiability-gate.sh` — 87 live, gate PASS.
- `scripts/matrix-status.sh` — current (regenerated at I.4 and I.6; untouched
  otherwise, verified per commit).
- `scripts/lint-discipline.sh` — 82 = 82 vs the `825671c` baseline, substance-normalized,
  every commit.
- `scripts/run-analyzers.sh` — 0/0 on every Core-touching slice.
- `scripts/perf-gate.sh` — clean solo at every commit.

Eleven commits, `482c0e6..ffeaf4c` + this close, all on PR #695 per the stacking
ruling.

## 6 — Forward

Chapter II — "The Ruling & the Epistemics" (Arcs R + E, fourteen slices) opens
immediately: the `OperatorRuling` carrier + keyed store, tightening provenance,
per-subject index rulings, abstain honesty, estate reception, the `projection rule`
verb; then the epistemic track (RowsetContract, typed erasures, AcquisitionScope,
journal read-side, typed fingerprints, finding pedigree). The master plan's Chapter II
spine governs; II.0 expands it.
