# Chapter B.4 open — operator-facing CLI + logging-format contract + LiveProfiler hygiene close

**Branch:** `claude/chapter-b4-opening-vGe7J`. **Predecessor:** chapter B.3 close (2026-05-19; merged via PR #550). **Chapter shape:** rescoped at chapter-mid (2026-05-19; see `DECISIONS 2026-05-19 (slice B.4.{4-7}.rescope)`) then expanded by one slice (2026-05-20; see `DECISIONS 2026-05-20 (logging-format implementation gap audit)`) to **8 effective slices** (chapter-open's 8 → rescope's 7 → gap-audit's 8 with the structural slice 6.5 inserted); axiom-first ordering preserved (logging-format contract led); hygiene close folded in (slices 2 + 3); CLI work narrowed to a single `full-export` subcommand per operator framing — `extract` + `profile` standalone subcommands dropped; multi-environment + UAT-users + LiveOssysConnection + `data-twin` Docker verb + Spectre TtyRenderer micro-chapter explicitly deferred to follow-up chapters.

This chapter operationalizes **V2_PRODUCTION_CUTOVER §7.5 (Phase B.4)** — Phase B's *structural* terminal chapter. V2 owns the operator-facing `full-export` subcommand end-to-end against snapshot connectivity (`SnapshotJson` / `SnapshotRowsets`); the V2 logging-format contract landed at slice 1 (`sidecar/projection/docs/logging-format.md`) and proposes two new L3 axioms (L3-X11: structured event-stream conformance; L3-X12: actionable events carry `suggestedConfig`) for the verifiability-triangle catalog, both landing at chapter close in Bucket A; `ModuleFilter` + `MetadataContractOverrides` ports carbon-copy from V1 at slices 4 + 5; slice 6 (new) operationalizes §12 of the logging contract on the existing JSON-artifact emitters so V2's decision-log / opportunities / validation outputs land **actionable** (suggestedConfig payloads + filter/cluster/cap on noisy axes — V1's diagnostic JSONs are noisy/cluttered by operator report); slice 7 (rescoped from old slice 8) wires `full-export` against the slices-4+5+6 surface and emits SSDT project + seed/migration scripts + actionable diagnostics. The post-slice-6b dead-code SQL captures retired at slice 2; the slice-1 composite-PK FK deferral resolved out-of-scope at slice 3 per principal-PO answer. **DACPAC binary + `data-twin` (DockerImageEmitter wrapper) CLI verb are NOT in this chapter** — they surface under a separate operator-paced workflow chapter; the DockerImageEmitter substrate already shipped at chapter 3.x close. **LiveOssysConnection + multi-environment + UAT-users are NOT in this chapter** — the operator's V1 managed-environment remote HEAD owns those today; V2 picks them up under a future chapter when the managed-environment access path is available. Estimated effort post-rescope: ~1.5 weeks for the four remaining slices.

## Strategic-frame axes

1. **Axiom-promotion is the chapter's load-bearing deliverable.** Slice 1 ships `sidecar/projection/docs/logging-format.md` first; everything downstream (slice-6 JSON-artifact actionability; slice-7 `full-export` CLI; future operator-tooling consumers) consumes that contract. Under partial-chapter pressure, the axiom-shape document is the highest-leverage artifact — operator's downstream-tooling rewrite (per `V2_PRODUCTION_CUTOVER §7.5`) gates on its existence, not on the CLI's. The contract proposes two new L3-X axioms (L3-X11 + L3-X12) for catalog entry at chapter close per the verifiability-triangle audit cadence; the chapter-open scoping treated them as a single "L3-Operational-LoggingContract" placeholder, but the catalog convention is `L3-X#` under §3.4 — see `docs/logging-format.md §16` for the exact statements. Slice 6's actionability work is L3-X12's structural cash-out on the artifact-side surface (event-stream side is L3-X11; the two are paired).

2. **Hygiene close folded in, not deferred.** Slices 2 + 3 retire the five `capture*` SQL surfaces (`captureAttributeRealities`, `captureColumnProfiles`, `captureForeignKeyRealities`, `captureCompositeUniqueCandidates`, `captureForeignKeyOrphanSamples` — all unused by `LiveProfiler.attach` post-slice-6b) and cash out the slice-1 composite-PK FK deferral (`Outcome = AmbiguousMapping` early-return; slice-6b's `projectTupleKeys` makes the multi-column Set.difference trivial). Both surface as chapter B.3 close-letter open questions; folding them into B.4's front cleans the LiveProfiler deck before slices 7-8 reference the live-probe path through CLI. Per the chapter-close-ritual discipline: chapter open absorbs the prior chapter's named open questions rather than punting them.

3. **CLI surface narrowed to one subcommand: `full-export`.** The chapter-open named three CLI subcommands (`projection extract` / `projection profile` / `projection full-export`); the rescope (2026-05-19 chapter-mid) drops the standalone `extract` + `profile` subcommands. Operator framing: the single-command shape is the V2-driver acceptance surface; standalone extract / profile carry no operator-paced workflow demand independent of full-export. `full-export` reads a unified config (per `Pipeline.Config` D9 connection-source resolution against `SnapshotJson` / `SnapshotRowsets` only — `LiveOssysConnection` deferred); operator UX is config + `--output <path>` override only; emits structured log events conforming to the slice-1 contract; ships with a Docker-gated integration test exercising end-to-end snapshot→SSDT+seeds+migration emission with actionable diagnostic JSONs.

4. **V1 inheritance via carbon-copy editorial discipline.** `ModuleFilter` (V1) and `MetadataContractOverrides` (V1) are slices 4 + 5 — carbon-copies per `DECISIONS 2026-05-16 (later) — V2 self-containment + carbon-copy editorial inheritance`. File-header citation comments name the V1 source + date; ADMIRE.md rows record the inheritance event; refactor at copy-time or in follow-up. V2 vocabulary applies (eventually) on every file.

5. **Pillar 9 — config-driven CLI carries OperatorIntent through registered transforms.** The CLI subcommands' configuration is operator-supplied intent; per pillar 9 + `L3-CC-Transform-Totality`, every operator-supplied choice surfaces as a registered transform with classification. `ModuleFilter` is `OperatorIntent of Selection`; `MetadataContractOverrides` is `OperatorIntent of Tightening` or `OperatorIntent of Emission` (per-axis classification at slice land time). The logging-format contract (slice 1) names the event categories that carry these classifications.

6. **Faker emitter stays deferred-with-trigger-met.** Per chapter B.3 close, the Active deferrals row remains `TRIGGER STRUCTURALLY MET — awaiting explicit promotion`. This chapter does not promote it. Rationale: cutover-window priority routes structural work to operator-facing CLI surface first; Faker awaits concrete consumer demand. No DECISIONS update; status unchanged.

7. **Chapter close = Phase B *structural* exit gate (functional-equivalence gate moves to a follow-up chapter).** Phase B's exit criterion (per `V2_PRODUCTION_CUTOVER §8.2`) requires the L3 catalog reaches its target bucket, V2 `full-export` runs against OSSYS and produces functionally-equivalent `osm_model.json` to V1, V2 profile probes produce functionally-equivalent `Profile` data, V2 logging format documented, ≥1 full end-to-end production dry-run completed. The rescope splits this: chapter B.4 closes the **structural** surface — `full-export` ships, logging contract documented, JSON artifacts actionable, ModuleFilter + MetadataContractOverrides ported — against `SnapshotJson` / `SnapshotRowsets`. The **functional-equivalence** arm (V2 `full-export` runs against OSSYS and matches V1's `osm_model.json` byte-for-modulo-tolerance) moves to a follow-up chapter once `LiveOssysConnection` lands; the operator's V1 managed-environment remote HEAD owns the live path until then. Phase B exit is gated on both arms; B.4 carries the first.

8. **No CDC governance dry-run in this chapter.** That work belongs to chapter 4.1.B's CDC-silence canary (shipped). This chapter is structural CLI shape; the per-CDC-table multi-redeploy property holds from chapter 4.1.B onward and is not re-validated here.

## Slice plan (8 effective slices, post-rescope + post-gap-audit; 8 DONE — chapter ready for close ritual)

Slice naming uses `B.4.<n>.<axis>` for chapter coherence. Each slice ships its own commit + DECISIONS entry. **Axiom-first ordering**: slice 1 was the load-bearing axiom-promotion deliverable; slices 2 + 3 were the hygiene close; slices 4 + 5 are V1 inheritance ports; slice 6 (new shape) operationalizes the logging contract's §12 actionability discipline on the existing JSON-artifact emitters; slice 7 (rescoped from old slice 8) wires `full-export` against the slices-4+5+6 substrate. The chapter-open's old slices 6 + 7 (`projection extract` + `projection profile` standalone subcommands) are dropped — see `DECISIONS 2026-05-19 (slice B.4.{4-7}.rescope)`.

| Slice | Scope | Status |
|---|---|---|
| **1** B.4.1.logging-format-contract | `sidecar/projection/docs/logging-format.md` — defines V2's structured event categories (config / extract / profile / transform / emit / deploy / canary / summary), envelope (runId / ts / level / category / code / phase / source / ssKey / stepId / payload), encoding (UTF-8; line-delimited JSON to stderr; one event per line), classification taxonomies carbon-copied verbatim from V1 (TighteningRationale + ProbeOutcome + TransformSource), the terminal `summary.runComplete` event + roll-up collapse algorithm, the `suggestedConfig` actionable-payload discipline, and 12 banned antipatterns. Proposes L3-X11 + L3-X12 for catalog entry at chapter close. The doc lands BEFORE downstream subcommands consume the contract; slice 6 lifts §12 onto the JSON artifacts; slice 7 emits conforming events at the CLI. | **DONE** |
| **2** B.4.2.capture-retirement | Retire the five unused `capture*` SQL surfaces from `LiveProfiler` (`captureAttributeRealities` / `captureColumnProfiles` / `captureForeignKeyRealities` / `captureCompositeUniqueCandidates` / `captureForeignKeyOrphanSamples`) — all dead post-slice-6b. The seven `LiveProfilerIntegrationTests` direct-SQL tests update to assert on `Cache.derive*` output (cache equivalence is structurally proven; SQL parity-witness no longer earns its keep). Net: ~1500 LOC of SQL-capture code retires; tests reshape but count preserved. | **DONE** |
| **3** B.4.3.composite-pk-fk | **RESOLVED OUT-OF-SCOPE.** At slice open, surfacing the design choice to the principal-PO produced the canonical answer: composite primary keys are not an OS use case the operator has encountered; the slice's premise (V2 probes FK reality against composite-PK targets) does not match operator-reality demand. The slice-1 `AmbiguousMapping` outcome stands as the correct answer for the degenerate case. Documentation-only change: clarifying comments at the two `ambiguous` branches in `Cache.deriveForeignKeyRealitiesWith` + `Cache.deriveForeignKeyOrphanSamplesWith` point at `DECISIONS 2026-05-19 (slice B.4.3.composite-pk-fk)`. The cross-module FK reserved IR refinement deferral remains separate and untouched. | **DONE — RESOLVED OUT-OF-SCOPE** |
| **4** B.4.4.module-filter-port | Carbon-copy `ModuleFilter` from V1 per `DECISIONS 2026-05-16 (later)`. Landed at `Projection.Core/ModuleFilter.fs` — single F# file consolidating V1's three donor files (executor + options + per-module-entity options). V1's per-module `IsSystemModule` bit translates to V2's per-kind `ModalityMark.SystemOwned` aggregate. V1's `ValidationOverrides` axis routed to slice 5 (`MetadataContractOverrides`) as its natural home. 30 new tests pass; build clean. TransformRegistry registration + `transform.applied` event wiring deferred to slice 7 where the orchestrator invokes the filter. See `DECISIONS 2026-05-19 (slice B.4.4.module-filter-port)` + `ADMIRE.md` 2026-05-19 ModuleFilter entry. | **DONE** |
| **5** B.4.5.metadata-contract-overrides | V1's `MetadataContractOverrides` (SQL-extraction column-relaxation surface) carbon-copied to `src/Projection.Adapters.OssysSql/MetadataContractOverrides.fs` (~290 LOC F#); 25 new tests pass; build clean. **Mechanism shipped; wiring deferred** — V2 has zero direct wiring carry-over (V1's sole consumer reads the V1-SUNSET `attrJson` rowset V2 skips); two new Active deferrals added (wiring into V2 mappers + `OverlayAxis.Extraction` candidate). **Chapter-open framing audit**: slice's stated "per-attribute tightening / emission overrides" framing was broader than V1's actual `MetadataContractOverrides`; V2's per-attribute tightening overrides already exist structurally as `Policy.TighteningOverride` (`Policy.fs:118`), so slice 7 wires the operator config to the pre-existing V2 mechanism. See `DECISIONS 2026-05-19 (slice B.4.5.metadata-contract-overrides)` + `ADMIRE.md` 2026-05-19 MetadataContractOverrides entry. | **DONE** |
| **6** B.4.6.actionable-diagnostics | **NEW (added at rescope; reshaped mid-implementation per principal-operator pushback against occluding source defects — see `DECISIONS 2026-05-20 (slice B.4.6 reshape — drop occluding cluster-cap)`).** Operationalize logging-format contract §12 (`suggestedConfig` actionable-payload discipline) on the existing JSON-artifact emitters (decision-log.json + opportunities.json + validation.json) + severity-sort + axis-cluster for navigation. **No occlusion** — every input entry surfaces in the output. Two-part work shipped: (a) `SuggestedConfig` typed record added to `Projection.Core/Diagnostics.fs` with smart constructor (rejects blank path; normalizes blank note to None); `DiagnosticEntry` gains `SuggestedConfig : SuggestedConfig option` field absorbing extension at one site per the slice-5.13.smart-constructor-lift pattern; `DiagnosticEntry.create` smart constructor lands sibling to `Attribute.create` / `Kind.create`; JSON emit path writes `suggestedConfig: { path, value, note }` per §12 when present, omits when None for back-compat with pre-slice-6 consumers. (b) New `Projection.Targets.OperationalDiagnostics/ActionableDiagnostics.fs` module: `Axis.tryFromCode` extracts top.sub two-segment cluster key from `Code`; `ActionableDiagnostics.organize` severity-sorts (Error > Warning > Info) + clusters by axis (sorted by axis name; unclustered entries at tail in input order) — pure navigation reshape, no entries dropped, no overflow markers. All three emitters' `emit` entry points call `ActionableDiagnostics.organize` before `buildArtifact`. Property tests verify the no-occlusion invariant (`result.Length = input.Length`) + idempotence + severity ordering within axis. 28 new tests pass; build clean; pillar 9 classification: pure DataIntent enrichment. Structural cash-out of L3-X12 on the artifact side; pairs with slice-1's event-stream side. **Per-finding-type emission gates** (reducing noise at the source via operator-tunable thresholds like `NullabilityTighteningConfig.NullBudget`) are a separate concern — that's Chapter C slice C.1 (tightening axis) cash-out, not this slice. | **DONE** |
| **6.5** B.4.6_5.logsink-rollup | **NEW (inserted 2026-05-19 per `DECISIONS 2026-05-19 (logging-format implementation gap audit)`).** `Projection.Pipeline/LogSink.fs` ships the structural emission substrate the chapter B.4 close gate requires — slice 1's logging-format contract specified the wire shape, but zero implementation landed. This slice fills the gap: hand-rolled `LogSink` per §15.2 ("no logger primitives — LogSink is hand-rolled"); NDJSON envelope serialization to stderr via `System.Text.Json`; **§11 roll-up aggregator** maintaining a `Dictionary<(category, code, ssKey option), GroupAccumulator>` during emission; terminal `summary.runComplete` event carrying `aggregates` array (group key 3-tuple; first-three-by-chronological-order samples; `Bench.Stats` per-label aggregates surface under `category=summary, code=bench.label`). Per §11 Big-O audit: groups built once during stream emission, not re-scanned at runSummary time. Property tests verify the rollup algorithm (group-key collapse + count accumulation + sample selection + bench-stat surfacing). **Required by slice 7's chapter-B.4 close gate** ("the default behavior emits conforming events" per §15.1). | **DONE** |
| **7** B.4.7.full-export-cli | **RESCOPED twice** — once at chapter-mid (2026-05-19) per `DECISIONS 2026-05-19 (slice B.4.{4-7}.rescope)`, then again at strategic exploration (2026-05-19) per `DECISIONS 2026-05-19 (chapter B.4 mid-chapter strategic exploration)`. `projection full-export --config <path> [--output <dir>] [--verbose]` CLI subcommand — **THIN scope**: wraps today's 3 wired `Pipeline.Config` consumers (`Model.Path` → `CatalogReader.parse`; `Overrides.TableRenames` → `TableRename.registered`; `Output.Dir` → `Compose.write`) + the slice-6 actionable-diagnostics enrichment + the slice-6.5 `LogSink` emission substrate + the `SnapshotJson` / `SnapshotRowsets` connectivity. Dormant config sections (Profile / Cache / Profiler / TypeMapping / Emission booleans / Policy.Selection / Policy.Insertion / Policy.UserMatching / Overrides.MigrationDependencies / Overrides.StaticData / Overrides.CircularDependencies) continue to parse-but-ignore — operator hand-writing future-shaped configs gets no surprises. The broader operator-facing surface (axes 2–6 + 7a + 9-revised) sequences into a sibling Chapter C (6 slices). Axis 3 (operator-supplied static seed data file format) deferred-with-trigger. Spectre channel-2 TtyRenderer (axis 7b per the contract §15.3 post-chapter framing) deferred to its own micro-chapter alongside the `data-twin` verb. **DACPAC + Docker-image emit are NOT here** — `data-twin` CLI verb is a separate chapter. In-process integration tests verify end-to-end snapshot→SSDT+JSON+Distributions+actionable-diagnostics flow + NDJSON event-stream conformance + `summary.runComplete` rollup correctness (10 tests; Docker-gated test deferred — the in-process suite catches every conformance regression at unit-test speed). **Phase B *structural* exit gate** runs here; functional-equivalence arm waits on a future `LiveOssysConnection` chapter. | **DONE** |

**Chapter close ritual** runs after slice 7. Eight items per `CLAUDE.md` operating disciplines: Active deferrals scan (verify Faker still trigger-met / consumer-pressure absent; check composite-PK FK rolled off; verify five retired captures no longer linked; check the new deferrals — `LiveOssysConnection` / multi-environment / UAT-users / `data-twin` CLI verb / Spectre TtyRenderer micro-chapter — are indexed with named trigger conditions); contract-vs-implementation walk on the new `LogSink` + `full-export` subcommand + actionable-diagnostics emitter (verify slice-1 contract §11 rollup algorithm + §15.1 channel-1 NDJSON discipline are operative in slice-6.5's `LogSink`); CLAUDE.md / README.md staleness (README pointer at CLI surface); HANDOFF + close-doc scope; fresh-eye walk; operating-disciplines table currency; **V1-input-envelope walk** (V2 vs V1 osm_model.json + Profile JSON byte-comparison for the canary fixture; structural arm only — functional-equivalence arm waits on live path); per-axis-stakes evaluation against V2_DRIVER (this chapter advances Schema + Data + Operational-Diagnostics + Identity axes; no advance on User-FK or RefactorLog).

**Bonus close item**: L3-X11 + L3-X12 catalog entry. Slice 1's contract proposes both axioms; chapter close lands the entries in `AUDIT_2026_05_12_VERIFIABILITY_TRIANGLE.md §3.4` + coverage-map row in §10.4 (Bucket A — the doc landed; slice 6 lifts the artifact-side actionability; slice 7 emits conforming events at the CLI; property tests + Docker-gated integration tests verify).

## Out of scope

- **Standalone `projection extract` + `projection profile` subcommands.** Dropped at chapter-mid rescope (2026-05-19); operator framing names no operator-paced workflow that consumes these independently of `full-export`. Trigger to re-open: a concrete operator-paced workflow surfaces that needs catalog-extract or profile-attach as a stand-alone step (e.g., a CI lane that materializes the snapshot without emitting). Until then, `full-export` is the operator-touchable surface.
- **`LiveOssysConnection` variant of `SnapshotSource`.** Reserved in `SnapshotSource` DU (`CatalogReader.fs`); the operator's V1 managed-environment remote HEAD owns the live OSSYS path today. V2 picks this up under a future chapter when the managed-environment access path is available. Until then, B.4's `full-export` runs against `SnapshotJson` + `SnapshotRowsets` (chapter 3.2) only. **Phase B's functional-equivalence exit-gate arm** waits on this chapter; the structural arm closes in B.4.
- **Multi-environment configuration (DEV / TEST / UAT / PROD).** The operator's V1 managed-environment remote HEAD already has multi-environment configuration fully done; V2 will pick this up under a future chapter when the operator can share the V1 implementation. B.4's `full-export` uses a single-environment config shape.
- **UAT-users logic.** Same as multi-environment — V1 managed-environment remote HEAD has it fully done; V2 picks it up under a future chapter.
- **`data-twin` CLI verb.** The `DockerImageEmitter` substrate shipped at chapter 3.x close (`CHAPTER_3_X_CLOSE.md`) — it produces a runnable `DockerImageContext { Dockerfile; DacpacBytes; EntrypointScript; Readme }` ready for `docker build .`. The operator framing names this as a separate chapter ("one-shot SQL Server replica that's ready to be dockerized" — the data-twin verb wraps the existing emitter as a CLI surface for the dev team's one-click stand-up workflow). B.4 does NOT ship the verb; the surfacing chapter sequences after the operator's V1 managed-environment material lands so the verb can be designed against the full operator-paced workflow context.
- **DACPAC binary emission in `full-export`.** The DacpacEmitter substrate exists (chapter 3.x); B.4's `full-export` deliberately scopes to SSDT + seeds + migration + actionable diagnostics only. DACPAC surfaces under the `data-twin` CLI verb's chapter, not under `full-export`'s output set.
- **Spectre channel-2 TtyRenderer.** Per logging-format contract §15.3 + the 2026-05-20 implementation-gap audit: the `--pretty` flag + `Projection.Cli/TtyRenderer.fs` Spectre.Console adapter + `--json-out <path>` dual-channel routing is the operator-facing UX layer over slice-6.5's `LogSink` (channel 1; the structural commitment). The contract explicitly frames TtyRenderer as post-chapter ("optional for chapter B.4; the chapter close gate does NOT require it — it's a post-chapter slice if operator feedback warrants"). Surfaces as its own micro-chapter alongside the `data-twin` verb when operator-pull demand materializes (operator runs CLI + reports NDJSON-only stderr emission as unfriendly for interactive runs).
- **Faker emitter promotion.** Per chapter B.3 close, the Active deferrals trigger is structurally met (all four `ADMIRE.md`-named gating evidence chain nodes shipped). This chapter does NOT promote; status remains `TRIGGER STRUCTURALLY MET — awaiting explicit promotion`. Re-evaluate at chapter B.5 or 5+ open under concrete consumer demand (per the principal-PO's cutover-window priority).
- **`MaxIdentityValueQueryBuilder`** (Q1 cash-out per V2_PRODUCTION_CUTOVER §7.4 B.3 task list). Independent of CLI shape; surfaces under chapter 4.x consumer pressure if at all. Not blocking Phase B's structural exit.
- **`ProfilingQueryExecutor` (672 C# LOC orchestration port).** Per chapter B.3 close: V2 already has F#-shaped composition primitives; orchestration shape lifts inline at each `LiveProfiler` capture function. The 672 C# LOC is V1's plumbing; V2's plumbing is the Task-monad + Result-monad composition. Cut at this chapter open.
- **Cross-module FK IR refinement.** Reserved DU variant remains; trigger condition (DacpacEmitter cross-database reference; multi-V1-SS-instance OssysOriginal) does not fire in this chapter.
- **CSV adapter for `ManualOverride` (UserMapLoader).** Chapter 4.2 deferral; surfaces under concrete operator workflow demand. Not blocking Phase B's structural exit.
- **CDC governance dry-run.** Per chapter 4.1.B close + V2_DRIVER per-axis stakes: dry-run runs on operator's production-CDC-enabled tables, owned by operator, scheduled jointly. Not gated on this chapter's close.

## Dependency map (post-rescope)

```
Slice 1 (logging-format contract) — DONE
  └─ no upstream dependency
  └─ DOWNSTREAM consumer: slice 6 lifts §12 (suggestedConfig
     discipline) onto JSON artifacts; slice 7 emits conforming events
     at the CLI

Slice 2 (capture retirement) — DONE
  └─ no upstream dependency (post-slice-6b dead code retires)
  └─ DOWNSTREAM consumer: slice 7's `full-export` references the
     live-probe path via `LiveProfiler.attach`; retiring dead captures
     first prevents the new CLI surface from accidentally
     re-introducing references

Slice 3 (composite-PK FK cash-out) — DONE (resolved out-of-scope)
  └─ no upstream dependency (documentation-only)
  └─ DOWNSTREAM consumer: none

Slice 4 (ModuleFilter port) — next
  └─ no upstream dependency (carbon-copy)
  └─ DOWNSTREAM consumer: slice 7's `full-export` reads ModuleFilter
     from config

Slice 5 (MetadataContractOverrides port)
  └─ no upstream dependency (carbon-copy)
  └─ DOWNSTREAM consumer: slice 7's `full-export` reads
     MetadataContractOverrides from config

Slice 6 (actionable diagnostics) — NEW post-rescope
  └─ depends on slice 1 (logging-format contract §12)
  └─ DOWNSTREAM consumer: slice 7's `full-export` writes the
     enriched JSON artifacts as part of the emit-chain output

Slice 6.5 (LogSink + roll-up emission) — NEW post-gap-audit
  └─ depends on slice 1 (logging-format contract §11 rollup +
     §15.1 channel-1 NDJSON + §15.2 hand-rolled-LogSink disciplines)
  └─ DOWNSTREAM consumer: slice 7's `full-export` emits via
     `LogSink.emit`; chapter-close-gate conformance verifier asserts
     `summary.runComplete` rollup correctness

Slice 7 (full-export CLI) — RESCOPED from old slice 8
  └─ depends on slices 1 + 4 + 5 + 6 + 6.5
  └─ Phase B *structural* exit gate runs here; functional-equivalence
     arm waits on `LiveOssysConnection` (deferred)
```

## Anchor commitments preserved across the chapter

These don't shift during B.4. If a slice trips one, the discipline is to write the amendment first.

- **F#-pure-core / no-I/O-in-Core.** CLI subcommands live in `Projection.Cli`; live SQL probes live in `Projection.Adapters.Sql`. Core stays I/O-free.
- **A18 amended.** CLI subcommands route through `Compose.run` (chapter A.4.7'); no emitter consumes Policy directly.
- **Pillar 9 — DataIntent / OperatorIntent classification.** Every CLI option that affects projection output is `OperatorIntent` with explicit `OverlayAxis` classification (Selection / Emission / Insertion / Tightening). The logging-format contract names how the classification surfaces in event payload.
- **Smart-constructor-FIRST.** Any new IR aggregate (none expected this chapter) gets `.create` before the slice that consumes it.
- **EvidenceCache discovery-then-derive.** Slice 7's `projection profile` consumes the existing `LiveProfiler.attach` cache pipeline; no new SQL probes land. New evidence shapes (none expected) would land as `Cache.deriveX` per the chapter B.3 codification.
- **V2 self-containment.** Carbon-copy discipline at slices 4 + 5; file-header citations + ADMIRE rows; no V1 ProjectReference.
- **Sibling-wrapper discipline.** CLI subcommands likely surface `runX` + `runXWith config` pairs; the wrapper supplies the config-default path; the canonical surface is the With variant.
- **R6 split-brain governance.** V2 emits-but-doesn't-ship while V1 owns the production write path. CLI subcommands write to disk; they do NOT touch the production DB. The canary asserts equivalence; this chapter does not change R6.
- **V1 stays warm through cutover+30.** Phase B exit at slice 8 close does NOT mean V1 retires; V1 sunset begins at chapter 5+.

## Test baseline at chapter open

- **1695 / 1695 non-Docker** baseline at B.3 close.
- **33 / 33 LiveProfiler Docker-gated** integration tests at B.3 close.
- **0 build warnings** under `TreatWarningsAsErrors=true`.

Slice 2 reshapes 7 tests (SQL-direct → cache-derivation assertions); slice 3 is documentation-only (no test change). Slices 4 + 5 add ~6-10 carbon-copy tests (V1's `ModuleFilter` + `MetadataContractOverrides` test surface mirrored). Slice 6 adds ~8-12 property + example tests (suggestedConfig non-empty invariant; cluster-cap FsCheck property; per-axis filter ordering). Slice 6.5 adds ~10-15 tests on `LogSink` (envelope-shape conformance per §3; rollup-aggregator group-key collapse + count accumulation + sample-selection invariants per §11; `Bench.Stats` surfacing under `category=summary, code=bench.label`; `summary.runComplete` end-of-run emit). Slice 7 adds 1 Docker-gated `full-export` end-to-end integration test (no separate extract / profile subcommand tests post-rescope) plus envelope-conformance verification against `LogSink.emit` output. Expected close: ~1725-1740 non-Docker + ~34 Docker-gated; build clean.

## Reading order for the slice agent (recurrence reference)

1. **This document** (~5 minutes; the chapter strategic frame + slice plan).
2. **`HANDOFF.md`** — latest letter at the top; the next-agent orientation block from chapter B.3 close.
3. **`CHAPTER_B_3_CLOSE.md`** — the substantive contributions that earn this chapter (DATA-axis silent-default closure; EvidenceCache substrate; Faker evidence-chain complete).
4. **`V2_PRODUCTION_CUTOVER.md §7.5`** — the chapter's task-list reference.
5. **`V2_DRIVER.md` per-axis stakes table** — verify each slice advances at least one named axis; logging-format contract sits under Operational diagnostics (lower verification depth) but the L3 promotion is structural.
6. **`AUDIT_2026_05_12_VERIFIABILITY_TRIANGLE.md §3.4 (L3-X axioms)** — slice 1 reserves L3-X11 + L3-X12; chapter close adds them to the catalog. Read the existing L3-X1 through L3-X10 statements to mirror the convention.
7. **Active deferrals index at `DECISIONS.md`** — slice 2 + 3 closing-confirmation; Faker row stays unchanged.
8. **`src/Projection.Cli/Program.fs`** — current CLI surface; slices 6-8 extend it.
9. **`src/Projection.Pipeline/Config.fs`** — current `Config` record; slices 4 + 5 extend it.
10. **`src/Projection.Adapters.Sql/LiveProfiler.fs`** — current capture surfaces; slice 2 retires 5 of them; slice 3 amends FK reality for composite PKs.

When you open slice 4 (next on the queue), the work is bounded: carbon-copy `ModuleFilter` from V1 per the V2 self-containment + carbon-copy editorial inheritance discipline. File-header citation + ADMIRE row. The consumer is slice 7's `full-export` config surface; the pillar 9 classification is `OperatorIntent of Selection`.

When you open slices 6 + 7, the discipline is: every operator-supplied config knob gets a pillar 9 classification at slice land time. The classification surfaces in the logging events per the slice-1 contract. Slice 6's actionability work lifts the §12 suggestedConfig discipline onto the JSON artifacts (filter/cluster/cap on noisy axes); slice 7's `full-export` Docker-gated integration test exercises end-to-end conformance.

Hold the spine. Chapter B.4 closes Phase B's *structural* surface. The functional-equivalence arm waits on the live OSSYS path; the structural commitments earn that follow-up its confidence.

— The chapter B.4 architect (opening).
