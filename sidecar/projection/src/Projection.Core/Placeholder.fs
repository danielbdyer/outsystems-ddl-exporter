namespace Projection.Core

/// Placeholder — replaced in subsequent commits as the IR, lineage monad,
/// and passes are introduced. Existence of this module guarantees the
/// project compiles to a non-empty assembly, which the test project can
/// reference.
module internal Placeholder =
    let internal anchor : unit = ()
