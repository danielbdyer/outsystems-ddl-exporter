module Projection.Tests.ScaffoldingTests

open Xunit

/// A trivial test that proves the test runner is wired up. Replaced by
/// real tests in subsequent commits.
[<Fact>]
let ``scaffolding: solution builds and tests run`` () =
    Assert.True(true)
