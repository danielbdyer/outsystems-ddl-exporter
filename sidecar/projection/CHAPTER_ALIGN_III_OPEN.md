# CHAPTER OPEN — align-III: State, Law, Grain & Lexicon (2026-08-16)

> The third and final chapter of THE ALIGNMENT PROGRAM
> (`AUDIT_2026_08_16_GEOMETRIC_ALIGNMENT.md`; the master plan's Arcs S + L + H + X;
> the standing operator authorization runs all three chapters consecutively).
> Chapters I and II are closed (`CHAPTER_ALIGN_I_CLOSE.md`,
> `CHAPTER_ALIGN_II_CLOSE.md`). This chapter's charges: the STATE plane's ledgers
> stop tolerating tautologies and fabricated instants (Arc S); the LAW registry
> becomes as honest as the code it governs (Arc L); three grain hierarchies gain
> their missing carriers (Arc H); and the lexicon's known misnomers are renamed,
> strictly last (Arc X).

## 1. The four charges

- **Arc S (III.1–III.7) — state honesty.** `SyncOrdinal` VO + typed instants
  (the `MinValue` lie retires the II-era fail-closed-decode way); `ChainAdmission`
  declared on `LedgerSpec` (the SinkJournal fingerprint tautology becomes
  unwritable; `PrevSyncId` linkage verified) — **the II.1 deferred ruling HISTORY's
  `BasisAnchor.SinkEdition` widen fires here**; the R6 gate's `CanaryVerdict`
  retires the `"green"` string pair (micro-ruling executed at III.3: RunHistory
  subsumes RunLedger, so X5 is residue-only); `EstateHistory.replay` = FTC at the
  reading grain; the `Lifecycle`/`CatalogSnapshot` dead twin DELETES;
  `DataObservation` (measured-zero ≠ unmeasured); the rename-isometry predicate
  (A43's static half).
- **Arc L (III.8–III.12) — law honesty.** A44 resident in AXIOMS.md; A45/T17/A47/A48
  Skip-prose → live facts (the citationOf grammar; live count 89 → 93); the matrix
  generator's bucket counts become multiline-aware and comment-stripping with ONE
  re-baseline, corrections named; `TriggerProbes` (machine-evaluable deferrals);
  L3-Eject as a named Bucket-C Skip-with-trigger.
- **Arc H (III.13–III.16) — grain carriers.** `PhysicalTableRef` with
  `SchemaBasis(Observed|Assumed)` — test-FIRST (no in-repo multi-schema specimen
  exists); `Environment.parse` total + canonicalizing (III.14 AFTER III.5 —
  Lifecycle.fs); the sink env label → digest SET; `TemporalConfig.HistoryTable`
  typed.
- **Arc X (III.17–III.22, STRICTLY LAST) — the lexicon.** One rename per commit,
  blast radius counted: `EvidenceCache` → `ForecastEvidence`; the estate doc-anchor
  re-alignment; the sink tri-modal measured cut (the "Witness-" freeze has been
  active since Chapter II); Pipeline `Environment` → `Place` (after III.14);
  RunLedger residue per III.3's subsumption; X6 (statement-algebra rehome)
  OPTIONAL, deferrable by named trigger.

## 2. The wave map

Spine: III.0 → S-track III.1→III.7 → L-track III.8→III.12 (AxiomTests serialization
is automatic on one branch) → H-track III.13→III.16 (III.14 after III.5) → X-arc
III.17→III.22 → III.23 the PROGRAM close. **Named seam after III.12**: if the
chapter drags, split IIIa (S+L) / IIIb (H+X) with a chapter-mid-audit standing in
for the extra close.

## 3. Standing rulings inherited

The gate ladder per commit (delta-lint 82=82 substance-normalized; the chain lives
in the session scratchpad); `--no-verify` names the pre-existing red lint gate every
commit; the known-red Docker set is exactly {StagedMergeDeployE2ETests,
T18CycleBreakCanaryTests}; behavioral slices carry DECISIONS entries FIRST; goldens
re-record only with the deviance named (III.6 episode goldens are the one expected
re-record); new DU variants APPEND where order is serialization-load-bearing;
exemplary surfaces stay untouched except where a workpaper names the seam; Voice
codes land complete (copy + all + the three totality lists) in their slice
(III.2/III.3/III.13/III.15 each carry one).

## 4. Acceptance laws (the close's witness map draws from these)

1. A tautological ledger fingerprint is UNWRITABLE (the `ChainAdmission` type
   forces a real comparator or an explicit `Recompute`/`Linkage` declaration).
2. Every stored instant parses fail-closed (no `MinValue` fabrication survives).
3. `loadLatest = replay(loadAll)` — FTC at the estate reading grain.
4. The live axiom count reaches 93 with zero phantoms; the matrix re-baseline's
   corrections are named in one commit.
5. A multi-schema estate round-trips `PhysicalTableRef` without a fabricated `dbo`
   (the test-first specimen is the witness).
6. Every X-arc rename lands alone, blast-radius counted, with the old name absent
   from src/ after its commit (grep-verified in the commit).
7. III.23 closes the PROGRAM: the audit gains its DISPOSITIONED banner (findings →
   slices/deferrals map), BACKLOG + V2_DRIVER rows, the final HANDOFF letter,
   CLAUDE.md currency, and the last PR refresh.
