# Trigger-Gated Hold-Out Inventory — V2 Projection Sidecar (2026-06-16)

> **What this is.** A read-only inventory, produced by a focused agent study, of the named, evidence-gated piece that
> stands between each engine feature and "completing its cycle" — the exact pattern of *the Atomic `BEGIN TRAN` is to
> `migrate`*. The engine's discipline is that **naming a trigger-gated absence IS its completion**; this is the map of
> what remains, with each item's re-open trigger and where it is recorded. It recommends building nothing.
>
> Frame: THE VECTOR is complete (Waves 0–5); the migrate envelope gained M21 (schema compensating-undo), M22 (the
> opt-in `--atomic` envelope, LOCAL full-access), and M23 (the data-leg compensating-undo / `--auto-revert`). The
> remaining frontier is the named moat plus a handful of in-code consumer-gated primitives.

## Ranked table (most load-bearing to the operator's day first)

| # | Feature / capability | Works today | Named hold-out (the "giant-TRAN" analog) | Trigger + where recorded | What firing takes | Proximity |
|---|---|---|---|---|---|---|
| 1 | **`migrate` transactionality** | schema migrate + M21 compensating-undo; mid-write is a *named* refusal (`GateLabel.MidWriteNotProtected`); M22 adds opt-in `--atomic` (LOCAL); M23 adds the data-leg `--auto-revert` | the estate-scale giant transaction (production) | "managed-login grant survey **+** P7b throughput" — `Preflight.fs` A3 scaffold, `THE_VECTOR.md` moat, `KICKOFF §10` row 2, matrix footer (a) | capability conjunct **validated** (`ATOMIC_ENVELOPE_VALIDATION.md` §4); P7b throughput number + the cost probes (§2.1/2.4/2.5/2.6) remain | **Closer** — capability fired; production schema is Octopus/SSDT (not direct-connect) so the estate giant-TRAN is largely moot there |
| 2 | **Reverse-leg transfer / P7b throughput** | two-phase streaming + materialized engine, `CaptureJournal` resume, packed remap, MERGE-OUTPUT capture, reconcile∘streaming, archetype fork — Docker/operator-scale witnessed | the real-wire swept proof at estate scale (≥20k rows/sec over ~288M rows) | **P7b** open after J5 — `j5-cloud-uat-ledger.md`, `reverse-leg-execution-readiness.md`, DECISIONS 2026-06-15 (Phase 5) | a live two-DB wire + the managed estate; the harness + gates are built (governance-gated, not code) | **Close, infra-gated** — loopback figures ~27k rows/sec / 3.0h; only the real wire is missing |
| 3 | **Permissions axis** | A2 pre-flight *gates* on grants; capability *disposition* landed (`Archetype`/`CapabilityProfile.of`) | permissions as a *projected content* axis (`Grant` IR facet, `GRANT` in `Statement`, a `CatalogDiff` channel, readback) | "a flow must **publish** grants (**the eject**)" — matrix footer (b), `THE_VECTOR.md`, KICKOFF §10 row 7 | the eject milestone + new IR/Statement/diff facets | **Structurally distant** — correctly NOT a `ToleratedDivergence` (it is not a round-trip axis) |
| 4 | **Atomic transfer wrapper (data leg)** | `Resumable` idempotent-upsert (default); M23 compensating-undo (`--auto-revert`); `Unprotected` paths classify exit-9 | the `WriteProtection.Atomic` giant *data*-transaction wrapper in `TransferRun.writePlan` | OPEN-2 / P6 / P11 — `Preflight.fs` A3 scaffold, BACKLOG.md | live container witness + the grant survey | **Far** — J5 disfavors it for managed sinks; M23 is the evidence-backed arm |
| 5 | **DACPAC / 2nd `Ingest` source** | one source (OSSYS); `DacpacEmitter` *writes* dacpacs | a second catalog source → lifts an `ICatalogReader` interface | "a second *catalog* source materializes" — DECISIONS Active deferrals, KICKOFF §10 row 4 | a genuine second reader (pure build, needs the use case) | **Far** — `SnapshotRowsets` ruled a variant, not a source |
| 6 | **`SchemaMove` unification** | the four move enumerations all work; the zero-risk `Changed → Reshaped` rename landed (Wave 3) | the full unification into one closed `SchemaMove` DU | "a **real divergence** between the four enumerations fires" — `THE_VECTOR.md`, CONSTELLATION §11, KICKOFF §10 row 1 | a divergence event (they agree today), then a high-blast-radius pure refactor | **Structurally distant** — the canonical "restraint" hold-out |
| 7 | **CDC / change-norm (T15)** | CDC count = the data norm; CDC-silence on idempotent redeploy; `ChangeManifest.toJson` (M18) is machine-queryable | the `fromJson` inverse (read a recorded change-report back) + the CDC estate survey | "a verb that reads a recorded change-report materializes" — DECISIONS 2026-06-16 (M18); CDC survey via J5 | a consuming verb (pure build) / the CDC estate survey | **Far** — no read-back consumer; toJson is the witnessed half |
| 8 | **Tightening passes (`M10`)** | four tightening passes work | the `TighteningStrategy` descriptor (4 files → 4 values) | "a **fifth** tightening intervention, or a divergence" — `THE_VECTOR.md`, KICKOFF §10 row 3, CONSTELLATION §9.7 | a 5th intervention or a divergence + a pure refactor | **Far** — the four agree |
| 9 | **Lifecycle / episode store** | append-only episode NDJSON (CDC counts); `LifecycleStore`; NM-34 evolution discipline | policy-**version plane → Episode** (record policy digests) | "cross-run digest-comparison need **+ M5** (digest stability)" — `THE_VECTOR.md`, KICKOFF §10 row 5 | **M5 landed Wave 4** — only the consumer-need remains | **Half-fired** (see below) |
| 10 | **Synthetic-data σ (Faker emitter)** | Profile→Data; π∘σ≈id; all four gating evidence types shipped | promotion of the Faker emitter past its structurally-met trigger | "third evidence type lands OR a use case forces it" — DECISIONS Active deferrals, Faker row | an operator promotion conversation (no build gate) | **FIRED — awaiting promotion** (see below) |
| 11 | **`Tolerance` per-env config** | `Tolerance.parse` + `ToleratedDivergence` registry + per-run residual | `Tolerance` *consumed* by the canary/Config (today parsed-then-discarded) | "`Tolerance` is currently UNCONSUMED in src/" — BACKLOG.md:57 | a real per-env divergence consumer (pure build) | **Close, consumer-gated** |
| 12 | **Display / Voice / instrument** | NDJSON `LogSink` (channel-1); `code ⇔ copy` tested; M21/M23 added loud rollback arms | `Spectre.Console` TtyRenderer + `--pretty` (channel-2) + live progress bar | "operator reports NDJSON-only stderr as unfriendly" — DECISIONS Active deferrals | a CLI surface layer (no Core change) | **Far, UX-gated** |
| 13 | **Config control plane (CLI verbs)** | `full-export` is the only operator-touchable subcommand; A44 expressible⇔reachable | standalone `extract`/`profile`/`data-twin` verbs; multi-env config; CSV `UserMapLoader` | each: "a concrete operator-paced workflow surfaces" — DECISIONS Active deferrals (5 rows) | CLI surface only (substrate shipped) | **Far** — no demand independent of `full-export` |
| 14 | **Schema round-trip — computed columns (S7)** | 5/6 "hollow canary" features verified vs real SQL (DEFAULT/triggers/sequences/checks/ext-props) | the computed-column **real-SQL leg** (in-process only today) | "computed (S7) ◑ in-process-only … the lone remaining vertical" — BACKLOG.md:52 | a Docker/real-SQL witness | **Close, infra-gated** — the lone open schema vertical |
| 15 | **The eject** | the engine accumulates exact replayable provenance toward a terminal eject | the eject itself (no upstream to re-derive from) — gates #3 | the terminus — `THE_VECTOR.md`, matrix footer (b) | the full cutover program + R6 ladder | **Structurally distant** — the horizon event |

**Law-level hold-outs (AxiomTests Skip-with-trigger, Bucket C/D):** 6 Bucket-C + 1 Bucket-D deferred axioms (matrix T-II: 79 live / 6 C / 1 D / 113 total) — e.g. `H-042` Catalog set-algebra (multi-source merging), `H-063` free-monad scheduling (`LineageTree` prereq), **`H-096` mutation testing — BLOCKED on tooling (Stryker.NET lacks F# support)**, `H-016` PolicyExpr DSL. Categorically distinct from feature hold-outs: derived/out-of-Core/pure-categorical theorems kept as `Skip` because re-derivation would be ceremony.

**Fired-but-gate-held F# promotions (CONSTELLATION §9.7):** both have *landed* — `[<Struct>]` → `RowQuantum` (2026-06-12) and `[<Measure>]` → `Run.fs` ms (2026-06-12, R1d). The §9.7 ledger still phrases units-of-measure as "fires" (present tense) — a doc-currency lag, not an open hold-out.

## The five most consequential — detail

**1 · The Atomic envelope (the exemplar).** `Preflight.fs` A3 SCAFFOLD states the buildable-now default is a *resumable idempotent-upsert*; the full-transaction alternative is "deliberately NOT half-implemented here because its granularity is the survey's to resolve and its correctness needs a live container to witness." The trigger is a conjunction (managed-login grant survey **and** P7b throughput). The J5 run (2026-06-15) supplied evidence pointing *away* from a giant TRAN toward the compensating channel — which M21/M23 built. As of 2026-06-16 the capability conjunct is validated on a full-access SQL Server and the opt-in `--atomic` envelope (M22) is built for the LOCAL case; the estate-scale giant transaction stays gated on P7b and is largely moot for production (schema ships via Octopus/SSDT, not direct-connect).

**2 · Reverse-leg P7b.** `reverse-leg-execution-readiness.md`: the engine is far more built than the docs' "J5 unrun" framing implies — two-phase streaming, `CaptureJournal` resume, packed remap, reconcile∘streaming, archetype fork, `KeymapSpill` (armed but inert). The single gate is the real wire: throughput is loopback-only. DECISIONS 2026-06-15 (Phase 5) confirms the harness + estate-survey package shipped and the cutover gates are *governance*-gated (R6), not code-blocked. The closest-to-firing major hold-out — needs infra, not engineering.

**3 · Permissions as a projected axis.** Matrix footer (b): the A2 pre-flight gates on grants "but grants/roles/RLS are NOT a projected axis (no `Grant` IR facet, no `GRANT` in the `Statement` DU, no permission channel in `CatalogDiff`, no readback)." Wave 5's most disciplined move was *rejecting* a `PermissionsGatedNotProjected` tolerance as a category error (permissions is not a round-trip axis). The trigger is the eject. The *capability* disposition (can-this-sink-write) already landed via the archetype — do not conflate it with the *content* axis (publishing grants).

**6 · `SchemaMove` unification.** `THE_VECTOR.md`: the four enumerations "agree today, so unifying them is speculative abstraction with the highest blast radius in the codebase." The zero-risk `Changed → Reshaped` rename landed Wave 3. The trigger is a real divergence between the four; until then it stays cut. The canonical "restraint" hold-out.

**9 · Policy-version plane → Episode (half-fired).** Trigger is a conjunction: "a cross-run digest-comparison need **+ M5** (digest stability)." **M5 landed Wave 4** (`VersionedPolicy.digestOf` → the injective `canonicalToken`). So one conjunct is now satisfied; the remaining conjunct (a consumer that needs cross-run digest comparison) is unfired, and no digest is persisted into episodes yet.

## Possibly-silently-fired triggers (operator should re-examine)

1. **Faker / synthetic-data emitter — FIRED, un-cashed.** The deferral row reads **"TRIGGER STRUCTURALLY MET — chapter B.3 close (2026-05-19)"** and instructs the next agent to "bring this row to the principal-PO as the chapter-open conversation." The highest-confidence outstanding fired trigger — sitting fired for ~4 weeks. **Re-examine first.**
2. **Policy-version plane → Episode — HALF-FIRED.** The `+M5` conjunct landed Wave 4 (2026-06-16); the trigger went from two unmet conditions to one without an index update.
3. **CDC / OPEN-3 estate survey — now RUNNABLE.** The J5 run lifted the blocker (no managed-env access); the readiness doc still lists it as "open," but it is now answerable, not blocked. Gates the NM-73 CDC auto-fallback in `Policy.fs`.
4. **`[<Measure>]` units-of-measure promotion — doc says "fires," code says "landed"** (CONSTELLATION §9.7). A doc-currency lag, not an open hold-out.
5. **Atomic `BEGIN TRAN` trigger — partially resolved, correctly *not* over-cashed.** The managed-login conjunct was answered by J5 (and 2026-06-16's probe); it redirected the build to the compensating channel (M21/M23) + the LOCAL `--atomic` (M22) rather than the estate giant-TRAN. The P7b conjunct remains. Listed here to confirm it was handled, not missed.

**Net:** the engine's "name-the-absence-is-completion" discipline is holding — of ~15 feature-level hold-outs, only **#10 (Faker)** is an unambiguously fired-but-uncashed trigger, **#9 (Policy→Episode)** is freshly half-fired, and **#3/CDC survey** is newly *runnable*. Everything else is honestly far, infra-gated (P7b, computed-column S7), or consumer-gated (`Tolerance` config). The moat is intact and every entry traces to a live trigger.
