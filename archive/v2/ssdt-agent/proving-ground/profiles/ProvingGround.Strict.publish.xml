<?xml version="1.0" encoding="utf-8"?>
<!--
  ProvingGround.Strict.publish.xml — the strict profile: it reproduces what the data blocks.

  This profile mirrors the refusals a production publish would raise. A change whose .sql text
  looks harmless can still be blocked by the data underneath it. A Strict publish surfaces that
  block, and the blocked publish — with its row counts — is the evidence: it shows the change
  affects existing data and needs more review than the schema text alone implied, and it is
  what the pull request's Deployment evidence reports.

  Aimed only at the ProvingGround database on the warm container — a disposable copy of Dev.
  DropObjectsNotInSource is True here precisely because that copy is disposable and its drops
  should be visible; never point this profile at anything real.

    BlockOnPossibleDataLoss = True   -> blocks the deployment. NOT NULL on NULL data, a
                                        narrowing that truncates, and drops of populated objects
                                        are all refused.
    GenerateSmartDefaults   = False  -> does not silently backfill. The refusal is preserved,
                                        not repaired into a fix.
    DropObjectsNotInSource  = True   -> drop-by-absence is visible; the ProvingGround database
                                        is a disposable copy of Dev.
    IgnoreColumnOrder       = True   -> column-order churn does not masquerade as a real change.

  CONNECTOR POINT (CONNECTORS.md §6): the TargetConnectionString points at the warm-sql.sh
  container. Reuse that existing substrate; never introduce a new orchestration script.
-->
<Project ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <TargetConnectionString>Server=localhost,11433;Initial Catalog=ProvingGround;User ID=sa;Password=Projection@Strong1;TrustServerCertificate=True;Encrypt=False</TargetConnectionString>
    <BlockOnPossibleDataLoss>True</BlockOnPossibleDataLoss>
    <GenerateSmartDefaults>False</GenerateSmartDefaults>
    <IgnoreColumnOrder>True</IgnoreColumnOrder>
    <DropObjectsNotInSource>True</DropObjectsNotInSource>
    <IncludeTransactionalScripts>True</IncludeTransactionalScripts>
    <AllowIncompatiblePlatform>False</AllowIncompatiblePlatform>
    <IgnorePermissions>True</IgnorePermissions>
  </PropertyGroup>
</Project>
