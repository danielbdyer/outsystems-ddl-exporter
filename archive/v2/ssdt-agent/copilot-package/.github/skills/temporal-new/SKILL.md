---
name: temporal-new
description: Use when the developer says "I want full history on this new entity", "keep every version of every row (new table)", "system-versioned table from the start", "show me what this record looked like last Tuesday — for a brand-new entity" — temporal versioning on a NEW entity. SSDT destination = a declarative system-versioned CREATE (SYSTEM_VERSIONING = ON + history table + period columns).
---

<!-- Generated by ssdt-agent/scripts/ssdt-agent-package.mjs copilot-apply (the tree carries its own generator; in the source monorepo the tree sits at sidecar/projection/ssdt-agent/). Do not edit by hand; edit the canonical tree or the generator and re-run. -->

The full skill lives in the vendored tree, where its cross-references resolve.
**Read `ssdt-agent/skills/op/temporal-new/SKILL.md` (from the repository root) and follow it exactly.**
This file is only the dispatch pointer Copilot matches on; the skill body is in the tree.
