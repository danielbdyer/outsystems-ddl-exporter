---
name: drop-check
description: Use when the developer says "remove the rule that Total must be positive", "stop enforcing that status list", "drop the check so this load can go through" — removing a CHECK constraint. Always publishes clean (a drop validates nothing), but the rule stops being enforced and re-adding it later re-validates every row.
---

<!-- Generated by ssdt-agent/scripts/ssdt-agent-package.mjs copilot-apply (the tree carries its own generator; in the source monorepo the tree sits at sidecar/projection/ssdt-agent/). Do not edit by hand; edit the canonical tree or the generator and re-run. -->

The full skill lives in the vendored tree, where its cross-references resolve.
**Read `ssdt-agent/skills/op/drop-check/SKILL.md` (from the repository root) and follow it exactly.**
This file is only the dispatch pointer Copilot matches on; the skill body is in the tree.
