# PAY_ONCE_AUDIT — the single-receipt discipline, named and hunted

> Opened 2026-07-02 (operator directive: name the consolidation shape
> precisely and search for it intentionally). This document carries the
> named law, the audit method, and the adjudicated receipt ledger. The
> findings sections are filled from the audit workflow's verified output;
> the frame stands on its own.

## 1 — The law

**Every fact enters the system once, at its cheapest sufficient
representation; everything else derives from that single payment by pure,
threaded functions.**

This is the perf-dual of the engine's soul. The adjunction
(`Ingest ∘ Project = identity`, modulo named erasures) is a conservation
law downward: *nothing is lost in silence*. The pay-once law is the same
conservation upward: *nothing is paid twice in silence*. The change ledger
counts every displacement once; the cost ledger must count every fact
once. A second payment is an unbalanced book.

## 2 — The four planes (+ the tower)

A second payment hides on exactly one of these planes:

| Plane | The duplicated cost | Session exemplars |
|---|---|---|
| **Acquisition** | the wire/server paid twice for one substrate | P1 (profiler re-read what the data lanes held); batched nullability reflection |
| **Representation** | one piece of information re-encoded hop after hop | P3 (cell → Map → typed Map → AST literal); typed values round-tripped through `"i:"+ToString` keys |
| **Derivation** | a pure `f(x)` recomputed while `x` never changed | topo re-runs; `emittedNames` ×3 per kind; per-item linear scans |
| **Materialization** | something built eagerly that no consumer demands at that strength | the ~100MB `Fused` string guarding one boolean gate |
| **Tower** (structural) | per-element wrappers each re-wrapping every element to carry trivial state | two probe layers = 2 Tasks/row carrying a counter |

## 3 — Why the payments were invisible: the naming theorem

**A second payment is invisible until the shared value has a name.**
`Fused` hid because the interleave had no on-demand surface; the topo
re-runs hid because nothing threaded `ComposeState.TopologicalOrder`; the
carrier tax hid because `StaticRow` was "the" row and the quantum had no
consumers. Conversely, every fix in the family was *naming plus
threading*: `TargetKeySet`, `readsideIdentity`, `loadForWith`,
`renderQuanta`, `*Using` forms, `SamplingPolicy`.

So the audit predicate is not "find slow code." It is: **find the unnamed
intermediate value with two purchase receipts.** Corollaries the house
already operates: discover-once/derive-pure (EvidenceCache), carriers
reify eagerly, `Catalog.kindIndex`'s `ConditionalWeakTable` (the ONE
sanctioned cache shape: keyed by the immutable value itself, so staleness
is impossible by construction — caching is otherwise refused in favor of
threading).

## 4 — The audit method (repeatable)

1. **Sweep** — plane-specific finders over disjoint areas, information
   only: each finding must cite BOTH receipts (`file:line` where the fact
   is first paid; where it is paid again), the payment grain
   (per-cell/row/kind/publish/verb), the path class, and the NAME the
   shared value would take. Exclusion list = everything already executed
   or declined-with-reasons (PERF_OPPORTUNITIES).
2. **Adversarial verify** — two independent skeptics per finding, strict
   AND-survival, each defaulting to *refuted* under uncertainty:
   - the **value-identity skeptic**: are the receipts really for the same
     fact over the same immutable input? (This lens is what caught
     `Rendered` being a non-copy before a refactor was wasted on it.)
   - the **consumer skeptic**: does some consumer genuinely need the
     second form — a law surface (A35 carriers), a verification purpose
     (canary re-reads), a staleness boundary?
3. **Synthesize** — a completeness critic names what each plane failed to
   surface; adjudication and ranking happen here, by hand, grain × path ×
   risk, each survivor carrying its named carrier, identity gate, and
   measurement leg.

## 5 — Verified receipt ledger (adjudicated)

The audit ran 2026-07-02: 8 plane×area finders (119 raw findings), batched
dual-lens adversarial verification (strict refute-by-default), one coverage
critic. **62 findings survived, 38 were killed, 1 was unadjudicated**
(hand-ruled IN — its receipt is textually verifiable) — a 39% kill rate,
which is the audit working as designed: the skeptics are the reason the
survivors can be trusted.

The survivors cluster into **11 plan items** (full executable detail in
`PAY_ONCE_PLAN.md`; raw per-finding receipts in the appendix below):

| Rank | Item | Findings | The second payment, in one line |
|---|---|---|---|
| 1 | **PL-1** one estate acquisition per combined verb | S00 S45 S50 S05 S51 S53 S52 S13 | publish-then-load/store re-runs the WHOLE extract + chain + render it just finished — a second publish per combined verb |
| 2 | **PL-2** AllData streams static kinds once | S04 | the static-graft arm streams rows for a lane AllData dispatches empty; bootstrap streams them again |
| 3 | **PL-4** SSDT lookups derive once | S46 S47 S48 S37 S49 S28 S54 S56 | `buildLookups` ×3–4, `fkDef` ×4/reference, `DecisionOverlay` ×3, keyset Set per construction |
| 4 | **PL-3** render-constant hoisting | S18 S19 S38 S20 S59 S23 S27 S57 S61 +1 | per-kind vocabulary/counts/TableId/rows recomputed 2–4× inside one render; per-row recompute of per-kind constants |
| 5 | **PL-5** profile evidence indexes | S35 S36 S39 S40 S41 S42 | `Profile.tryFind*` linear scans × every attribute × every tightening pass; graph constants per PageRank iteration |
| 6 | **PL-6** the text plane pays per byte once | S25 S26 S33 S24 S14 S31 S30 S29 S32 | whole-artifact split/rejoin copies; re-PARSING our own rendered text to rediscover GO boundaries we inserted; UTF-16↔UTF-8 round-trips |
| 7 | **PL-7** schema-only reads | S01 S09 S02 S10 | verbs lift every table's rows to consume only schema; profile-capture lifts, STRIPS, then re-streams |
| 8 | **PL-8** single-scan live discovery | S03 S06 | every unsampled table full-scanned twice (aggregate + stream); COUNT-probe before a full stream |
| 9 | **PL-9** streaming transfer stages per-kind once | S15 S16 S17 S22 S11 | per-chunk re-render of per-kind SQL; full-width Phase-2 re-stream for a PK+FK update |
| 10 | **PL-10** small verb-state threadings | S12 S08 S43 S44 S58 S34 S21 S60 | lifecycle double-load, ingest-then-filter, per-reference Set rebuilds, O(N²) fold, per-cell seed re-serialize |
| 11 | **PL-11** delete the dead readers | S07 | reflection SQL maintained twice, zero callers — the dead-algebra precedent |

Two adjudication notes of record: **S52 is subsumed by PL-1** (the discarded
`Outputs` build disappears with the redundant projection itself — do not fix
it separately); and PL-3's per-row arm rides along on cleanliness grounds —
the K13 kill correctly bounded its wall-clock at the ≤1000-row staging
threshold, so its value is shape, not seconds.

## 6 — Killed findings (the skeptics' standing law)

All 38 kills with their refutation cores are in the appendix. The ones that
now function as STANDING LAW for any future pay-once proposal — refute the
refutation before re-proposing:

- **K11** — the raw-string cell quantum is the codec's LAW (SQL → IR-raw →
  SQL with `RawValueCodec` as single source of truth), not a gratuitous hop.
- **K34** — the per-call `Sql160ScriptGenerator` is a recorded correctness
  decision (the generator holds real per-render state); only the options are
  shared.
- **K23** — NM-02: every emit step runs on artifact-producing runs so
  `registered ⇔ executed` stays property-testable. Weak-consumer arguments
  do not apply to that fold.
- **K17** — `pinNewlines`' two Replaces are two DIFFERENT transforms, not two
  receipts.
- **K01** — the extended-properties triple scan is a disjoint predicate
  partition; no row is read twice.
- **K03/K08** — CDC count re-reads are deliberate before/after measures (the
  data norm), not duplicates.
- **K26** — `emittedResidualCollector` runs over DIFFERENT catalog values
  (hydrated vs emitted); receipts must match on the VALUE, not the function.
- **K06** — per-chunk staging table create/drop is transactional-boundary
  load-bearing on the resumable lane.

## 7 — Coverage gaps the critic named (unverified leads)

Six concrete receipts in areas no finder read — each needs its own verify
pass before planning (Tier 3 in `PAY_ONCE_PLAN.md`): the CLI layer's
**projection.json parsed ×4 per migrate leg**; `SliceFlowRun`'s **in-memory
dataset round-tripped through a temp file**; `LogSink`'s **double envelope
serialization** and **per-envelope stream+copy tower**; `Run.list`
**fully parsing every run.json (events + artifact blobs) to render an
index**; `SummaryFormatter`'s **build-then-split-then-rejoin**.

## 8 — Recommendation

Execute in the plan's order (PL-1 → PL-2 → PL-4 → PL-3 → PL-5 → PL-6 →
PL-8 → PL-7 → PL-9/10/11; Tier 3 after its own verification). PL-1 alone is
worth more than everything else combined — a combined publish+load/store verb
currently pays for two publishes. Every item carries its identity gate in the
plan; none should move a golden. The audit method itself (§4) is the
repeatable artifact: re-run it after major chapters — the receipts drift as
the code grows, the law does not.

---

## Appendix A — the 62 surviving receipts (verbatim from the verified ledger)

### S00 · Publish-with-load runs the entire model read + static/bootstrap row hydration twice (readAndHydrateConfigModel paid once by runWithConfig, again by emittedSeedPlan)
- **plane/grain/path:** acquisition / per-publish / publish-critical
- **fact:** The OSSYS metadata snapshot (the 23-result-set rowsets SQL) plus every static-marked and bootstrap-eligible kind's full row stream for one config, in one verb invocation of runWithConfigAndLoad (Export.fs:160): runWithConfig's extract stage acquires it, then loadFromConfig -> emittedSeedPlan re-acquires the identical substrate from the same live source to re-project the seed plan.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1827 — extract stage: `let! parsed = readAndHydrateConfigModel cfg` (OSSYS snapshot + hydrateCatalogUsing + hydrateBootstrapRowsExcludingUsing).
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1944 — emittedSeedPlan: `let! parsed = readAndHydrateConfigModel cfg` again, invoked at Pipeline.fs:2334 (loadFromConfig) after runWithConfig at Pipeline.fs:2357 inside runWithConfigAndLoad.
- **evidence:** First: `let! parsed = readAndHydrateConfigModel cfg` (Pipeline.fs:1827). Second: docstring admits parity-by-repetition — "WP6 step 4 parity — hydrate the store-leg catalog the SAME way the publish path does (`readAndHydrateConfigModel`)" then `let! parsed = readAndHydrateConfigModel cfg` (Pipeline.fs:1941-1944); call chain `let! reportResult = runWithConfig cfg` (2357) then `let! seedR = emittedSeedPlan cfg` (2334). Both hit the same cfg.Model.Ossys source; the second pass re-runs MetadataSnapshotRunner and re-streams every hydrated kind's rows.
- **suggested carrier:** projectSeedPlanUsing (catalog, bootstrapRows, migration) — thread the extract stage's already-hydrated triple from runWithConfig into the load leg instead of re-reading

### S01 · Transfer apparatus reads every source table's rows twice: ReadSide.read materializes them into the contract catalog, then Ingestion.collectInOrder streams them again
- **plane/grain/path:** acquisition / per-kind / verb-only
- **fact:** Each source table's full row set (plus a COUNT(*) probe) in one transfer run: runThroughConnectionsResumable reconstructs the schema contract via ReadSide.read, which lifts every <=100k-row table's rows into Modality.Static (readRows: COUNT(*) + full SELECT per table), and the contract's consumers (resolveLoadSet, reconciliation, DataLoadPlan) use only schema — then runCore streams every kind's rows a second time from the same connection via Ingestion.collectInOrder.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1972 — `match! ReadSide.read source with` (rows materialized per table via ReadSide.fs:1937-1946 `readRows cnn k maxRows` with maxRows = 100_000).
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:758 — `let! rawRows = Ingestion.collectInOrder source ingestCatalog topo` re-streams every kind's rows for the load plan.
- **evidence:** First: `match! ReadSide.read source with \| ... \| Ok contract ->` (TransferRun.fs:1972-1974); ReadSide.read's per-kind loop: `let! rowsOpt = readRows cnn k maxRows` then `{ k with Modality = [ Static rows ] }` (ReadSide.fs:1940-1944). Second: `let! rawRows = Ingestion.collectInOrder source ingestCatalog topo` (TransferRun.fs:758). The Static populations on `contract` are never consumed by runCore — the plan is built from `rows` (TransferRun.fs:782-784).
- **suggested carrier:** ReadSide.readSchema (a rows-off contract read) — the schema-only projection the transfer contract actually consumes

### S02 · profile-capture verb (and Source.ofLive/ofOssys) reads every table's rows via ReadSide.read, discards them via stripStaticPopulations, then LiveProfiler re-streams the same tables
- **plane/grain/path:** acquisition / per-kind / verb-only
- **fact:** Every table's row data per capture: ReadSide.read pulls each <=100k-row table's full rows into Modality.Static (COUNT(*) + full SELECT per table), the very next line strips those populations because LiveProfiler skips Static kinds (the 4.4 trap), and LiveProfiler.attach then re-acquires the same tables from the server (per-kind COUNT_BIG aggregate + full row stream) to build the EvidenceCache — evidence that EvidenceCache.cachedKindOfRows (Core, EvidenceCache.fs:280) can derive purely from rows already in hand.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/ProfileCaptureRun.fs:34 — `match! ReadSide.read cnn with` (rows materialized per table).
- **second receipt:** sidecar/projection/src/Projection.Pipeline/ProfileCaptureRun.fs:36 — `return! LiveProfiler.attach cnn (Catalog.stripStaticPopulations catalog) Profile.empty` (populations discarded; every kind re-scanned live).
- **evidence:** First: `match! ReadSide.read cnn with` (ProfileCaptureRun.fs:34). Second: `LiveProfiler.attach cnn (Catalog.stripStaticPopulations catalog) Profile.empty` (:36). Same pattern at Source.fs:162 (`return! ReadSide.read cnn`) + Source.fs:178-179 (`let profileCatalog = Catalog.stripStaticPopulations catalog` / `return! LiveProfiler.attach cnn profileCatalog Profile.empty`) and in ofOssys at Source.fs:217-218. The pay-once sibling already exists: captureEvidenceCacheDerived consumes hydrated rows via `EvidenceCache.cachedKindOfRows` (LiveProfiler.fs:552-555) but only on the publish path.
- **suggested carrier:** LiveProfiler.attachDerived over the ReadSide-materialized Static populations (EvidenceCache.cachedKindOfRows, live fallback for >100k tables)

### S03 · LiveProfiler.discoverKind scans each unsampled table twice: a COUNT_BIG aggregate full scan, then a full row-stream of the same table
- **plane/grain/path:** acquisition / per-kind / verb-only
- **fact:** One table's row substrate per live discovery: query 1 (`cacheAggregateSql`) is a full server-side scan computing exact RowCount + per-column NullCounts; query 3 (`cacheRowStreamSql`) then streams every row of the same table — for the unsampled (default full-scan) case the aggregate values are a pure function of the streamed rows (EvidenceCache.cachedKindOfRows derives exactly them). Borderline: the executed single-scan item (captureEvidenceCacheDerived) closed this only for kinds the data lanes hydrated; attach/attachConcurrent, Preflight.tighteningViolations, DataIntegrityChecker.compare and the derived path's live fallback still pay both scans per kind.
- **first receipt:** sidecar/projection/src/Projection.Adapters.Sql/LiveProfiler.fs:305 — `cmd.CommandText <- cacheAggregateSql kind` (SQL at :248-249: "SELECT COUNT_BIG(*) AS [c_rows], <per-column null counts> FROM <table>").
- **second receipt:** sidecar/projection/src/Projection.Adapters.Sql/LiveProfiler.fs:333 — `cmd.CommandText <- cacheRowStreamSql maxRows kind` (SQL at :283-284: "SELECT <all columns> FROM <table>") over the same table in the same discovery.
- **evidence:** Aggregate: `"SELECT COUNT_BIG(*) AS [c_rows], " + selectList + " FROM " + table` (LiveProfiler.fs:248-249) executed at :305-307. Stream: `"SELECT " + topClause + columnList + " FROM " + table + orderClause` (:283-284) executed at :333-335 in the same `discoverKind`. Pure derivation exists: `EvidenceCache.cachedKindOfRows` (EvidenceCache.fs:280) — used by the derived path only (LiveProfiler.fs:552).
- **suggested carrier:** EvidenceCache.cachedKindOfQuanta over the one row stream (aggregate query retained only for sampled kinds, where the capped stream cannot yield exact counts)

### S04 · Under bootstrapAllData (AllData composition) every static kind's rows are streamed from the source twice — once by the static-graft arm, again by the bootstrap arm — while the grafted copy feeds a lane dispatched empty
- **plane/grain/path:** acquisition / per-kind / publish-critical
- **fact:** Static-marked kinds' full row sets, twice per AllData publish: hydrateCatalogUsing streams and grafts them whenever data emission is on (it never consults DataComposition), and hydrateBootstrapRowsExcludingUsing's eligibility filter under AllData is `true` for every data-bearing kind including the static ones, so the same tables are drained again into bootstrapRows; the composer then dispatches the StaticSeeds lane EMPTY under AllData, so the grafted copy reaches no data lane (it survives only into the catalog snapshot artifact).
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Hydration.fs:104 — static graft arm: `let! rowsByKind = Ingestion.collectInOrderFor ownedStatic cnn catalog topo` (gated only by emitDataOf, Hydration.fs:168-171).
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Hydration.fs:239-242 — bootstrap eligibility: `match composition with \| AllData -> true \| AllRemaining \| AllExceptStatic -> not (isStaticKind k) && not (Set.contains k.SsKey migrationKinds)` — static kinds re-streamed under AllData.
- **evidence:** Arm 1: `Ingestion.collectInOrderFor ownedStatic cnn catalog topo` (Hydration.fs:104) runs for every data-on OSSYS publish. Arm 2: `\| AllData -> true` in the eligible filter (Hydration.fs:240) includes static kinds. Consumption gap: `\| AllRemaining -> StaticSeedsEmitter.emitWithTopo ... \| AllExceptStatic \| AllData -> emptyArtifact catalog` (DataEmissionComposer.fs:122-124) — under AllData the Static lane never reads the grafted rows.
- **suggested carrier:** compositionOf-gated static graft (skip hydrateCatalogUsing's stream under AllData, or graft from the bootstrapRows map already drained)

### S05 · runWithConfigAndStore runs the full OSSYS rowsets extraction a second time (emittedSchema -> readConfigModel) after runWithConfig already extracted the same model
- **plane/grain/path:** acquisition / per-publish / publish-critical
- **fact:** The OSSYS metadata snapshot (MetadataSnapshotRunner over the 23-result-set rowsets SQL) for one config, twice per store-recording publish: runWithConfig's extract stage reads it, then storeLegFromConfig -> emittedSchema calls readConfigModel again on the same cfg to recompute the emitted-schema plane the run already had in hand (RunReport threads the manifest but not the catalog). Sweep-2 item 5 names only the store leg's separate Kahn/Tarjan as remaining — the second server extraction itself is unrecorded.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2182 — `let! reportResult = runWithConfig cfg` (extract stage at :1827 reads the model live).
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2147 — `let! emittedR = emittedSchema cfg`, whose body is `let! parsed = readConfigModel cfg` (:1883) — a second MetadataSnapshotRunner.runAsync against the same source.
- **evidence:** First: `let! reportResult = runWithConfig cfg` (Pipeline.fs:2182). Second: `let! emittedR = emittedSchema cfg` (:2147) with `emittedSchema` = `let! parsed = readConfigModel cfg; return parsed \|> Result.bind (applyRenames cfg)` (:1881-1885). Production caller: FullExportRun.fs:325 `Compose.runWithConfigAndStore cfgForRun storePath timeline environment at`.
- **suggested carrier:** storeLegFromCatalog (emitted: Catalog) — thread the extract stage's catalog (post-applyRenames) into the store leg instead of a second readConfigModel

### S06 · ReadSide.readRows probes COUNT(*) over a table it is about to stream in full — the same table server-scanned twice per readback kind
- **plane/grain/path:** acquisition / per-kind / verb-only
- **fact:** Each <=100k-row table's row substrate per ReadSide.read: a `SELECT COUNT(*)` full aggregate probe decides the threshold, then readRowsStream re-scans the same table to drain every row; the count's only consumers are the >maxRows gate and the =0 shortcut, both derivable from a capped drain (stop at maxRows+1).
- **first receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1140-1144 — `countCmd.CommandText <- "SELECT COUNT(*) FROM " + qualified; let! countObj = countCmd.ExecuteScalarAsync()`.
- **second receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1150-1151 — `let stream = readRowsStream cnn kind \|> materializeStream kind; let! rows = AsyncStream.toList stream` — the same table streamed end to end.
- **evidence:** Probe: `System.String.Concat("SELECT COUNT(*) FROM ", qualified)` then `ExecuteScalarAsync` (ReadSide.fs:1141-1143). Re-scan: `readRowsStream cnn kind \|> materializeStream kind` drained via `AsyncStream.toList` (:1150-1151), invoked per kind from `read` at :1940. Count used only as `if count > maxRows ... elif count = 0 ...` (:1145-1148).
- **suggested carrier:** readRowsCapped (drain with a maxRows+1 early stop; overflow => None) — one scan carries both the gate and the rows

### S07 · Four standalone ReadSide readers duplicate the combined batch's SQL verbatim and have zero callers — the reflection SQL is maintained twice
- **plane/grain/path:** materialization / per-verb / verb-only
- **fact:** The same reflection artifact built twice in source: readColumnRows, readPrimaryKeys, readIdentityColumns and readForeignKeys each carry a private copy of SQL that readSchemaCombined re-embeds verbatim in its one batched command (the file itself says the column-reader body "was previously duplicated verbatim in readColumnRows and readSchemaCombined"), and grep finds no call site for any of the four (all private; `read` uses only readSchemaCombined) — a dead twin that can silently drift from the live batch. Borderline: never paid at runtime; the second payment is the duplicated artifact and its drift risk (already flagged in AUDIT_2026_06_04 line 913 for readForeignKeys, with no recorded resolution).
- **first receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:230, 278, 310, 562 — the standalone readers with their SQL (e.g. :562 `let private readForeignKeys (cnn: SqlConnection) : Task<FkRow list>` with the sys.foreign_keys join at :567-581).
- **second receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1540-1591 — readSchemaCombined embeds the identical SELECTs inline (FK join at :1557-1571 byte-matches :567-581); comment at :195-196: "The body was previously duplicated verbatim in readColumnRows and readSchemaCombined".
- **evidence:** Standalone FK SQL: `FROM sys.foreign_keys fk JOIN sys.foreign_key_columns fkc ... ORDER BY SCHEMA_NAME(t.schema_id), t.name, c.column_id` (ReadSide.fs:571-581); combined batch repeats it at :1561-1571. Grep across ReadSide.fs shows only definitions, Bench labels and comments for all four names — no invocation; `read` calls `readSchemaCombined cnn` (:1882-1883).
- **suggested carrier:** the combined batch as the single definition site (delete the dead standalone readers, or derive the batch text from their shared SQL constants)

### S08 · runCore with a declared table subset (LoadSet) ingests EVERY kind's rows, then filters the non-listed kinds' rows away
- **plane/grain/path:** acquisition / per-kind / verb-only
- **fact:** Only loadSet ∪ reconciledKinds rows are needed, but Ingestion.collectInOrder (= collectInOrderFor over the FULL topo.Order) materializes every kind's rows from the source; the Map.filter immediately discards the rows of every kind outside the subset — data fetched but never consumed on any '--tables'-restricted transfer.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:758 — 'let! rawRows = Ingestion.collectInOrder source ingestCatalog topo' (Ingestion.fs:82-87: collectInOrderFor (Set.ofList topo.Order) — all kinds)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:776 — 'repointed \|> Map.filter (fun k _ -> Set.contains k loadSet \|\| Set.contains k reconciledKinds)' discards the already-fetched rows of non-listed kinds
- **evidence:** Ingestion.fs:87 'collectInOrderFor (Set.ofList topo.Order) cnn catalog topo' vs TransferRun.fs:774-777 'match writeOpts.LoadSet with \| Some loadSet -> repointed \|> Map.filter …'. Ingestion.fs:50 already exposes the scoped 'collectInOrderFor (owned: Set<SsKey>)' the filter could ride.
- **suggested carrier:** Ingestion.collectInOrderFor (loadSet ∪ reconciledKinds) — the existing scoped collector threaded with the load-set

### S09 · SliceApplyRun reads the target with the row-lifting ReadSide.read though apply/emit consume only the schema
- **plane/grain/path:** acquisition / per-row / verb-only
- **fact:** The target's schema is the only thing consumed (mapToTarget resolves entities/attributes; emit/runGoldenApply build the plan from GOLDEN rows), but ReadSide.read pays a COUNT(*) plus a <=100k-row drain per TARGET table to lift Static rows nothing in slice-apply ever touches.
- **first receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1940 — 'let! rowsOpt = readRows cnn k maxRows' inside ReadSide.read, invoked at SliceApplyRun.fs:113 (applyToFile) and :147 (applyLive)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/SliceApplyRun.fs:150 — 'match mapToTarget catalog golden' (and emit at :116) consume attributes/kinds only; the lifted target rows are never read
- **evidence:** SliceApplyRun.fs:147 'match! ReadSide.read cnn with … Ok catalog -> match mapToTarget catalog golden' — mapToTarget (fs:34-63) touches kind.Attributes only; ReadSide.fs:1944 '{ k with Modality = [ Static rows ] }' is dead weight here.
- **suggested carrier:** ReadSide.readSchemaOnly (target-contract read without the per-table row drain)

### S10 · Tightening preflight builds a FULL EvidenceCache (three queries per table over the whole catalog) for a gate that needs only NULL counts of the tightened columns
- **plane/grain/path:** materialization / per-kind / verb-only
- **fact:** The migrate safety gate needs one fact — live NULL counts for the columns in overlay.EnforceNotNull (typically a handful) — but Preflight.tighteningPreflight captures the complete EvidenceCache over every kind in the catalog (per-table COUNT_BIG+null aggregate, TOP-capped distinct select, and a SELECT * row-stream) and then dataViolatesTightening projects just those few columns' null counts out of it.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Preflight.fs:156 — 'match! LiveProfiler.captureEvidenceCache cnn profileCatalog' (invoked from MigrationRun.fs:548 safetyGates; LiveProfiler walks every kind, LiveProfiler.fs:249/284/327)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Preflight.fs:158 — 'return Ok (dataViolatesTightening cache overlay)' consumes only the overlay columns' null counts (MigrationRun.fs:546 already scopes: 'if Set.isEmpty overlay.EnforceNotNull then return Ok ()')
- **evidence:** MigrationRun.fs:545-548 'let overlay = Preflight.tighteningOverlay source target / if Set.isEmpty overlay.EnforceNotNull then Ok () else match! Preflight.tighteningPreflight cnn source overlay' — the overlay names the exact columns, yet the capture is catalog-wide. Borderline: the executed 'batched nullability reflection' item covers reflection, not this gate's whole-estate evidence walk.
- **suggested carrier:** a scoped null-count probe keyed by the overlay (e.g. LiveProfiler.nullCountsFor overlay.EnforceNotNull), or captureEvidenceCacheWith restricted to the tightened kinds

### S11 · Streaming phase 2 re-streams the deferred kinds' FULL row width from the source when only the PK and deferred FK columns feed the UPDATE
- **plane/grain/path:** acquisition / per-cell / verb-only
- **fact:** The phase-2 re-STREAM of a deferred kind is DELIBERATE (bounded memory — the docstring names it: 'Phase 2 re-STREAMS the deferred kinds against the completed remap'), but the second pull pays the full SELECT column width via Ingestion.streamKind while phase2Chunks consumes only the PK cell (re-keyed through the remap) and the DeferredFkColumns cells for the rendered UPDATE — every other column crosses the wire a second time unconsumed.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1449 — phase 1: 'let stream = Ingestion.streamKind source ingestKind' (full-width pull of the kind's rows)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1524 — phase 2: 'let stream = Ingestion.streamKind source ingestKind' pulls the same kind full-width again; consumption is only idIx + deferred columns (:1489-1501 PK cell re-key; :1523 'phase2UpdateSqlQuantum basis kind load.DeferredFkColumns')
- **evidence:** TransferRun.fs:1246-1248 '/// Phase 2 re-STREAMS the deferred kinds against the completed remap (idempotent UPDATEs…)' — the re-stream is a named memory-bound choice; the unnamed cost is the width: Ingestion.fs:19-20 streamKind = ReadSide.readRowsStream (all columns).
- **suggested carrier:** ReadSide.readRowsProjectedStream (pk :: deferredFkColumns) — a column-projected phase-2 stream

### S12 · recordVerified loads and parses the durable LifecycleStore twice in one record leg (nextCoordinate, then record)
- **plane/grain/path:** acquisition / per-verb / verb-only
- **fact:** The persisted episode chain at 'path': recordVerified first calls nextCoordinate, which does LifecycleStore.load to read the latest ordinal, then calls record, which does LifecycleStore.load AGAIN on the same unchanged file to append — the whole store is read and deserialized twice per recorded migration (three times if the verb also ran previewFromStore, MigrationRun.fs:204-208).
- **first receipt:** sidecar/projection/src/Projection.Pipeline/MigrationRun.fs:276 — nextCoordinate: 'match LifecycleStore.load path with \| … \| Ok existing -> Ok (Version.ordinal … + 1)' (invoked from recordVerified at :323)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/MigrationRun.fs:248 — record: 'match LifecycleStore.load path with \| … \| Ok existing -> EpisodicLifecycle.append episode existing' (invoked from recordVerified at :325)
- **evidence:** MigrationRun.fs:323-325 'match nextCoordinate path environment at with … \| Ok coordinate -> record path timeline coordinate …' — both callees begin with 'if System.IO.File.Exists path then match LifecycleStore.load path' on the same path with no write in between.
- **suggested carrier:** a loaded EpisodicLifecycle chain binding threaded once (e.g. nextCoordinateOfChain chain / recordOnChain chain, with one load + one save at the verb boundary)

### S13 · migrate-with-data computes CatalogDiff.between(sinkSource, target) twice — once in the schema leg's plan, again in the data leg's rename map
- **plane/grain/path:** derivation / per-verb / verb-only
- **fact:** The A→B displacement of the SAME unchanged catalog pair: executeWithDataWith's schema leg runs executeWith → preview → Migration.plan, which computes CatalogDiff.between sinkSource target (Migration.fs:217); the data leg then calls Transfer.runWithRenamesWith / runReconcilingWithRenamesWith, which recompute CatalogDiff.between sourceContract sinkContract on the identical pair to derive the rename map (same double-pay in executeWithDataAndRecordWith at fs:901+915/917).
- **first receipt:** sidecar/projection/src/Projection.Pipeline/MigrationRun.fs:598 — executeWith's emit stage: 'match preview declaration source target' → Migration.plan → 'let diff = CatalogDiff.between source target' (Migration.fs:217)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1154 — runWithRenamesWith: 'let diff = CatalogDiff.between sourceContract sinkContract' recomputed for the same (sinkSource, target) pair when invoked from MigrationRun.fs:842/847
- **evidence:** MigrationRun.fs:836-847 'A5 — the data source is at A (sinkSource); … The renameMap derives from CatalogDiff.between sinkSource target' — the comment names the same diff the schema leg already holds in artifacts.Plan.Diff (returned in MigrationOutcome.Artifacts).
- **suggested carrier:** thread schema.Artifacts.Plan.Diff (or its RenameProjection.renameMap) into the data leg — a *Using form of runWithRenamesWith taking the precomputed CatalogDiff

### S14 · Rendered statements are joined into text and executeBatch re-parses that text with ScriptDom to rediscover batch boundaries the caller just knew — at per-chunk/per-statement cardinality
- **plane/grain/path:** representation / per-verb / verb-only
- **fact:** The batch structure of self-assembled SQL: phase-2 update lists, wipe DELETEs, capture-staging DDL, and migrate rename statements are joined ('String.concat "\n"') or passed one-statement-at-a-time into Deploy.executeBatch, which pays a TSql160Parser parse (~5-10ms per call, per Deploy.fs's own perf citation that assumes 'one per phase' cardinality) to split what the caller assembled from typed statements moments earlier — paid per 50k-row chunk on the streaming phase 2 and per statement in the migrate deploy loop.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:381 — 'do! Deploy.executeBatch sink (String.concat "\n" updates)' (the caller holds the statement list; streaming sibling per chunk at :1505; per-table wipe at TransferResume.fs:50; per-rename at MigrationRun.fs:627)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Deploy.fs:204 — 'let segments = BatchSplitter.splitWithLoudFallback "deploy.executeBatch" sql' re-parses the joined text to recover the segmentation
- **evidence:** Deploy.fs:195-199 perf citation: 'ScriptDom Parse adds ~5-10ms per executeBatch call … At executeBatch-per-deploy cardinality (one per phase)' — an assumption the per-chunk (TransferRun.fs:1505) and per-statement (MigrationRun.fs:626-631) call sites violate. Borderline: segments ≠ statements (GO-splitting), so the parse is only fully redundant when the caller's text carries no GO.
- **suggested carrier:** Deploy.executeSegments : SqlConnection -> string list -> Task<unit> (pre-split segments pass the parser by construction)

### S15 · Surrogate-capture rebuilds and re-renders its per-kind-constant SQL (staging DDL, capture MERGE, keymap SQL) and insert-column list on EVERY 50k-row chunk
- **plane/grain/path:** derivation / per-kind / verb-only
- **fact:** The capture lane's SQL texts and column vocabulary for one kind — buildCaptureStaging / buildCaptureMerge / keymap statements and insertColsOf are pure functions of the unchanged Kind, yet each chunk of an AssignedBySink kind re-runs the ScriptDom build + generateOne render and the attribute filters (thousands of chunks per kind at estate scale).
- **first receipt:** sidecar/projection/src/Projection.Pipeline/SurrogateCapture.fs:251,262,166-169 — captureChunk computes `insertColsOf kind`, `createStaging` (buildCaptureStaging -> generateOne) and mergeOutputChunk regenerates `buildCaptureMerge ... + ";"` for the first chunk
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:243 and 1409-1414 — the chunk recursions (`captureChunks ... rest`, `loadKindChunks ... (chunkIx + 1)`) re-pay the identical builds on every subsequent chunk of the same kind
- **evidence:** SurrogateCapture.fs:166-169 `cmd.CommandText <- ScriptDomGenerate.generateOne (ScriptDomBuild.buildCaptureMerge kind.Physical StagingTempName (insertCols \|> List.map colName) (colName identityAttr) None) + ";"` — per chunk; SurrogateCapture.fs:251 `let insertCols = insertColsOf kind` — per chunk. The temp-table EXECUTION per chunk is required (dropped per chunk); the TEXT generation is not.
- **suggested carrier:** CaptureKindSql (a per-kind record of rendered staging/merge/keymap texts + insertCols, staged once beside the sticky lane in captureChunks/writeChunk)

### S16 · Streaming write loop re-stages its per-kind-constant projections (cell getters, FK ordinals) on every chunk, against the docstring's 'once per kind' claim
- **plane/grain/path:** derivation / per-kind / verb-only
- **fact:** The per-kind staged column getters of quantumCellsOver and the FK ordinal set of repoint (fkColumnsTargeting + Map.filter + fkOrdinalsTargeting, each ordinal an O(width) Array.tryFindIndex) — constant for a kind's stream, rebuilt on every 50k-row chunk because staging happens inside writeChunk, not beside pkOf.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/TransferCellShaping.fs:46-53 — quantumCellsOver stages `cols` (per-attribute `RowQuantum.cellGetter basis a.Name`, each a linear basis scan); docstring 42-44 claims getters are "STAGED against the stream's (renamed) basis once per kind"
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1368 (also 1357, 1371) — writeChunk calls `TransferCellShaping.quantumCellRows basis kind ...` per chunk; likewise TransferRun.fs:1304-1312 repoint recomputes `SurrogateRemap.fkColumnsTargeting remapTargetKinds kind \|> Map.filter ...` + `fkOrdinalsTargeting basis fkTargets` per chunk (called at 1348 and 1475)
- **evidence:** Catalog.fs:130-131 `tryOrdinal` = `Array.tryFindIndex` (O(width) per attribute => O(width^2) staging per chunk); TransferRun.fs:1301-1303 acknowledges: "FK ordinals resolved against the stream's renamed basis once per call (cheap; the chunk behind it is 50k rows)" — a conscious per-call note in code, but not in the declined ledger. Contrast pkOf, correctly staged once per kind at TransferRun.fs:1448.
- **suggested carrier:** kindWriteLane (per-kind closures — cellRowsOf, cellRowsExcludingIdentityOf, repointChunk — staged next to pkOf in phase1/phase2 before the chunk loop)

### S17 · Buffered Phase-2 recomputes the kind's PK/deferred attribute sets and re-resolves column names for EVERY row (the staged quantum sibling already exists)
- **plane/grain/path:** derivation / per-row / verb-only
- **fact:** The kind's pkAttrs and deferredAttrs (two List.filter walks of kind.Attributes) plus per-attribute column-name text — pure functions of (kind, deferred), recomputed inside phase2UpdateSql for every row of every deferred kind on the materialized transfer.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/TransferCellShaping.fs:88-89 — `let pkAttrs = kind.Attributes \|> List.filter (fun a -> a.IsPrimaryKey)` / `let deferredAttrs = ...` inside the per-row function
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:379 — `rowsForUpdate \|> List.choose (TransferCellShaping.phase2UpdateSql kind load.DeferredFkColumns)` applies it per row, re-paying the filters each time
- **evidence:** TransferCellShaping.fs:104-121: the quantum-grain sibling `phase2UpdateSqlQuantum` stages exactly these ("the per-attribute literal getters are staged against the stream's basis once per kind") and returns a per-row closure — the Map-carried path has no staged form, so `ColumnRealization.columnNameText a.Column` (line 97) also re-derives per row.
- **suggested carrier:** phase2UpdateSqlStaged (the StaticRow-carrier twin of phase2UpdateSqlQuantum: kind+deferred resolved once, returns StaticRow -> string option)

### S18 · Both data emitters build the identical DataInsertRow list TWICE for deferred kinds (Phase1Merges and Phase2Updates)
- **plane/grain/path:** materialization / per-row / publish-critical
- **fact:** The kind's typed DataInsertRow list — mkRow is pure over (id, values) with kind/deferred constants, so phase2Rows is element-for-element structurally identical to phase1Rows, yet a second full List.map materializes it for every deferred kind's rows.
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:186 — `let phase1Rows = typedRows \|> List.map (fun (id, vs) -> mkRow id vs)`
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:188-189 — `if Set.isEmpty deferred then [] else typedRows \|> List.map (fun (id, vs) -> mkRow id vs)` (byte-copy sibling at MigrationDependenciesEmitter.fs:245-248)
- **evidence:** mkRow (StaticSeedsEmitter.fs:181-185) = `{ KindKey = kind.SsKey; Identifier = identifier; Values = values; DeferredFkSet = deferred }` — no per-list state; `let phase2Rows = phase1Rows` is value-identical (records are immutable, sharing is safe).
- **suggested carrier:** phase1Rows (reused: `let phase2Rows = if Set.isEmpty deferred then [] else phase1Rows`)

### S19 · renderMerge derives the kind's column vocabulary repeatedly in one render: matchColumnNames twice, writableAttributes three-plus times
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** The kind's row-identity match columns (matchColumnNames: writable/identity/deferred filter chain) and writable attributes — pure functions of (deferred, kind), computed twice / thrice within a single renderMerge flow (and again in renderStagedPhase1 for staged kinds).
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:60 — `let matchColumns = KindColumns.matchColumnNames deferred k \|> Set.ofList`
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:79 — `PkColumns = KindColumns.matchColumnNames deferred k` (same call, same args, same flow)
- **evidence:** Also MergeRender.fs:75 `KindColumns.orderedColumnNames k` (internally writableAttributes), fs:81 `KindColumns.writableAttributes k`, and StagedMerge.fs:136 `stagingColumnDefsOf (KindColumns.writableAttributes k)` inside the same staged render; KindColumns.fs:85-91 shows matchAttributes itself calls writableAttributes.
- **suggested carrier:** matchColumnNames / writableAttrs (local bindings computed once at renderMerge top and threaded into args + StagedMerge)

### S20 · The staging verdict and the O(n) row count of typedRows are recomputed across scriptOfTyped and renderMerge (three full list-length walks plus a duplicated shouldStage)
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** The kind's row count (O(n) on an F# list) and DataStagingPolicy.shouldStage verdict — computed inside renderMerge (twice: gate + index gate, plus a .Length for the Bench sample) and recomputed by the calling emitter for Phase-2 routing, all over the same unchanged typedRows in one flow.
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:86,89,42 — `if DataStagingPolicy.shouldStage staging (List.length typedRows)`, `(DataStagingPolicy.shouldIndex staging (List.length typedRows))`, `(int64 typedRows.Length)`
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:163 — `elif DataStagingPolicy.shouldStage staging (List.length typedRows) then` recomputes the same verdict after renderMerge (line 160) already paid it (mirror: MigrationDependenciesEmitter.fs:226)
- **evidence:** Policy.fs:165-169 shouldStage is pure over (policy, rowCount). Four O(n) walks + two verdicts per kind per emit; if the verdicts ever diverge (they cannot today, but the coupling is implicit) Phase-1 staging and Phase-2 escalation would silently disagree — the shared binding also makes the phase-coherence explicit.
- **suggested carrier:** rowCount / stagedVerdict (computed once in scriptOfTyped and threaded into renderMerge, e.g. a renderMerge overload taking rowCount)

### S21 · attachDefaults and attachComputed recompute the kind's qualifiedParts (schema/table strings) inside the per-attribute map — per attribute instead of once per kind
- **plane/grain/path:** derivation / per-cell / verb-only
- **fact:** The kind's (schemaText, tableText) string pair from TableId.qualifiedParts — constant per kind, re-derived (two VO unwraps + a tuple) for every attribute of every kind in both attach helpers; the sibling attachAnnotations hoists it.
- **first receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1344-1346 — attachDefaults: `k.Attributes \|> List.map (fun a -> let schemaStr, tableStr = TableId.qualifiedParts k.Physical ...)`
- **second receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1367-1371 — attachComputed repeats the same per-attribute recompute shape
- **evidence:** Contrast ReadSide.fs:1394 attachAnnotations: `let schema, table = TableId.qualifiedParts k.Physical` hoisted above the walks. Coordinates.fs:322 `qualifiedParts (t) = (schemaText t, tableText t)` — cheap but per-attribute x per-kind x per-readback.
- **suggested carrier:** schemaStr/tableStr (one hoisted binding above each List.map, matching attachAnnotations)

### S22 · Streaming transfer recomputes each deferred kind's renamed RowBasis in Phase 2 after Phase 1 already built it
- **plane/grain/path:** derivation / per-kind / verb-only
- **fact:** The kind's renamed row basis (RowBasis.rename over Kind.rowBasis: array build + a name-sort permutation) — a pure function of (renameMap, ingestKind) computed in phase1 and recomputed from scratch in phase2 of the same run for every deferred kind.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1447 — phase1: `let basis = RowBasis.rename renameMap (Kind.rowBasis ingestKind)`
- **second receipt:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1522 — phase2: `let basis = RowBasis.rename renameMap (Kind.rowBasis ingestKind)` (same inputs, same run)
- **evidence:** RowBasis.ofNames (Catalog.fs:112-117) sorts an index permutation per build; rename (139-143) rebuilds via ofNames. Cheap per kind, but it is the exact 'recompute instead of thread' shape the topo-threading refactors (composeRenderedBundleWithBootstrapUsing etc.) just eliminated elsewhere.
- **suggested carrier:** basisByKind (Map<SsKey, RowBasis> built once before phase1 and threaded to both phases, alongside renderUpdate staging)

### S23 · The data lane rebuilds a catalog-stripped TableId from Kind.Physical at four sites, twice within one staged render flow
- **plane/grain/path:** representation / per-kind / publish-critical
- **fact:** The kind's catalog-less deploy coordinate `{ Schema = k.Physical.Schema; Table = k.Physical.Table; Catalog = None }` — k.Physical IS a TableId; the same re-encode is built in renderMerge and rebuilt by the emitter for renderStagedPhase2 in the same scriptOfTyped flow (and again per row in renderUpdate).
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:43-45 — `let table : TableId = { Schema = k.Physical.Schema; Table = k.Physical.Table; Catalog = None }` (via scriptOfTyped's renderMerge call, StaticSeedsEmitter.fs:160)
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:171-173 — the same record rebuilt for `StagedMerge.renderStagedPhase2` in the same flow (mirrors: MergeRender.fs:132-134 per Phase-2 row; MigrationDependenciesEmitter.fs:230-232)
- **evidence:** Coordinates.fs:218-229 — TableId already carries Catalog:string option; the rebuild exists only to null the Catalog qualifier. Contrast TransferCellShaping.fs:99 which passes `Target = kind.Physical` directly. Borderline-small; the semantic content (deploy-to-connection's-catalog) deserves a name rather than four inline re-encodes.
- **suggested carrier:** TableId.withoutCatalog (or a `deployTarget` binding threaded from scriptOfTyped into both phase renders)

### S24 · Deploy re-parses V2's own rendered data scripts with TSql160Parser to recover the GO boundaries V2 itself inserted
- **plane/grain/path:** representation / per-kind / publish-critical
- **fact:** The batch boundaries of a per-kind rendered data script (Phase-1 MERGE + per-row Phase-2 UPDATEs), each batch framed by renderDataBatch's own ';\nGO\n' literal at render time
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/ScriptDomGenerate.fs:189 — renderDataBatch appends the GO frame itself: the boundary is known typed, per statement list, at render time
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Deploy.fs:478 — executeBatchParallel re-parses every member text with the full T-SQL parser (BatchSplitter.splitWithLoudFallback → BatchSplitter.fs:118-148 TSql160Parser.Parse + per-batch Substring) to rediscover those same boundaries
- **evidence:** renderDataBatch: `sb.Append("GO\n").ToString()` (ScriptDomGenerate.fs:189); executeBatchParallel: `\|> Array.collect (BatchSplitter.splitWithLoudFallback "deploy.executeBatchParallel")` (Deploy.fs:478); splitViaScriptDom: `let fragment = parser.Parse(reader, &errors)` then `sql.Substring(batch.StartOffset, len).Trim()` (BatchSplitter.fs:124,145). The members flow from LeveledDeploymentText (DataEmissionComposer.fs:494-496), whose Phase-2 texts contain one GO per row (StaticSeedsEmitter.fs:177-178). executeLeveledSeed (Deploy.fs:521-532) is the production seed dispatch. Note declined-list adjacency: 'lane streaming writes (Outputs.DataBundle contract)' was declined, but that was about streaming writes, not about re-parsing rendered text to recover self-inserted batch boundaries.
- **suggested carrier:** ParallelSafe<SegmentedScript> (segments carried as string list from renderDataBatch instead of a joined blob re-split by parser)

### S25 · Render.toTextWith builds the whole SSDT artifact, then ConstraintFormatter.format copies it twice more via split-then-rejoin
- **plane/grain/path:** representation / per-publish / publish-critical
- **fact:** The rendered script's line sequence — materialized once in the accumulation StringBuilder, then re-materialized as a line array, then re-accumulated into a second StringBuilder
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/Render.fs:148-167 — statements fold into `sb`, then `ConstraintFormatter.format mode (sb.ToString())` pays the first whole-text copy
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/ConstraintFormatter.fs:510-521 — `script.Split([\| '\n' \|], ...)` copies the whole text into per-line strings, then `StringBuilder(script.Length + 256)` re-joins everything into a fourth whole-text materialization (`sb.ToString()`)
- **evidence:** `ConstraintFormatter.format mode (sb.ToString())` (Render.fs:167); `let lines = script.Split([\| '\n' \|], StringSplitOptions.None)` and `sb.Append(line).Append(newLine)` (ConstraintFormatter.fs:510,520). Runs per kind file too (SsdtDdlEmitter.fs:785 `Render.toTextWith renderMode separated`) and on the ≥100k-statement RawTextEmitter path the docstring itself flags copy costs (Render.fs:127-131). Also each line's \r check (ConstraintFormatter.fs:515) re-guards what pinNewlines already normalized.
- **suggested carrier:** per-statement formatting threaded into the one StringBuilder (formatIntoUsing / a RenderedLines seq carrier instead of the text→lines→text round-trip)

### S26 · ConstraintFormatter pays TrimStart and the body-keyword index for the same line two to three times across dispatch and formatting
- **plane/grain/path:** derivation / per-row / publish-critical
- **fact:** The trimmed form of a line and the position of its constraint body keyword (FOREIGN KEY / PRIMARY KEY / CHECK / REFERENCES) — pure functions of the unchanged line
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/ConstraintFormatter.fs:474,485-490 — tryFormatLine computes `let trimmed = line.TrimStart()` and dispatches on `indexAfterBracketedName trimmed "FOREIGN KEY"` / `"REFERENCES"` / `"PRIMARY KEY"` / `"CHECK"`
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/ConstraintFormatter.fs:260,183-184,286 — formatTableLevelForeignKey recomputes `indentOf line` (TrimStart again inside indentOf:133-134) plus `line.TrimStart()` a third time, and appendForeignKeySegment/formatTableLevelTwoLine recompute the same keyword indexes on the same content
- **evidence:** `appendForeignKeySegment sb (indentOf line) (line.TrimStart())` (ConstraintFormatter.fs:260) after tryFormatLine already trimmed (474); `let fkIdx = indexAfterBracketedName withoutComma "FOREIGN KEY"` (183) repeats the dispatch probe from 485. indentOf itself allocates a trimmed copy just to measure it: `let trimmed = line.TrimStart() in line.Substring(0, line.Length - trimmed.Length)` (133-134).
- **suggested carrier:** TrimmedLine (indent, trimmed) pair threaded from tryFormatLine into the per-shape formatters (tryFormatLineUsing)

### S27 · MergeRender.renderUpdate recomputes per-kind-constant values (Bench label, matchAttributes, deferred-attribute filter) for every Phase-2 row
- **plane/grain/path:** derivation / per-row / publish-critical
- **fact:** For a fixed (kind, deferred) pair: the concatenated Bench label 'emit.<lane>.renderUpdate', the row-scope attribute list KindColumns.matchAttributes deferred k, and the deferred-column attribute filter — all constants across the row loop
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:131,141-142,151 — computed for the first row: `Bench.scope (System.String.Concat(bench, ".renderUpdate"))`, `k.Attributes \|> List.filter (fun a -> Set.contains a.Name deferred)`, `KindColumns.matchAttributes deferred k`
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:177 and MigrationDependenciesEmitter.fs:236 — the per-row loops (`Bench.iterMap "emit.staticSeeds.phase2Row" (fun (_, vs) -> MergeRender.renderUpdate "emit.staticSeeds" cdcAware kind deferred vs)`) re-pay all three for rows 2..N
- **evidence:** `use _ = Bench.scope (System.String.Concat(bench, ".renderUpdate"))` (MergeRender.fs:131) executes once per row; `KindColumns.matchAttributes deferred k \|> List.map cellOf` (MergeRender.fs:150-152) rebuilds the same attribute scan per row. Only `typedValues` changes across the loop.
- **suggested carrier:** renderUpdateForKindUsing (per-kind prebound closure carrying label, setAttrs, whereAttrs; rows thread only typedValues)

### S28 · SsdtDdlEmitter recomputes the 'first kind of this schema in the module' by re-filtering and re-sorting the module's kinds for every kind
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** The alphabetically-first kind per (module, schema) — a per-container constant that decides which kind emits the module's SCHEMA-level extended properties
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:715-719 — computed for kind #1: `m.Kinds \|> List.filter (fun candidate -> candidate.Physical.Schema = schema) \|> List.sortBy (fun candidate -> candidate.SsKey) \|> List.tryHead`
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:744 — moduleSchemaPropertyStatements runs inside kindToSsdtFile for EVERY kind of the module (`yield! moduleSchemaPropertyStatements m k`), re-paying the O(K log K) filter+sort per kind (O(K² log K) per module)
- **evidence:** `let firstKindOfSchema = m.Kinds \|> List.filter ... \|> List.sortBy ... \|> List.tryHead` (SsdtDdlEmitter.fs:715-719); caller loop is per-kind file emission (kindToSsdtFile, 728-786). Contrast with the same file's own fixed precedent one screen down: 'The emitted-index-name map derives ONCE per kind and feeds all three consumers — previously each recomputed it' (738-741).
- **suggested carrier:** firstKindBySchema : Map<SchemaName, SsKey> per module, threaded into kindToSsdtFile (moduleSchemaPropertyStatementsUsing)

### S29 · CatalogCodec.wStaticRow sorts the row's values then immediately discards the sort by rebuilding a Map just to iterate it
- **plane/grain/path:** representation / per-row / publish-critical
- **fact:** The row's (column name, value) pairs in deterministic iteration order — already available from Map.toSeq (F# Map enumerates in key order)
- **first receipt:** sidecar/projection/src/Projection.Targets.Json/CatalogCodec.fs:236 — the sorted pair sequence exists: `r.Values \|> Map.toSeq \|> Seq.sortBy (fun (n, _) -> Name.value n)`
- **second receipt:** sidecar/projection/src/Projection.Targets.Json/CatalogCodec.fs:236 — the same pairs are paid again through `\|> Map.ofSeq` (an O(n log n) tree rebuild whose own key ordering supersedes the just-computed sortBy) before the `for KeyValue (name, value) in ...` enumeration
- **evidence:** `for KeyValue (name, value) in (r.Values \|> Map.toSeq \|> Seq.sortBy (fun (n, _) -> Name.value n) \|> Map.ofSeq) do` (CatalogCodec.fs:236). The Map.ofSeq re-keys by Name's own comparison, so the explicit Seq.sortBy is dead work and the rebuilt Map is enumerated once and dropped; runs for every static row of every catalog serialize (episodes, digests, snapshots).
- **suggested carrier:** sortedValues (the seq from Map.toSeq consumed directly — one binding, no Map rebuild)

### S30 · LifecycleStore embeds each episode's catalog by serializing to a UTF-16 string and having WriteRawValue re-scan and re-encode it back to UTF-8
- **plane/grain/path:** representation / per-publish / publish-critical
- **fact:** The catalog's JSON byte stream for one episode — produced as UTF-8 bytes inside CatalogCodec.serialize, decoded to a string, then re-validated and re-encoded to UTF-8 by the outer writer
- **first receipt:** sidecar/projection/src/Projection.Core/PinnedWriting.fs:94-100 — writeToString produces the UTF-8 bytes then pays `Encoding.UTF8.GetString(stream.ToArray())` to make the string carrier
- **second receipt:** sidecar/projection/src/Projection.Pipeline/LifecycleStore.fs:129 — `jw.WriteRawValue(CatalogCodec.serialize e.Schema)` transcodes the whole string back to UTF-8 (and JSON-validates it by scanning) into the store's stream
- **evidence:** `jw.WritePropertyName "schema"; jw.WriteRawValue(CatalogCodec.serialize e.Schema)` (LifecycleStore.fs:128-129); the module's own header says 'Composes the codec, does not re-encode the Catalog' (LifecycleStore.fs:20) — but the byte→string→byte hop is exactly a re-encoding of the carrier. The catalog JSON is the largest payload in the store; it is paid per episode on every save.
- **suggested carrier:** CatalogCodec.writeTo : Utf8JsonWriter -> Catalog -> unit (the existing private wCatalog exposed so the episode writer nests directly, no string hop)

### S31 · PinnedWriting's string renderers copy the whole serialized artifact an extra time via stream.ToArray() before decoding
- **plane/grain/path:** representation / per-publish / publish-critical
- **fact:** The serialized JSON bytes sitting in the MemoryStream buffer
- **first receipt:** sidecar/projection/src/Projection.Core/PinnedWriting.fs:95-99 — the bytes are first paid into the MemoryStream by the Utf8JsonWriter
- **second receipt:** sidecar/projection/src/Projection.Core/PinnedWriting.fs:100 (and 87, and ManifestEmitter.fs:955) — `Encoding.UTF8.GetString(stream.ToArray())` copies the entire buffer a second time before the third (string) materialization; `GetString(span)` over the stream's live buffer would skip the middle copy
- **evidence:** `Encoding.UTF8.GetString(stream.ToArray())` (PinnedWriting.fs:87 renderNodeToString, PinnedWriting.fs:100 writeToString, ManifestEmitter.fs:955 toJson). Every codec serialize (Catalog/Profile/Correction/Golden/Slice) and the manifest ride this. BORDERLINE: idiomatic BCL pattern; the double payment is one full byte copy per serialize.
- **suggested carrier:** GetString over stream.GetBuffer()/TryGetBuffer span (one binding: writtenSpan)

### S32 · ProfileCodec.wCdc serializes each SsKey twice — once as the sort key, once to write the value
- **plane/grain/path:** representation / per-row / verb-only
- **fact:** SsKey.serialize k for each CDC-enabled key / instance-map key (the typed key round-tripped through its string form for ordering, then again for emission)
- **first receipt:** sidecar/projection/src/Projection.Targets.Json/ProfileCodec.fs:197,200 — `Set.toList \|> List.sortBy SsKey.serialize` and `Map.toList \|> List.sortBy (fst >> SsKey.serialize)` pay the serialization as the sort key
- **second receipt:** sidecar/projection/src/Projection.Targets.Json/ProfileCodec.fs:55,202 — wSsKeyVal pays it again to write the same element: `jw.WriteStringValue (SsKey.serialize k)` / `wField jw "key" wSsKeyVal k`
- **evidence:** `wList jw "enabled" wSsKeyVal (c.CdcEnabled \|> Set.toList \|> List.sortBy SsKey.serialize)` (ProfileCodec.fs:197): sortBy projects SsKey.serialize per element, then wSsKeyVal (55) re-serializes each for the write. Same shape at 200-202 for CdcInstance. Also Set.toList/Map.toList already enumerate in the keys' own comparison order, which the re-sort may or may not match — the serialized form is the pinned truth, computed twice.
- **suggested carrier:** serializedKeys : string list (List.map SsKey.serialize \|> List.sort, written directly)

### S33 · Deploy.executeStreamWith renders each DDL statement into a throwaway StringBuilder, then copies the text into the pending-DDL buffer
- **plane/grain/path:** representation / per-row / publish-critical
- **fact:** One DDL statement's rendered text — written once into a fresh local StringBuilder, then copied again into pendingDdl
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Deploy.fs:598-599 — `let sb = System.Text.StringBuilder() in Render.toSql sb s` pays the text into the local builder
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Deploy.fs:600 — `pendingDdl.Append(sb.ToString()) \|> ignore` pays the same text twice more (ToString materialization + Append copy), although Render.toSql takes any StringBuilder and could write into pendingDdl directly
- **evidence:** `let appendDdl (s: Statement) = let sb = System.Text.StringBuilder() in Render.toSql sb s; pendingDdl.Append(sb.ToString()) \|> ignore` (Deploy.fs:597-600); Render.toSql's signature is `(sb: StringBuilder) (s: Statement) : unit` (Render.fs:81) — the indirection buys nothing.
- **suggested carrier:** Render.toSql pendingDdl s (direct write; the pendingDdl binding IS the carrier)

### S34 · FakerRealization re-serializes the row's SsKey for every fresh-draw cell in the row
- **plane/grain/path:** derivation / per-cell / verb-only
- **fact:** SsKey.serialize rowKey — the row-identity string that seeds both the per-row person faker and every per-cell fresh-draw seed, constant across the row's cells
- **first receipt:** sidecar/projection/src/Projection.Pipeline/FakerRealization.fs:222,29 — paid per row (per locale group): `fakerOfLocale locale (seedOf rowKey)` where seedOf does `let s = SsKey.serialize key`
- **second receipt:** sidecar/projection/src/Projection.Pipeline/FakerRealization.fs:124 via 189,228-229 — paid again for EVERY fresh-draw cell: `seedOfCell rowKey column` does `let s = SsKey.serialize rowKey + " " + Name.value column`, and realizeRow folds `freshOrValue rowKey colName ...` over all non-person bindings
- **evidence:** `let s = SsKey.serialize rowKey + " " + Name.value column` (FakerRealization.fs:124) inside the per-cell fold (228-229); the FNV hash also re-hashes the identical rowKey prefix per cell where a row-prefix hash state could be threaded.
- **suggested carrier:** rowSeedBasis (the serialized rowKey — or its partial FNV state — bound once per row and threaded into seedOfCell)

### S35 · Profile.tryFind* lookups are O(n) list scans re-paid per attribute/reference across every tightening and anomaly pass — the SsKey index (kindIndex/attributeIndex CWT precedent) was never built for Profile
- **plane/grain/path:** derivation / per-row / publish-critical
- **fact:** The location of an attribute's/reference's evidence inside profile.Columns / UniqueCandidates / Distributions / ForeignKeys / ForeignKeyCardinalities — a pure SsKey→entry association over an immutable Profile — is re-derived by a fresh linear scan on every per-item lookup instead of once as an index
- **first receipt:** sidecar/projection/src/Projection.Core/Profile.fs:1057-1058 — the lookup is defined as a full linear scan: `p.Columns \|> List.tryFind (fun c -> c.AttributeKey = attributeKey)` (same shape for tryFindUnique:1076, tryFindCategorical:1082, tryFindNumeric:1095, tryFindForeignKey:1062, tryFindForeignKeyCardinality:1068)
- **second receipt:** sidecar/projection/src/Projection.Core/Strategies/NullabilityRules.fs:249 — `Profile.tryFindColumn attribute.SsKey profile` invoked per (attribute × intervention) by Composition.fanOut (Composition.fs:143-145); likewise ProfileAnomalyPass.fs:71 and :94 per attribute over allKinds, UniqueIndexRules.fs:163 per index, CategoricalUniquenessRules.fs:216 per attribute, ForeignKeyRules.fs:288 per reference — O(attributes × profile-size) per chain run
- **evidence:** Profile.fs: `let tryFindColumn (attributeKey: SsKey) (p: Profile) : ColumnProfile option = p.Columns \|> List.tryFind (fun c -> c.AttributeKey = attributeKey)`. NullabilityRules.fs:249: `match Profile.tryFindColumn attribute.SsKey profile with`. ProfileAnomalyPass.fs:71: `match Profile.tryFindColumn a.SsKey profile with` inside `allKinds \|> List.collect (fun k -> k.Attributes \|> List.choose ...)`. House precedent for the fix: Catalog.fs:1779-1806 kindIndex ConditionalWeakTable ('the prior List.tryPick was O(modules × kinds) per call; at 300-table scale that compounded') — Profile has no such sibling.
- **suggested carrier:** Profile.columnIndex / Profile.evidenceIndex — ConditionalWeakTable<Profile, Map<SsKey, _>> caches per Profile instance (the Catalog.kindIndex precedent), or a per-pass prebuilt index threaded via a fanOutUsing form

### S36 · ProfileDerivation FK derivations re-resolve the target kind and its single-PK attribute per reference at two-plus sites in one attachFromCache flow
- **plane/grain/path:** derivation / per-row / publish-critical
- **fact:** For each Reference, the resolution 'target kind → its single primary-key attribute' (Catalog.tryFindKind + Kind.primaryKey, a List.filter over attributes with no cache) is computed when building the ForeignKeyTargetIndex and computed again inside deriveForeignKeyRealitiesWith just to reconstruct the (kindKey, attrKey) lookup key the index was built under — a third copy sits in deriveForeignKeyOrphanSamplesWith
- **first receipt:** sidecar/projection/src/Projection.Core/ProfileDerivation.fs:501-507 — buildForeignKeyTargetIndex: `match Catalog.tryFindKind reference.TargetKind catalog with ... match Kind.primaryKey tgtKind with \| [ tgtPkAttr ] -> Some (tgtKind.SsKey, tgtPkAttr.SsKey)` (called at attachFromCache line 1003)
- **second receipt:** sidecar/projection/src/Projection.Core/ProfileDerivation.fs:549-557 — deriveForeignKeyRealitiesWith (called at attachFromCache line 1009) repeats `match Catalog.tryFindKind reference.TargetKind catalog ... match Kind.primaryKey tgtKind with \| [ tgtPkAttr ] -> ... Map.tryFind (tgtKind.SsKey, tgtPkAttr.SsKey) targetIndex`; third copy at :627-637 in deriveForeignKeyOrphanSamplesWith
- **evidence:** The index docstring (ProfileDerivation.fs:479-486) says it 'eliminates 2x duplicate work across the two FK derivation passes' — but only the TargetKeySet CONSTRUCTION was deduped; the key derivation (tryFindKind + primaryKey per reference) is still paid per consumer because the Map is keyed by (targetKindKey, targetPkAttrKey) rather than by Reference. Kind.primaryKey (Catalog.fs:1553-1554) is an uncached List.filter, unlike its CWT-cached sibling attributeIndex (Catalog.fs:1588-1602).
- **suggested carrier:** ForeignKeyTargetIndex keyed by Reference.SsKey (Map<SsKey, TargetKeySet>), or a widened entry carrying the resolved tgtPkAttr so consumers look up once per reference

### S37 · Pipeline diagnostics assembly computes DecisionOverlay.ofComposeState finalState twice back-to-back
- **plane/grain/path:** derivation / per-publish / publish-critical
- **fact:** The DecisionOverlay projection of the final ComposeState — four Set builds over the three tightening decision lists — is a pure function of the unchanged finalState, computed once for foreignKeyDecisionDropDiagnostics and again six lines later for foreignKeyNameCollisionDiagnostics in the same diagnostics list expression
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1520-1521 — `@ SsdtDdlEmitter.foreignKeyDecisionDropDiagnostics (DecisionOverlay.ofComposeState finalState) finalState.Catalog`
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1526-1527 — `@ SsdtDdlEmitter.foreignKeyNameCollisionDiagnostics (DecisionOverlay.ofComposeState finalState) finalState.Catalog`
- **evidence:** Both call sites pass `(DecisionOverlay.ofComposeState finalState)` on the same finalState. DecisionOverlay.fs:94-100 shows ofComposeState builds EnforceNotNull/EnforceUnique/DropFk/NoCheckFk via four List.choose+Set.ofList walks (fkKeys runs twice over ForeignKeyDecisions).
- **suggested carrier:** let overlay = DecisionOverlay.ofComposeState finalState — one binding threaded to both diagnostic emitters

### S38 · writableAttributes re-derived two-to-three times per kind within one renderMerge (inline and staged paths)
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** The kind's writable (non-computed) attribute list is a pure per-kind filter recomputed inside one renderMerge flow: once via orderedColumnNames for AllColumns, again explicitly for the Rows projection, and a third time inside renderStagedPhase1 for the staging column defs on the staged path
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:75 — `AllColumns = KindColumns.orderedColumnNames k` (KindColumns.fs:50-51: `writableAttributes k \|> List.map ...`)
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:81 — `Rows = typedRows \|> List.map (KindColumns.typedValuesToSqlLiterals deferred (KindColumns.writableAttributes k))`; third payment on the staged path at StagedMerge.fs:136 — `stagingColumnDefsOf (KindColumns.writableAttributes k)` (invoked from renderMerge:88)
- **evidence:** KindColumns.fs:45-46: `let writableAttributes (k: Kind) : Attribute list = k.Attributes \|> List.filter (fun a -> a.Computed = None)`. MergeRender.fs:75 and :81 both reach it in one call; StagedMerge.fs:136 pays again for the same k when staging fires. matchColumnNames at lines 60/79 may run it a fourth/fifth time for no-PK kinds (KindColumns.fs:87-88).
- **suggested carrier:** let writable = KindColumns.writableAttributes k bound once in renderMerge and threaded (or a per-kind KindColumns vocabulary record: writable/ordered/matchNames computed together)

### S39 · Nullability and CategoricalUniqueness passes each re-derive the identical sorted (Kind × Attribute) context list over the same unchanged catalog in one chain run
- **plane/grain/path:** derivation / per-publish / publish-critical
- **fact:** The deterministic per-attribute iteration source — Catalog.kindContexts (fun k -> k.Attributes) (fun a -> a.SsKey), a sort of all kinds by SsKey plus a per-kind sort of attributes — is computed independently by two tightening passes (and the kinds-by-SsKey sort by all four) over the same catalog instance in one publish chain
- **first receipt:** sidecar/projection/src/Projection.Core/Passes/NullabilityPass.fs:81-82 — `let private sortedAttributes (catalog: Catalog) = Catalog.kindContexts (fun k -> k.Attributes) (fun a -> a.SsKey) catalog`
- **second receipt:** sidecar/projection/src/Projection.Core/Passes/CategoricalUniquenessPass.fs:85-86 — byte-identical `Catalog.kindContexts (fun k -> k.Attributes) (fun a -> a.SsKey) catalog`; both run sequentially over the unchanged catalog per RegisteredTransforms.fs:157-160 (tightening decision passes leave the Catalog untouched)
- **evidence:** Catalog.fs:1709-1720 kindContexts: `c.Modules \|> List.collect (fun m -> m.Kinds) \|> List.sortBy (fun k -> k.SsKey) \|> List.collect (fun k -> extract k \|> List.sortBy sortKey ...)`. Chain order (RegisteredTransforms.fs:157-160): Nullability → UniqueIndex → ForeignKey → CategoricalUniqueness, all decision-only steps over the same catalog; UniqueIndex/ForeignKey additionally re-pay the kinds-by-SsKey sort with different extractors.
- **suggested carrier:** CWT-cached Catalog.sortedKinds (the kindIndex precedent) consumed by kindContexts, or an attributeContexts value computed once and threaded to both passes via a fanOutUsing/SortedContextsUsing threading form

### S40 · ForeignKeyPass opportunityEntry pays the tryFindForeignKeyCardinality linear scan twice for the same reference in one decision
- **plane/grain/path:** derivation / per-row / display-only
- **fact:** The FK-cardinality evidence for decision.ReferenceKey — a linear scan over profile.ForeignKeyCardinalities — is looked up once by cardinalityMetadata (for the meanChildCount metadata) and again by cardinalitySuggestedConfig (for the enableCreation suggestion) within the same opportunityEntry evaluation of a PolicyDisabled decision
- **first receipt:** sidecar/projection/src/Projection.Core/Passes/ForeignKeyPass.fs:123 — cardinalityMetadata: `match Profile.tryFindForeignKeyCardinality referenceKey profile with` (invoked at :183-185 to build baseMetadata for every decision)
- **second receipt:** sidecar/projection/src/Projection.Core/Passes/ForeignKeyPass.fs:143 — cardinalitySuggestedConfig: `match Profile.tryFindForeignKeyCardinality referenceKey profile with` (invoked at :216 in the DoNotEnforce PolicyDisabled arm, same decision.ReferenceKey, same profile)
- **evidence:** ForeignKeyPass.fs:185: `\|> cardinalityMetadata decision.ReferenceKey profile`; :216: `(cardinalitySuggestedConfig decision.InterventionId decision.ReferenceKey profile)`. Profile.fs:1067-1068 shows each lookup is `p.ForeignKeyCardinalities \|> List.tryFind (fun c -> c.ReferenceKey = referenceKey)`.
- **suggested carrier:** one `cardinalityOpt` binding per decision (Profile.tryFindForeignKeyCardinality resolved once, threaded into both metadata and suggested-config builders)

### S41 · SchemaComplexityPass cohesion re-tests every edge's module membership twice per module and re-walks the full edge list per module
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** Whether an edge endpoint belongs to a module's kind set — Set.contains src/tgt kindSet — is evaluated in the intra-edge filter and evaluated again for the same (module, edge) pair in the total-edge filter immediately below; and the whole t.Edges list is re-walked twice for every module though a single pass with the CWT-cached kindOwnershipIndex would classify each edge once
- **first receipt:** sidecar/projection/src/Projection.Core/Passes/SchemaComplexityPass.fs:82-86 — `let intra = t.Edges \|> List.filter (fun (src, tgt) -> Set.contains src kindSet && Set.contains tgt kindSet) \|> List.length`
- **second receipt:** sidecar/projection/src/Projection.Core/Passes/SchemaComplexityPass.fs:87-91 — `let total = t.Edges \|> List.filter (fun (src, tgt) -> Set.contains src kindSet \|\| Set.contains tgt kindSet) \|> List.length` (same edges, same kindSet, same membership tests re-run)
- **evidence:** Both filters run inside `moduleKindSets \|> List.choose (fun kindSet -> ...)` — O(modules × edges × 2) membership probes. The pass runs on every canonical chain (RegisteredTransforms.fs:135-141); output is advisory. Catalog.kindOwnershipIndex (Catalog.fs:1794-1801, CWT-cached) already answers kind→module in O(log n) — one fold over edges classifying (intraModule, touchingModule) would pay each edge once.
- **suggested carrier:** a single fold per module returning (intra, total) in one edge walk — or edge classification through Catalog.kindOwnershipIndex done once for all modules (edgeModuleIncidence)

### S42 · CentralityPass pageRankStep re-derives graph-constant facts (node count, dangling-node set) on every PageRank iteration
- **plane/grain/path:** derivation / per-publish / publish-critical
- **fact:** decimal(List.length nodes) and the identification of dangling nodes (outDegree = 0) are pure functions of the unchanged node list and adjacency built once before the loop, yet both are recomputed inside pageRankStep on each of up to maxIterations iterations (the dangling-node scan probes outDegree per node per iteration; only the rank SUM over that fixed set changes)
- **first receipt:** sidecar/projection/src/Projection.Core/Passes/CentralityPass.fs:143 + :74 — `let outDegree, revAdj = buildAdjacency nodes t.Edges` (out-degrees fixed once) and initialRank's `let nDecimal = decimal (List.length nodes)`
- **second receipt:** sidecar/projection/src/Projection.Core/Passes/CentralityPass.fs:83-90 — per iteration: `let nDecimal = decimal (List.length nodes)` and `nodes \|> List.sumBy (fun u -> let du = if outDegree.ContainsKey u then outDegree.[u] else 0; if du = 0 then Map.tryFind u rank ... )` re-testing dangling-ness of every node every iteration (driven by Fixpoint.iterate at :117-124)
- **evidence:** pageRankStep is called once per iteration by `Fixpoint.iterate maxIterations` (CentralityPass.fs:121-124; maxIterations from AdvisoryTuning). Lines 83-90 quoted above; List.length is O(n) per call on an F# list, and the `du = 0` classification never changes after buildAdjacency.
- **suggested carrier:** nodeCount + danglingNodes bindings hoisted before runUntilConverged and threaded into pageRankStep (pageRankStepWith nodeCount danglingNodes ...)

### S43 · Closure.report rebuilds the target kind's closed-PK-value Set from the row Map once per reference instead of once per target kind
- **plane/grain/path:** derivation / per-row / verb-only
- **fact:** The set of closed PK values for a target kind — closedKeysOf, a full Map.toSeq/Set.ofSeq walk over all of that kind's accumulated rows — is a pure function of the final ClosureState, recomputed inside the per-reference loop so N references pointing at the same target rebuild the identical set N times
- **first receipt:** sidecar/projection/src/Projection.Core/Closure.fs:247-251 — `let private closedKeysOf (target: SsKey) (state: ClosureState) = state.Rows \|> Map.tryFind target \|> Option.defaultValue Map.empty \|> fun m -> m \|> Map.toSeq \|> Seq.map fst \|> Set.ofSeq`
- **second receipt:** sidecar/projection/src/Projection.Core/Closure.fs:271 — `let parentKeys = closedKeysOf r.TargetKind state` inside `for r in kind.References` inside `for (kindKey, rowsByPk) in Map.toList state.Rows` — re-paid per mandatory reference, including repeats for the same TargetKind
- **evidence:** report's dangling loop (Closure.fs:263-284): every mandatory-FK reference triggers `closedKeysOf r.TargetKind state`, an O(rows) Set materialization; two kinds each holding a mandatory FK to the same parent rebuild the parent's key set twice. The same Map already answers membership via Map.containsKey.
- **suggested carrier:** closedKeySets : Map<SsKey, Set<string>> built once per report (Map.map over state.Rows) and consulted per reference — or direct Map.containsKey against state.Rows entries

### S44 · Closure.nextFetches materializes a fresh closed-keys Set per (target × step) when the backing Map already answers the needed membership
- **plane/grain/path:** materialization / per-kind / verb-only
- **fact:** The 'already-closed keys of target' value is eagerly re-encoded from Map<string, StaticRow> keys into a Set<string> on every fixpoint step for every demanded target, though the consumer needs only membership tests (Set.difference against candidates) that Map.containsKey on the existing structure provides without the copy
- **first receipt:** sidecar/projection/src/Projection.Core/Closure.fs:130-132 — the closed rows already live keyed by PK value: `Map.add key row acc` folded into `state.Rows` (membership is answerable in place)
- **second receipt:** sidecar/projection/src/Projection.Core/Closure.fs:184-186 — per step, per target: `let closed = st.Rows \|> Map.tryFind target \|> Option.defaultValue Map.empty \|> fun m -> m \|> Map.toSeq \|> Seq.map fst \|> Set.ofSeq` then `Set.difference candidateKeys (Set.union closed requested)`
- **evidence:** nextFetches (Closure.fs:175-196) runs once per fixpoint step; the Set.ofSeq copy is O(closed rows) per (target × step) and grows as the closure grows, while the only use is `Set.difference candidateKeys ...` — `candidateKeys \|> Set.filter (fun k -> not (Map.containsKey k closedMap) && not (Set.contains k requested))` needs no materialized copy.
- **suggested carrier:** direct Map.containsKey filtering over candidateKeys (closedMap binding), no Set projection — or a maintained closedKeys : Map<SsKey, Set<string>> field on ClosureState updated incrementally in foldFetched

### S45 · Publish-then-load re-reads the model, re-hydrates live rows, re-runs the whole pass chain, and re-renders every seed lane the publish just produced
- **plane/grain/path:** acquisition / per-publish / publish-critical
- **fact:** The hydrated catalog, bootstrap rows, migration context, post-chain ComposeState, and the per-kind rendered Phase1/Phase2 seed scripts for the config — all already produced inside runWithConfig's publish — are re-acquired and re-derived from scratch by the load leg's emittedSeedPlan (second OSSYS/file model read, second live row hydration, second full registered pass chain, second dispatchSiblings render).
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2357 — `let! reportResult = runWithConfig cfg` (publish path: readAndHydrateConfigModel at :1827, chain+lanes at :1451/:803)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2334 — `let! seedR = emittedSeedPlan cfg` → :1944 `readAndHydrateConfigModel cfg` again, :1914-1915 `projectWithState policy Profile.empty …` (whole chain again), :1925 `composeRenderedLeveledWithBootstrap` (lanes re-rendered)
- **evidence:** First: `runWithConfigAndLoad … let! reportResult = runWithConfig cfg` then `let! loadResult = loadFromConfig …` (Pipeline.fs:2357-2361). Second: `let private emittedSeedPlan (cfg) … let! parsed = readAndHydrateConfigModel cfg … projectSeedPlan cfg …` (Pipeline.fs:1939-1947) and `let _, finalState = projectWithState policy Profile.empty folders groups catalog … DataComposer.composeRenderedLeveledWithBootstrap policy finalState.Catalog …` (Pipeline.fs:1914-1926). The comment at :1941-1943 names the duplication as a parity duty: "hydrate the store-leg catalog the SAME way the publish path does". Caveat for adjudication: PERF_OPPORTUNITIES.md Sweep 2 item 5 acknowledges only "the store-leg's separate run" as a remaining TOPO compute; the model re-read / row re-hydration / chain re-run / lane re-render are not adjudicated there. Note also composeRenderedLeveledWithBootstrap re-runs Kahn/Tarjan (DataEmissionComposer.fs:533) although finalState.TopologicalOrder is in hand at the call site.
- **suggested carrier:** loadFromConfigUsing (report: RunReport, finalState: ComposeState, bootstrapRows, migration) — thread the publish's already-hydrated catalog + ComposeState (and its TopologicalOrder) into the seed-plan render instead of re-deriving from cfg

### S46 · buildLookups (allKinds walk + pkAttrByKey map) rebuilt three times per publish by the emit step and the two FK diagnostics siblings; a fourth allKinds walk in the decision-drop sibling
- **plane/grain/path:** derivation / per-publish / publish-critical
- **fact:** The FK-resolution lookup triple over the post-chain catalog — Catalog.allKinds flattening plus the pkAttrByKey Map (per-kind PK attribute) — is a pure function of the unchanged finalState.Catalog, rebuilt by SsdtDdlEmitter.emitSlicesWithRendering, foreignKeyDropDiagnostics, and foreignKeyNameCollisionDiagnostics within one runWithConfigCore publish (foreignKeyDecisionDropDiagnostics re-walks Catalog.allKinds a fourth time; targetByKey escapes only because Catalog.kindIndex is ConditionalWeakTable-cached — allKinds and pkAttrByKey are not cached).
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:964 — `let _allKinds, targetByKey, pkAttrByKey = buildLookups catalog` (the publish's SSDT emit step, Pipeline.fs:566)
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:1013 — `let allKinds, targetByKey, pkAttrByKey = buildLookups catalog` in foreignKeyDropDiagnostics (invoked at Pipeline.fs:1511); third at SsdtDdlEmitter.fs:1120 in foreignKeyNameCollisionDiagnostics (Pipeline.fs:1526-1527); fourth allKinds walk at SsdtDdlEmitter.fs:1077 in foreignKeyDecisionDropDiagnostics (Pipeline.fs:1520-1521)
- **evidence:** buildLookups body (SsdtDdlEmitter.fs:817-829): `let allKinds = Catalog.allKinds catalog … let pkAttrByKey = allKinds \|> List.choose (fun k -> k.Attributes \|> List.tryFind (fun a -> a.IsPrimaryKey) …) \|> Map.ofList`. Under the default EmissionPolicy, EmissionSeam.apply is reference-preserving (`if policy.IncludePlatformAutoIndexes then c`, Policy.fs:773), so the emit step's `ctx.EmittedCatalog` and the diagnostics' `finalState.Catalog` are the SAME reference — the rebuilt lookups are byte-identical facts. Pipeline assembly: `@ SsdtDdlEmitter.foreignKeyDropDiagnostics finalState.Catalog … @ SsdtDdlEmitter.foreignKeyNameCollisionDiagnostics (DecisionOverlay.ofComposeState finalState) finalState.Catalog` (Pipeline.fs:1511-1527), immediately after `projectWithStateWithPinsAndBootstrap` (:1450-1451) whose emit fold already built them.
- **suggested carrier:** FkEmissionLookups (the buildLookups triple as a named value) threaded via *Using siblings — foreignKeyDropDiagnosticsUsing / foreignKeyNameCollisionDiagnosticsUsing — from the one binding the emit step already pays for

### S47 · fkDef resolves every deployable reference four times in one publish (CREATE TABLE inline FKs, NOCHECK alters, drop-witness diagnostics, name-collision diagnostics)
- **plane/grain/path:** derivation / per-row / publish-critical
- **fact:** The resolved ForeignKeyDef of a reference (source-column linear scan over k.Attributes, target/PK map probes, IdentifierBudget.fit over the concatenated FK name) is a pure function of the unchanged catalog+overlay, recomputed per reference at four sites in one publish flow.
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:328 — createTableStatement: `\|> List.choose (fkDef targetByKey pkAttrByKey k)` (via kindToSsdtFile :745)
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:383 — untrustedFkAlters: `match fkDef targetByKey pkAttrByKey k r with` (via kindToSsdtFile :756); third at :1035 in foreignKeyDropDiagnostics; fourth at :1128 in foreignKeyNameCollisionDiagnostics (`fkDef targetByKey pkAttrByKey k r \|> Option.map (fun fk -> k, r, fk)`)
- **evidence:** fkDef per call re-does `k.Attributes \|> List.tryFind (fun a -> a.SsKey = r.SourceAttribute)` (SsdtDdlEmitter.fs:251-253) and `IdentifierBudget.fit (System.String.Concat("FK_", …))` (:273-280). All four sites run per publish over the same catalog/overlay: kindToSsdtFile yields `createTableStatement overlay targetByKey pkAttrByKey k` then `untrustedFkAlters overlay targetByKey pkAttrByKey k` (:745,:756 — same shape on the flat-stream path :913,:918), and the diagnostics assembly (Pipeline.fs:1511, 1526-1527) resolves the same references again. The DropFk filter differs only for the drop-witness site.
- **suggested carrier:** resolvedFkByReference : Map<SsKey, ForeignKeyDef> derived once per kind (sibling of emittedIndexNames' E3 fix) and threaded via createTableStatementUsing / untrustedFkAltersUsing / the diagnostics *Using forms

### S48 · SchemaMigrationEmitter pays a whole-catalog buildLookups per individual FK via foreignKeyDefOf
- **plane/grain/path:** derivation / per-row / verb-only
- **fact:** foreignKeyDefOf rebuilds the full-catalog lookup triple (Catalog.allKinds + pkAttrByKey map) on every call, and the migration emitter calls it once per added/reshaped/removed reference inside per-kind folds — O(references × catalog) for a fact that is constant per catalog.
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:836-838 — `let foreignKeyDefOf (catalog…) = let _, targetByKey, pkAttrByKey = buildLookups catalog; fkDef …` (whole-catalog build for ONE reference)
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/SchemaMigrationEmitter.fs:238 — per added FK in a fold: `match SsdtDdlEmitter.foreignKeyDefOf targetCatalog targetKind r with`; likewise :226 (sourceFkName, called per removed key at :279 and per reshaped change at :261) and :256
- **evidence:** kindReferenceMigration folds `targetKind.References \|> List.filter … \|> List.fold (fun (stmts, refusals) r -> match SsdtDdlEmitter.foreignKeyDefOf targetCatalog targetKind r with …)` (SchemaMigrationEmitter.fs:233-247) and `rd.Removed \|> Set.toList … \|> List.choose (fun key -> sourceFkName key …)` (:276-279) where `sourceFkName` is `SsdtDdlEmitter.foreignKeyDefOf sourceCatalog sk r \|> Option.map (fun fk -> fk.Name)` (:226). Each call re-executes `buildLookups` — `Catalog.allKinds` + `pkAttrByKey … \|> Map.ofList` (SsdtDdlEmitter.fs:817-829) — over the source or target catalog. Migrate-verb path (MigrationRun), not the publish.
- **suggested carrier:** foreignKeyDefOfUsing (lookups: FkEmissionLookups) — build the source-catalog and target-catalog lookup triples once per migration and thread them through kindReferenceMigration

### S49 · DecisionOverlay.ofComposeState computed three times per publish over the same ComposeState
- **plane/grain/path:** derivation / per-publish / publish-critical
- **fact:** The overlay projection of the post-chain decision sets (nullabilityKeys/uniqueKeys/fkKeys set builds over ComposeState) is a pure function of the unchanged final ComposeState, computed once in the SSDT emit step and twice more in the diagnostics assembly of the same publish.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:565 — `let decisionOverlay = DecisionOverlay.ofComposeState ctx.ComposedState` (SSDT emit step)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1521 — `(DecisionOverlay.ofComposeState finalState) finalState.Catalog`; and again at :1527 for foreignKeyNameCollisionDiagnostics
- **evidence:** The emit fold's ctx.ComposedState IS the finalState the diagnostics receive: projectFromChainWithState returns `diagnosticsGated, composedState` (Pipeline.fs:683) and builds `EmitContext { … ComposedState = composedState … }` (:640); runWithConfigCore binds `let outputs, finalState = projectWithStateWithPinsAndBootstrap …` (:1450-1451) then calls ofComposeState twice in the diagnostics list (:1520-1521, :1526-1527). ofComposeState builds three key-sets per call (DecisionOverlay.fs:94-98).
- **suggested carrier:** a single `decisionOverlay` binding in runWithConfigCore (or an EmitContext.DecisionOverlay field) shared by the emit step and both diagnostics calls

### S50 · runWithConfigAndLoad re-acquires the estate and re-runs the whole compose+render for the load leg
- **plane/grain/path:** acquisition / per-publish / publish-critical
- **fact:** The hydrated model (catalog + full-estate static/bootstrap row streams + migration context), the pass chain, and every kind's rendered MERGE text are produced by runWithConfig, then produced a second time by emittedSeedPlan for the same publish: readAndHydrateConfigModel runs again (a second full model read — live OSSYS snapshot or file parse — plus a second full-estate hydration over the wire), projectWithState re-runs the entire chain, and composeRenderedLeveledWithBootstrap re-renders every kind's Phase1/Phase2 text.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2357 — runWithConfigAndLoad first runs the full publish (`let! reportResult = runWithConfig cfg`, whose extract stage at Pipeline.fs:1827 does `let! parsed = readAndHydrateConfigModel cfg` and whose emit stage renders the data bundle at Pipeline.fs:803)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2334→1944 — loadFromConfig calls emittedSeedPlan, which does `let! parsed = readAndHydrateConfigModel cfg` AGAIN, then Pipeline.fs:1915 `projectWithState policy Profile.empty folders groups catalog` re-runs the chain and Pipeline.fs:1925 `composeRenderedLeveledWithBootstrap` re-renders every kind
- **evidence:** First: `let! reportResult = runWithConfig cfg` (2357) → `let! parsed = readAndHydrateConfigModel cfg` (1827). Second: `let! seedR = emittedSeedPlan cfg` (2334) → `let! parsed = readAndHydrateConfigModel cfg` (1944); `let _, finalState = projectWithState policy Profile.empty folders groups catalog` (1914-1915); `DataComposer.composeRenderedLeveledWithBootstrap policy finalState.Catalog …` (1925). NOTE doc adjacency: PERF_OPPORTUNITIES Sweep-2 item 5 names only the store-leg's TOPO recompute as remaining; the model re-read / re-hydration / re-render is unadjudicated.
- **suggested carrier:** loadFromConfigUsing (catalog, bootstrapRows, migration, finalState) — thread the publish's extract triple + ComposeState into the load leg instead of cfg

### S51 · LifecycleStore.load fully deserializes the same lifecycle file four times per store leg
- **plane/grain/path:** acquisition / per-publish / publish-critical
- **fact:** One runStoreLeg invocation loads and fully parses the SAME durable lifecycle store four times — each load is File.ReadAllText + JsonDocument.Parse + CatalogCodec.deserialize of EVERY episode's full catalog snapshot + per-edge monotone append verification — because priorSchemaFromStore, priorAccumulatedRefactorLog, nextStoreCoordinate, and recordEpisode each call LifecycleStore.load path independently.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1957 — priorSchemaFromStore: `match LifecycleStore.load path with` (called from runStoreLeg at Pipeline.fs:2073)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1977, 2006, 2029 — the same `LifecycleStore.load path` in priorAccumulatedRefactorLog (runStoreLeg:2077), nextStoreCoordinate (:2085), and recordEpisode (:2097), all in the same flow over the same file
- **evidence:** Load cost: LifecycleStore.fs:364 `Ok (System.IO.File.ReadAllText path)`, :370 `use doc = JsonDocument.Parse text`, :313 `CatalogCodec.deserialize (schemaEl.GetRawText())` per episode, :348 `EpisodicLifecycle.append ep lc` per edge. Four call sites: `match LifecycleStore.load path with` at Pipeline.fs:1957, 1977, 2006, 2029.
- **suggested carrier:** runStoreLegUsing (chain: EpisodicLifecycle) — one loaded chain threaded through the four consumers (recordEpisode appends to it rather than reloading)

### S52 · Full Outputs (SSDT ScriptDom render, manifest, JSON, snapshot, fidelity) built eagerly then discarded by state-only consumers
- **plane/grain/path:** materialization / per-publish / publish-critical
- **fact:** projectFromChainWithState unconditionally folds all emitSteps — per-kind ScriptDom SSDT rendering, ManifestEmitter.buildFull, JSON + Distributions emitters, CatalogCodec.serialize of the whole catalog, and the fidelity report with its per-cell static-row residual walk — but two consumers (projectSeedPlan on the export+load leg, applyShapingToCatalog on migrate/preview) bind the Outputs to `_` and consume only finalState.Catalog: the entire artifact set is materialized and dropped.
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:659-661 — the eager build: `emitSteps \|> List.fold (fun acc step -> step.Emit emitContext acc) (seedOutputs emitContext)` (seedOutputs also pays `CatalogSnapshot = CatalogCodec.serialize ctx.EmittedCatalog` (553), `Manifest = ManifestEmitter.build` (554), `Fidelity = fidelityOf ctx` (557))
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1914-1915 — projectSeedPlan: `let _, finalState = projectWithState policy Profile.empty folders groups catalog` (Outputs discarded); same shape at Pipeline.fs:1800-1802 in applyShapingToCatalog: `let _, finalState = projectWithStateWithPins …; Result.success finalState.Catalog`
- **evidence:** Build: `List.fold (fun acc step -> step.Emit emitContext acc) (seedOutputs emitContext)` (659-661). Discard: `let _, finalState = projectWithState policy Profile.empty folders groups catalog` (1914-1915) and `let _, finalState = projectWithStateWithPins pins policy Profile.empty folders groups renamedCatalog` … `Result.success finalState.Catalog` (1800-1802). The NM-02 comment (662-668) confirms every EmitStep always runs.
- **suggested carrier:** composeStateOnly (chain runner returning ComposeState/emittedCatalog without the emitSteps fold) — the registered⇔executed invariant scoped to artifact-producing runs

### S53 · The per-edge schema diff chain is computed twice per store leg, and the prior schema is re-derived by the FTC fold although each episode stores the full snapshot
- **plane/grain/path:** derivation / per-publish / publish-critical
- **fact:** Within one runStoreLeg, the O(episodes) chain of full-catalog CatalogDiff.between computations is paid twice — once inside reconstructLatestSchema (priorSchemaFromStore) and again in schemaEvolutionChain (priorAccumulatedRefactorLog) — and reconstructLatestSchema itself re-derives state A via the diff+apply fold even though its own docstring says recovery reads the stored latest snapshot directly (the fold is a verification property, not the recovery path).
- **first receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1960 — priorSchemaFromStore: `EpisodicLifecycle.reconstructLatestSchema chain`, which runs Episode.fs:198-200 `List.pairwise \|> List.map (fun (prior, next) -> CatalogDiff.between prior.Schema next.Schema)` then folds applyDiff (Episode.fs:218)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1980 — priorAccumulatedRefactorLog in the same runStoreLeg: `EpisodicLifecycle.schemaEvolutionChain chain` recomputes the identical pairwise diff list
- **evidence:** Episode.fs:209-211 docstring: 'each episode carries FULL state, so recovery reads the stored latest snapshot directly; the FTC fold earns its keep by AGREEING with it' — yet priorSchemaFromStore uses the fold as recovery and never compares. Both sites: `reconstructLatestSchema chain` (Pipeline.fs:1960) and `schemaEvolutionChain chain` (Pipeline.fs:1980), same loaded chain content.
- **suggested carrier:** edgeDiffs : CatalogDiff list threaded once (schemaEvolutionChainUsing), with prior = (List.last episodes).Schema for the recovery read

### S54 · moduleSchemaPropertyStatements re-filters and re-sorts the whole module's kind list for every kind
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** The per-(module, schema)-constant fact 'which kind is alphabetically first in this schema' is recomputed per kind: kindToSsdtFile runs for every kind of the catalog and each invocation filters m.Kinds by schema and sorts the filtered list — O(K² log K) per module per publish — to make a decision that is identical for every kind of the same schema.
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:715-719 — `m.Kinds \|> List.filter (fun candidate -> candidate.Physical.Schema = schema) \|> List.sortBy (fun candidate -> candidate.SsKey) \|> List.tryHead` computed for the first kind of a module
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/SsdtDdlEmitter.fs:969-972 → :744 — the per-kind driver: emitSlicesWithRendering maps every catalog kind through kindToSsdtFile, whose statement seq does `yield! moduleSchemaPropertyStatements m k`, re-running the same filter+sort for every other kind of the module
- **evidence:** `let firstKindOfSchema = m.Kinds \|> List.filter … \|> List.sortBy … \|> List.tryHead` (715-719) inside a function invoked per kind: `ArtifactByKind.perKindBenched "emit.ssdt.emitSlices.kind" catalog (fun k -> … kindToSsdtFile … m k …)` (969-972) with `yield! moduleSchemaPropertyStatements m k` (744).
- **suggested carrier:** firstKindBySchema : Map<SchemaName, SsKey> derived once per module and threaded into kindToSsdtFile (the emittedNamesForKind pattern)

### S55 · unionSiblings materializes the full union artifact on the bundle path where only the partition-law gate is consumed
- **plane/grain/path:** materialization / per-publish / publish-critical
- **fact:** On the per-lane bundle path the union ArtifactByKind is built in full — a per-kind coverage list, an accumulated rows list, List.rev, Map.ofList over all kinds, then ArtifactByKind.create's two keyset Sets and two Set.differences — and immediately discarded (`Ok _union`): the only information consumed is the boolean overlap verdict, which needs none of the map/keyset materialization.
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/DataEmissionComposer.fs:193-197 — `match List.fold folder (Ok []) allKinds with … let slices = rows \|> List.rev \|> Map.ofList; ArtifactByKind.create catalog slices` (plus per-kind `coverageOf` list allocs at 165-172)
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/DataEmissionComposer.fs:458 (and :403) — `\| Ok _union ->` with the comment '`_union` exists for `unionSiblings`' partition-law assertion only'; the artifact is dropped and the lanes are rendered from siblings directly
- **evidence:** `let slices = rows \|> List.rev \|> Map.ofList` + `ArtifactByKind.create catalog slices` (196-197) vs `\| Ok _union -> // _union exists for unionSiblings' partition-law assertion only` (458-462). BORDERLINE: Sweep-2 recorded skipping only the union's RENDER ('only the union's RENDER is skipped'); the map+keyset materialization behind the gate is the residual.
- **suggested carrier:** assertPartition : Catalog -> SiblingArtifacts -> Result<unit, EmitError> (the gate as its own weak projection; unionSiblings keeps the map for callers that consume it)

### S56 · ArtifactByKind.create rebuilds the catalog's SsKey keyset Set on every construction, including re-validation of a key-preserving rewrite
- **plane/grain/path:** derivation / per-publish / publish-critical
- **fact:** Every ArtifactByKind.create call rebuilds `required` (Catalog.allKinds → List.map → Set.ofList) and `provided` (Map → Set) for the same catalog — paid 4+ times per publish (three sibling emitters' perKind creates + unionSiblings' create) — and applyEmissionFolderOverrides pays it once more to re-validate a Map.map that preserved keys by construction (its own comment says the failure is unreachable).
- **first receipt:** sidecar/projection/src/Projection.Core/ArtifactByKind.fs:83-88 — `let required = Catalog.allKinds catalog \|> List.map (fun k -> k.SsKey) \|> Set.ofList; let provided = slices \|> Map.toSeq \|> Seq.map fst \|> Set.ofSeq` (first paid by the first sibling emitter's perKind create in dispatchSiblings, DataEmissionComposer.fs:122/129/133)
- **second receipt:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:343 — applyEmissionFolderOverrides: `match ArtifactByKind.create catalog rewritten with` re-deriving the identical keyset for a rewrite whose comment (346-349) states 'Unreachable: we Map.map preserving keys; the input keyset equals Catalog.allKinds by construction'
- **evidence:** `let required = Catalog.allKinds catalog \|> List.map (fun k -> k.SsKey) \|> Set.ofList` (ArtifactByKind.fs:84-86) executed per create; sibling creates at DataEmissionComposer.fs:122-133 + unionSiblings:197 in one compose; re-validation at Pipeline.fs:343 with the 'Unreachable' comment. Catalog.kindIndex shows the cached-per-catalog precedent (Catalog.fs:1785-1792); the keyset Set has no such cache.
- **suggested carrier:** Catalog.kindKeySet (ConditionalWeakTable-cached keyset like kindIndex), or a mapValues on ArtifactByKind that preserves the proven keyset without re-validation

### S57 · renderUpdate recomputes per-kind-constant setCells/whereCells attribute projections on every row
- **plane/grain/path:** derivation / per-row / publish-critical
- **fact:** The deferred-attribute list (`k.Attributes` filtered by `deferred`) and the row-identity match attributes (`KindColumns.matchAttributes deferred k`) are pure functions of the per-kind-constant pair (k, deferred), yet both are recomputed — two full attribute-list walks — for every row in the Phase-2 loop.
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/StagedMerge.fs:169-170 — the staged sibling computes the same projections ONCE per kind before touching rows: `let pkAttrs = KindColumns.matchAttributes deferred k / let deferredAttrs = k.Attributes \|> List.filter (fun a -> Set.contains a.Name deferred)` (proving loop-invariance)
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:140-143 and 150-152 — inside per-row `renderUpdate`: `k.Attributes \|> List.filter (fun a -> Set.contains a.Name deferred)` and `KindColumns.matchAttributes deferred k`, re-run for every row of the loop at StaticSeedsEmitter.fs:176-177 / MigrationDependenciesEmitter.fs:235-236
- **evidence:** MergeRender.fs:141-142 `k.Attributes \|> List.filter (fun a -> Set.contains a.Name deferred) \|> List.map cellOf`; MergeRender.fs:150-151 `KindColumns.matchAttributes deferred k \|> List.map cellOf`; KindColumns.fs:85-91 shows matchAttributes is a pure filter of (deferred, k). For a 20k-row kind with 30 attributes that is 2 x 20k x O(30) filter walks plus per-row list allocations for data that never changes across the loop.
- **suggested carrier:** Phase2UpdateShape — per-kind (setAttrs x whereAttrs) bindings hoisted before the row loop and threaded via a renderUpdateUsing form

### S58 · SchemaMigrationEmitter.foldByKind re-copies the accumulated statement list on every kind (O(N^2) @-append fold)
- **plane/grain/path:** materialization / per-kind / verb-only
- **fact:** The already-accumulated migration statements and diagnostic entries: at each fold step the whole accumulated prefix is rebuilt via `accStmts @ stmts, accEntries @ entries`, so statements produced for kind 1 are re-materialized (list-copied) once for every subsequent kind — the exact `xs @ [x]` fold pattern the codebase's own audits retired elsewhere.
- **first receipt:** sidecar/projection/src/Projection.Targets.SSDT/SchemaMigrationEmitter.fs:423 — `let stmts, entries = f kindKey d` produces each kind's statements once
- **second receipt:** sidecar/projection/src/Projection.Targets.SSDT/SchemaMigrationEmitter.fs:424 — `accStmts @ stmts, accEntries @ entries` re-copies the entire accumulated prefix at every subsequent kind (inside List.fold at 421-425), for three folds (attrs 431, indexes 432, references 433)
- **evidence:** SchemaMigrationEmitter.fs:421-425 `List.fold (fun (accStmts, accEntries) (kindKey, d) -> ... accStmts @ stmts, accEntries @ entries) ([], [])`. The codebase names this pattern as a fixed Tier-1 defect elsewhere: Result.fs:243-244 "pre-sized accumulators (ResizeArray) avoid the prior O(N^2) `xs @ [x]` pattern"; OssysJsonReader.fs:378 "Retires the O(N^2) `xs @ [x]` fold pattern"; PolicyDiff.fs:91. A 1000-kind diff with a few statements per kind pays ~N^2/2 cons-cell copies per fold.
- **suggested carrier:** foldByKind via List.collect (or the Result.aggregate ResizeArray-accumulator precedent), pairing one collected Statement list with one collected entries list

### S59 · typedRows row count (and the staging verdict derived from it) recomputed via O(n) walks up to four times per kind
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** The row count of the per-kind typedRows list — an F# list whose Length is O(n) — is recomputed by separate linear walks: once for the rows telemetry sample, once for the Phase-1 shouldStage decision, once for shouldIndex, and again by the caller for the Phase-2 shouldStage decision over the same unchanged list.
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:42 — `Bench.recordSample ... (int64 typedRows.Length)` (first O(n) walk)
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:86 and 89 — `DataStagingPolicy.shouldStage staging (List.length typedRows)` / `DataStagingPolicy.shouldIndex staging (List.length typedRows)`; plus the caller re-pays it: StaticSeedsEmitter.fs:163 / MigrationDependenciesEmitter.fs:226 `elif DataStagingPolicy.shouldStage staging (List.length typedRows) then`
- **evidence:** MergeRender.fs:42 `int64 typedRows.Length`; MergeRender.fs:86 `if DataStagingPolicy.shouldStage staging (List.length typedRows)`; MergeRender.fs:89 `(DataStagingPolicy.shouldIndex staging (List.length typedRows))`; StaticSeedsEmitter.fs:163 `elif DataStagingPolicy.shouldStage staging (List.length typedRows) then`. For a 30k-row kind that is up to 120k unnecessary cons-cell hops per kind, and the Phase-1/Phase-2 staging verdicts are the same fact computed at two sites.
- **suggested carrier:** rowCount — one int binding computed at scriptOfTyped entry, threaded into renderMerge and the Phase-2 branch

### S60 · attachDefaults/attachComputed recompute the kind's qualifiedParts inside the per-attribute lambda
- **plane/grain/path:** derivation / per-cell / verb-only
- **fact:** `TableId.qualifiedParts k.Physical` — a per-kind constant (schema, table) tuple — is recomputed (with a fresh tuple allocation) for every attribute of every kind in attachDefaults and attachComputed, while the sibling attachAnnotations computes it once per kind outside its loops.
- **first receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1394 — attachAnnotations hoists it: `let schema, table = TableId.qualifiedParts k.Physical` once per kind (proving loop-invariance)
- **second receipt:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1345 and 1370 — `let schemaStr, tableStr = TableId.qualifiedParts k.Physical` INSIDE `k.Attributes \|> List.map (fun a -> ...)`, re-paid per attribute
- **evidence:** ReadSide.fs:1342-1346 `k.Attributes \|> List.map (fun a -> let schemaStr, tableStr = TableId.qualifiedParts k.Physical ...)`; identical shape at 1367-1371; Coordinates.fs:322 `let qualifiedParts (t: TableId) : string * string = (schemaText t, tableText t)` allocates a tuple per call. Runs for every column of every table on every readback.
- **suggested carrier:** hoisted qualifiedParts binding — `let schemaStr, tableStr = TableId.qualifiedParts k.Physical` above the attribute List.map (the attachAnnotations form)

### S61 · typedRows values-only projection (List.map snd) built twice per staged cyclic kind
- **plane/grain/path:** derivation / per-kind / publish-critical
- **fact:** The values-only row list `typedRows \|> List.map snd` over the unchanged per-kind typedRows is materialized twice in one flow for a large cyclic kind: once for the Phase-1 renderMerge argument and again for the Phase-2 renderStagedPhase2 argument — two O(rows) list spines of the same projection.
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:160 — `MergeRender.renderMerge "emit.staticSeeds" ... kind (typedRows \|> List.map snd)` (first projection)
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:174 — `StagedMerge.renderStagedPhase2 "emit.staticSeeds" cdcAware table kind deferred (typedRows \|> List.map snd)` (same projection rebuilt; both branches execute for a deferred-FK kind above the staging threshold); mirror at MigrationDependenciesEmitter.fs:223 and 233
- **evidence:** StaticSeedsEmitter.fs:160 `(typedRows \|> List.map snd)` and 174 `(typedRows \|> List.map snd)`; MigrationDependenciesEmitter.fs:223/233 identical. For a 30k-row staged cyclic kind, a second 30k-node list spine is allocated for a projection already in hand.
- **suggested carrier:** valueRows — one `typedRows \|> List.map snd` binding shared by the Phase-1 and Phase-2 branches


## Appendix B — the 38 killed findings with refutation cores

### K00 · ReadSide.read pays 7 additional sequential round-trips (defaults/computed/triggers/checks/extProps/indexes/sequences) outside the combined batch built to eliminate exactly that cost
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1906-1921 — `let! defaults = readDefaultConstraints cnn` / `readComputedColumns` / `readTriggers` / `readCheckConstraints` / `readExtendedProperties` / `readIndexes` / `readSequences` — seven more sequential round-trips in the same `read`.
- **refutation:** Refuted on VALUE IDENTITY: the two receipts are not for the same fact. readSchemaCombined (ReadSide.fs:1520-1591) acquires columns/PKs/identity/FKs/LogicalName+SsKey extended properties; the seven subsequent reads (1906-1921) acquire genuinely different facets — default constraints, computed columns, triggers, check constraints, annotation extended properties, indexes, sequences — each acquired exactly once per read. Even the closest-looking overlap is disjoint by construction: readExtendedProperties carries `ep.name NOT IN (N'Projection.LogicalName', N'Projection.SsKey', N'V2.LogicalName', N'V2.SsKey')` (ReadSide.fs:540), the precise complement of the batch's two extended-property result sets. Re-joining sys.tables in each query re-executes a join, not a re-acquisition of an already-held value. This is a round-trip batching/latency opportunity (fold more result sets into the one command), not a pay-twice-for-one-fact defect, so it fails the pay-once audit's value-identity test.

### K01 · sys.extended_properties is scanned three times per readback: LogicalName batch, SsKey batch, then readExtendedProperties' NOT-IN scan
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:532-540 (invoked at :1916) — `FROM sys.extended_properties ep JOIN sys.tables t ... WHERE ep.class = 1 ... AND ep.name NOT IN (N'Projection.LogicalName', N'Projection.SsKey', N'V2.LogicalName', N'V2.SsKey')`.
- **refutation:** VALUE IDENTITY fails: the three predicates (IN LogicalName/V2.LogicalName, IN SsKey/V2.SsKey, NOT IN all four — ReadSide.fs:1580, :1590, :540) are a complete disjoint partition, so no row and no value is ever acquired twice; each scan yields genuinely different information (name-binding recovery for buildKind/buildAttribute, SsKey identity recovery, and annotation payloads for attachAnnotations). The only shared payment is the server-side walk of a tiny catalog relation (extended properties on user tables), which sits alongside seven other separate per-read round-trips (defaults, computed, triggers, checks, indexes, sequences) in the same ReadSide.read; fusing it in would churn the readSchemaCombined return contract (already an 8-tuple, ReadSide.fs:1521-1528) for a trivially small saving.

### K02 · Publish acquires deployed column nullability twice: the model read's #ColumnReality (sys.columns) and the profiler's INFORMATION_SCHEMA.COLUMNS reflection
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Adapters.Sql/LiveProfiler.fs:142-145 — `batchedNullabilityReflectionSql = "SELECT TABLE_SCHEMA, TABLE_NAME, COLUMN_NAME, IS_NULLABLE FROM INFORMATION_SCHEMA.COLUMNS ..."`, run per publish via captureEvidenceCacheDerived (:536) from Pipeline.fs:1356 attachDerived.
- **refutation:** Not the same fact and the second form is load-bearing. Scope: #ColumnReality (rowsets SQL :415-423) covers only OSSYS-modeled attributes whose physical column matched the COALESCE(PhysicalColumnName, DatabaseColumnName, AttrName) collation join — a join miss silently drops the column, and nullabilitySliceOf defaults absent keys to NOT-NULL-reflected, exactly the false-tightening hazard LiveProfiler.fs:137-141 documents; the catalog-wide INFORMATION_SCHEMA reflection has no such coverage hole. Substrate: the two reads ride separately configured channels (cfg.Model.Ossys vs the D9 out-of-band PROJECTION_SOURCE_CONNECTION_STRING env var, Pipeline.fs:1387-1409) that nothing enforces to be the same database. Provider-independence: the profiler must also serve file-sourced/non-OSSYS catalogs where no snapshot exists, so the reflection is the profile stage's own guaranteed acquisition; the claim's own carrier concedes the live reflection must be retained as fallback, at which point the one global query is still paid whenever any coverage gap exists.

### K03 · CDC measure bracket re-discovers the tracked-table set and pays one COUNT round-trip per capture table, twice per load
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2245-2253 — `match! cdcCaptureTotal sink with ... Ok baseline -> ... do! loading ... match! cdcCaptureTotal sink with` — the identical discovery + loop paid again post-load (same bracket at MigrationRun.fs:722/729).
- **refutation:** The dominant payment is not a duplicate: baseline vs post capture counts are DIFFERENT values by design (the load changes them — this is the deliberate before/after measure of the data norm), and each call must also re-run sp_cdc_scan so post-load captures land (Deploy.fs:249); the per-table COUNTs within one call are N distinct facts, so batching them is a round-trip-shape optimization, not a duplicated receipt. The only true duplicate — the tracked-set discovery in the data-load bracket — is one trivial sys.tables probe, and the fix churns a contract: cdcCaptureTotal is the one injected sink (SqlConnection -> Task<Result<int>>) threaded through loadSeedAndRecord/loadMeasureAndRecord (Pipeline.fs:2232-2274) AND the migrate bracket (MigrationRun.fs:722/729), where the bracketed action is DDL (executeWith runs renames/ALTERs, and can drop/create tables), so the tracked set CAN change between calls there — re-discovery is load-bearing at that shared site, refuting the claim's 'tracked set unchanged between them' premise for the verb as deployed.

### K04 · OSSYS snapshot parses rowset #PhysColsPresent and several #ColumnReality/#FkReality columns that no V2 consumer reads
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Adapters.OssysSql/MetadataSnapshotRunner.fs:1101-1362 — toBundle never references snapshot.PhysColsPresent, cr.DefaultDefinition, fk.DeleteAction or fk.Referenced* (its FkReality join reads only `fk.UpdateAction` and `fk.IsNoCheck`, :1254-1260).
- **refutation:** Zero-consumer status confirmed, but every cited column is a recorded, governed deferral whose retirement would churn contracts for a trivially small cost. PhysColsPresent carries a Skip-stub with a named promotion trigger (matrix row 14, OssysRowsetParityInventoryTests.fs:27: orphan-attribute detection on the OSSYS source); DefaultDefinition 'stays un-lifted per matrix row 53's named trigger' as a deliberate authored-channel-over-reflected-channel decision, documented in OssysRowsetReader.fs:98-103 and pinned by ColumnRealitySourceSidePopulationTests (~:256); FkReality's DeleteAction/Referenced* are the matrix rows 58/59 evidence carriers (DeleteRuleCode deliberately rides the authored attr.DeleteRule), with UpdateAction/IsNoCheck already cashed out from the same row type. The runtime cost is a few readStringOpt/readInt calls per row — the wire cost is fixed anyway because the donor rowsets SQL (a V1 carbon-copy under the ADMIRE donor discipline) produces the result sets regardless — and dropping the fields churns the MetadataSnapshot record, the mapper shapes, and the recorded parity-matrix deferrals with their trigger-bearing Skip tests.

### K05 · Migration verify read-back lifts and hashes every row of the database while the verification verdict explicitly ignores the row axis
- **plane:** materialization  ·  **site:** sidecar/projection/src/Projection.Pipeline/MigrationRun.fs:672 — 'Verified = PhysicalSchema.isSchemaEqual sdiff' (PhysicalSchema.fs:828: 'ignoring row data (Missing/ExtraRows)') — the weaker schema-only projection is all that is consumed
- **refutation:** CONSUMER NEED succeeds: the row axis the claim calls dead IS consumed on the paths the claim sweeps in. The claim asserts compensateToSource ':492/:513 likewise' feeds nothing beyond isSchemaEqual — but its row-bearing residual diff is returned in PartialWriteUnrecovered and rendered WHOLE to the operator via PhysicalSchema.renderDiff at Migrate.fs:224-232, including the Missing/Extra-rows blocks (PhysicalSchema.fs:948-949), where row-hash divergence (rows are hashed under schema.table, so a residual rename or partial data mutation surfaces there) is exactly the 'exact divergence from A named for the operator' the M21 verdict promises. On the canary stage itself, the row-bearing outputs are a consumed contract: the integration canaries render result.SchemaDiff whole on verification failure (MigrationCanaryTests.fs:190, :278, :649, :1413) and dozens of round-trip tests consume MigrationOutcome.Reconstructed, so the suggested readSchemaOnly + ofCatalogSchemaOnly would churn the MigrationOutcome contract (Reconstructed/SchemaDiff both row-bearing) across the verification surface. With a genuine operator-facing consumer on one cited leg and contract churn on the other, the claim is refuted (and per instructions, residual uncertainty defaults to refuted).

### K06 · Surrogate-capture staging (and keymap) temp tables are re-created and re-dropped for every 50k-row chunk though their shape is constant per kind
- **plane:** materialization  ·  **site:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:243 — 'return! captureChunks sink kind … rest' recurses per chunk, re-entering captureChunk which re-creates/drops the identical staging for every subsequent chunk (streaming sibling: writeChunk per chunk at TransferRun.fs:1361)
- **refutation:** The facts check out (SurrogateCapture.fs:262/269 createStaging/dropStaging inside captureChunk; keymap create/drop per chunk at 179/196-199; TransferRun.fs:216-244 recurses per chunk), but it refutes on the consumer-need lens's triviality clause. (a) Cost is trivially small relative to the same chunk's paid work: two metadata statements (SELECT TOP 0 INTO a #temp; DROP TABLE IF EXISTS) per chunk that also pays a 50k-row bulk load plus a 50k-row MERGE...OUTPUT — milliseconds against seconds, per chunk. (b) The fix churns a load-bearing contract: captureChunk's documented semantics are 'Realize one chunk on ONE named rung' — hermetic per attempt. captureChunkDescending retries the SAME chunk on the next rung when the sink refuses (error 334 fires at MERGE time, AFTER the staging was created and bulk-loaded), and the try/finally dropStaging is exactly what guarantees the clean slate the retry's re-create (and the descent to RowwiseScopeIdentity, and abandonment on a data error) needs. Descent is not first-chunk-only — the lane is sticky but any chunk can descend — so a per-kind KindStagingSession with TRUNCATE-between-chunks must re-implement cleanup across the descent catch path and across kind boundaries on the shared session connection, churning SurrogateCapture's public captureChunk/captureChunkDescending surface for a per-chunk saving that is noise beside the chunk's bulk+MERGE.

### K07 · Phase-2 FK re-point recomputes the ENTIRE phase-1 remap (all non-deferred FK columns, all rows) only to keep the deferred columns
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:361 — phase 2: 'let remapped2 = repoint Set.empty kind load.Rows' re-pays the non-deferred remap; :365 filters skips to deferred, :379 'phase2UpdateSql kind load.DeferredFkColumns' renders only deferred columns (streaming sibling: TransferRun.fs:1475 'repoint basis Set.empty kind chunkRaw')
- **refutation:** Refuted on the consumer-need lens: the recomputed non-deferred results are NOT all discarded — two outputs of the full 'repoint Set.empty' are consumed. (1) The kept-row set: remapRowFksWith (SurrogateRemap.fs:291-319) DROPS a row on any unresolved non-NULL FK, so remapped2.Rows reproduces exactly the phase-1 insert set; rowsForUpdate is built from it (TransferRun.fs:367-378, streaming :1480-1502), gating which rows get phase-2 UPDATEs — the non-deferred lookups are what compute that gate. (2) The remapped cells feed the UPDATE's WHERE: phase2UpdateSql/Quantum (TransferCellShaping.fs:87-127) takes WhereCells from ALL PK attributes of the remapped row; for a non-AssignedBySink cycle member whose PK is itself a non-deferred FK to an AssignedBySink kind (the OutSystems extension-entity PK=FK pattern — nothing in Kind.References or DataLoadPlan excludes it; deferredFkColumns come from cycle membership regardless of disposition, DataLoadPlan.fs:119), phase 1 inserted the row under the REMAPPED PK, and only the full recompute supplies that remapped WHERE key — a repoint restricted to load.DeferredFkColumns would render UPDATEs keyed on the stale source PK, matching zero rows and silently losing the deferred re-point. So the second computation is load-bearing for phase 2's row set and WHERE keys, not a pure recompute-and-discard; and the genuinely redundant portion (non-deferred, non-PK cell values) is dictionary lookups dwarfed by the per-row UPDATE rendering and execution in the same loop.

### K08 · The CDC tracked-table set is re-queried up to three times within one measured migrate/transfer verb (baseline bracket, safety gate, post bracket)
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Pipeline/MigrationRun.fs:921 — 'match! Deploy.cdcCaptureTotal sink' post-bracket re-pays the same tracked-tables probe (and MigrationRun.fs:533's safetyGates 'ReadSide.cdcTrackedTables cnn' is the interleaved third payment)
- **refutation:** The call sites are as cited (Deploy.fs:244 inside cdcCaptureTotal; MigrationRun.fs:722/729 brackets, :533 gate, :908/921; Deploy.fs:1370/1374), but it refutes on the consumer-need lens. Each read serves a distinct, deliberately-fresh purpose: (a) the safetyGates read (MigrationRun.fs:529-539) is the NM-54 refusal gate — 'unverifiable CDC state is UNSAFE' — that must render its own live verdict immediately before DDL inside executeWith, which is a standalone public verb (plain execute has no measurement leg to borrow a list from; threading one in churns executeWith's signature for all callers); (b) the baseline/post reads are inside Deploy.cdcCaptureTotal, whose documented contract is a self-contained live change-measure primitive ('Callers bracket an action with this') — between baseline and post real DDL runs, and on the allowCdc path (gate skipped) that DDL can drop or rename a tracked table, changing the tracked set, so the post-bracket re-discovery is part of the honest measure (a threaded stale list would COUNT(*) a dropped CT table and fail, or misdefine the norm's domain), exactly the NM-54 never-fabricate posture. The residual saving is one or two sys.tables catalog-view queries (milliseconds) inside verbs that execute DDL, sp_cdc_scan, and CT-table scans — trivially small — and cdcCaptureTotalOn would churn the cdcCaptureTotal measure contract consumed across Deploy canaries, MigrationRun, and tests.

### K09 · Streaming DryRun preview issues one COUNT_BIG round-trip per kind — N per-item queries batchable into one
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1684-1691 — 'for l in plan.Loads do … return! countKindRows source srcKind' re-pays the round-trip for each subsequent kind in the same preview
- **refutation:** Refuted on the value-identity lens: the N queries are receipts for N DIFFERENT facts — each countKindRows (TransferRun.fs:1233-1241) counts a different table's rows, so the 'first receipt' and 'second receipt' (:1684-1691, each subsequent kind in the loop) share no substrate row, no input value, and no semantic content; nothing is paid twice — this is a round-trip-amortization suggestion, not a duplicate acquisition, and 'different rows, different scope' is the lens's explicit refutation trigger. Secondarily on the consumer lens: the aggregation work (scanning each table's narrowest index) is identical batched or not — only network round-trips are saved — in a display-only DryRun preview (the docstring at :1227-1230 already names the shape a deliberate 'cheap exact count, one round-trip per kind, no row scan'), and the proposed sys.dm_db_partition_stats carrier would churn that exactness contract (partition stats are approximate under concurrent activity) for an operator-preview estimate whose serial round-trip cost is trivially small.

### K10 · trustedFksOnLoadedTables fetches EVERY enabled+trusted FK in the sink database and filters to the loaded tables client-side
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Pipeline/TransferFkTrust.fs:51-52 — 'Seq.filter (fun (sch, tbl, _) -> Set.contains (TableId.normalizedKeyOf sch tbl) loaded)' discards the fetched non-loaded FKs
- **refutation:** No fact is paid twice: there is exactly ONE acquisition per transfer verb (single call site, TransferRun.fs:806 — the claim's 'per-kind' grain is wrong) and the Seq.filter is the pure derivation from it, the sanctioned discover-once/derive-pure shape. The over-fetch is metadata-sized (3 catalog-name strings per sys.foreign_keys row of one database), trivially small against the bulk load the verb brackets. Fixing it churns the identity seam: the client filter reuses TableId.normalizedKeyOf — the exact ruler that built loadedTableKeys, so membership agrees by construction — while a parameterized-IN/temp-table pushdown makes the sink's collation a second normalization authority for schema.table identity (and hits the 2100-parameter limit on wide plans).

### K11 · Transfer bulk lane round-trips every typed cell through a raw string: source reader formats CLR->string, Bulk immediately parses string->CLR for SqlBulkCopy
- **plane:** representation  ·  **site:** sidecar/projection/src/Projection.Pipeline/Bulk.fs:56 — Bulk.parseRaw parses the same string back to the identical CLR type for SqlBulkCopy (reached via TransferRun.fs:1368 quantumCellRows -> Bulk.copyRows)
- **refutation:** Refuted on consumer need: the raw-string quantum is the stated, load-bearing IR contract, not a gratuitous hop. Bulk.fs:14-20 names the SQL → IR-raw → SQL round-trip as the codec's law with RawValueCodec as the single source of truth ('V2's canonical raw-value format contract' per DECISIONS), and genuine consumers sit BETWEEN the format and the parse on the same stream: repoint's copy-on-write string-cell rewrites and PackedSurrogateRemap capture/lookup raw keys (TransferRun.fs:1294-1313, 1365), the CaptureJournal's persisted FirstPk/LastPk/Pairs with an explicit byte-compat resume guarantee across carrier changes (TransferRun.fs:1320-1322), SqlLiteral.ofRaw phase-2 rendering, captureCells staging, and the canary's quantum hashing. A TypedRowQuantum sibling churns the RowQuantum [<Struct>] carrier (a gated promotion landed 2026-06-12) and the journal's durable byte contract — exactly the contract-redesign shape the declined ledger routes to its own designed slice.

### K12 · ReadSide.read issues a COUNT(*) probe per kind, N scalar round-trips batchable into one metadata query
- **plane:** acquisition  ·  **site:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1939-1940 — `for k in kindsWithRefs do let! rowsOpt = readRows cnn k maxRows` re-pays the probe round-trip for every kind in the catalog
- **refutation:** Refuted on value identity: the 'second receipt' (ReadSide.fs:1939-1940) is literally the loop that invokes the first (fs:1140-1143) — each kind's row count is a DISTINCT fact paid exactly once; nothing is paid twice. The suggested single partition-stats query is a different semantic value (approximate, allocation-based, not transactionally adjacent — the claim itself concedes it 'changes the gate's precision'), so the substitute fails the same-fact test; and the exact COUNT(*) taken immediately before its own drain on the same connection is the ≤100k materialization gate's staleness boundary — a batched up-front count could be stale by drain time. This is an N-facts-N-payments batching wish, not a pay-once violation.

### K13 · MergeRender.renderUpdate re-derives per-kind constants (TableId, deferred/match attribute filters) once per ROW on the publish Phase-2 lane
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Targets.Data/StaticSeedsEmitter.fs:176-177 — `typedRows \|> Bench.iterMap "emit.staticSeeds.phase2Row" (fun (_, vs) -> MergeRender.renderUpdate ...)` invokes it once per row (mirror: MigrationDependenciesEmitter.fs:235-236)
- **refutation:** Receipts check out, but refuted on the trivial-cost-plus-contract-churn arm. Cost: the per-row lane is hard-capped by DataStagingPolicy's default Threshold = 1000 (Policy.fs:160-169; above it StagedMerge.renderStagedPhase2 takes over at StaticSeedsEmitter.fs:163-174), and the recomputed constants — one TableId record, one setCells filter, and matchAttributes (for a PK kind a single O(attrs) filter, KindColumns.fs:85-91) — are a small fraction of the unavoidable per-row work each call already does (per-row Bench.scope with label concat, per-attribute cellOf, UpdateBuildArgs, ScriptDom buildUpdateStatement + renderDataBatch with the CDC change-detection predicate): sub-millisecond per kind on the publish leg. Contract churned by the fix: MergeRender.renderUpdate's deliberately-unified single signature — the second-consumer extraction whose stated purpose is that the two emitters' byte-identical copies 'had DRIFTED to a swapped deferred/bracketIdentity argument order, a latent hazard a single signature removes' (MergeRender.fs:6-16); a per-kind staged-closure variant reshapes that shared seam and its per-lane bench-label threading for both StaticSeedsEmitter and MigrationDependenciesEmitter.

### K14 · ValidateBeforeApply guard rebuilds the inline VALUES AST of the whole row set two more times on top of the MERGE's own build (3x per kind)
- **plane:** materialization  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ScriptDomBuild.fs:1201-1202 — `existsExcept (sourceSelect () ...) ...` OR `existsExcept ... (sourceSelect () ...)`: each call re-runs lines 1152-1160 rebuilding the identical VALUES AST over args.Rows
- **refutation:** Refuted on CONSUMER NEED. The rebuilds are real (ScriptDomBuild.fs:1152-1160 via sourceSelect() at 1201-1202, plus the MERGE's own build at 948-953), but each build lands in a distinct REQUIRED position of the emitted SQL: the symmetric-EXCEPT drift guard semantically must reference the source relation in BOTH arms (EXISTS(src EXCEPT tgt) OR EXISTS(tgt EXCEPT src)), and the MERGE needs its own USING source — the output document genuinely contains the VALUES text three times. ScriptDom's mutable-fragment, one-parent model (deliberately asserted at 1142-1144) makes fresh per-site instances the correct payment; a fragment-clone helper allocates and formats the same O(cells) nodes (no real saving), and routing guard-bearing inline kinds through the Staged source changes the emitted bytes and the operator's staging-policy semantics (churns the byte-identical-output and DataStagingPolicy contracts). The whole cost is also opt-in — paid only when the operator chooses ValidateBeforeApply, a deliberate verification purpose.

### K15 · writeChunk walks the remapped chunk twice for its length and computes journal fingerprints (tryLast + length + pk getters) even on journal-less runs
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Pipeline/TransferRun.fs:1385 — `return Result.success (List.length remapped.Rows, descents, skips, succeededLane)` re-walks the identical list; fingerprint receipts: 1326-1328 `let lastPk = chunkRaw \|> List.tryLast ...; let rawCount = List.length chunkRaw` computed before the `match journaled` regardless of `journal = None`
- **refutation:** Refuted on CONSUMER NEED — the claim's central 'journal-less runs' scenario is unreachable. DryRun streaming runs never reach writeChunk at all: runStreamingReconcilingWithRenames returns the COUNT-based preview at TransferRun.fs:1672-1715 before writePlanStreaming is ever invoked. And a streaming --execute WITHOUT --journal is refused by name via ReverseLegRealization.executeJournalGate (TransferRun.fs:103-109), enforced at the CLI face (Projection.Cli/Faces/Transfer.fs:359-360; FOLLOWON_STREAMING_REVERT.md:80 confirms the invariant 'streaming execute already requires --journal'). So on every reachable writeChunk execution journal is Some, and the firstPk/lastPk/rawCount fingerprint (:1326-1328) always has a consumer — either the resume-admit fingerprint check (:1341) or the journal append (:1377-1384): it is load-bearing verification/ledger state, not waste. The residual true part — WrittenCount at :1383 vs the return at :1385 both calling List.length remapped.Rows — is a genuine but minor duplicate that does not rescue a claim whose stated failure mode (zero-consumer fingerprints on journal-less/DryRun runs) is false.

### K16 · ConstraintFormatter re-derives typed constraint structure (name, FK body, ON DELETE/UPDATE) by IndexOf-parsing the text ScriptDom just rendered from the typed AST
- **plane:** representation  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ConstraintFormatter.fs:183-209 — appendForeignKeySegment re-extracts the same parts from rendered text via indexAfterBracketedName/IndexOf/Substring (`let fkIdx = indexAfterBracketedName withoutComma "FOREIGN KEY"` ... `referencesPart.IndexOf("ON DELETE", ...)`)
- **refutation:** Refuted on consumer need. The text form is load-bearing: the formatter's input is Sql160ScriptGenerator's OUTPUT, whose exact line shape (inline placement, spacing, whether ON DELETE NO ACTION is printed) is decided by the opaque gold-standard generator, not by V2's typed AST — the NO-ACTION fill/drop normalization (ConstraintFormatter.fs:214-227) operates on what ScriptDom actually emitted, which a build-time ConstraintLayout could only replicate by coupling typed IR to the generator's undocumented emission behavior, the exact coupling the recorded decision avoids. That decision is a documented contract: the LINT-ALLOW-FILE header's pillar-7 four-question rationale (ScriptDom exposes no per-constraint formatting axis; subclassing rejected; 'text post-processing IS the canonical fit'), the registeredMetadata design note that realization-layer transforms operate on rendered text not the typed Statement stream (the A35 IR contract), HANDOFF.md:2908's explicit 'resist wrapping it typed', and the carbon-copy-from-V1 inheritance discipline. The cost is also trivially small — IndexOf/Substring plus the slice-3b scanner over constraint-bearing CREATE TABLE lines only, dwarfed by the ScriptDom generation that produced them (the claim's 'per-row' grain is wrong: no data rows flow here) — and fixing it would churn the named contracts above.

### K17 · pinNewlines Replace chain scans every rendered statement's whole text twice
- **plane:** representation  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ScriptDomGenerate.fs:108 — second full-text scan of the result on the same line: `.Replace("\r", "\n")`; applied per statement via generateOne (132), generateBatch (156), generateDataType (232) — i.e., per row on the data lane (renderDataBatch:183 calls generateOne per statement; Phase-2 renders one UPDATE statement per row)
- **refutation:** Refuted on value identity. The two receipts are the same line of code (ScriptDomGenerate.fs:108) performing two DIFFERENT transformations in sequence: the first Replace derives the CRLF-collapse over the raw generator output; the second derives the lone-CR-collapse over the first's RESULT — a different input value carrying genuinely different information (the ordering is even semantically required: collapsing lone CR first would turn \r\n into \n\n). Neither scan re-derives a fact the other already produced; this is a two-stage derivation of one fact, not two payments for one fact, so there is no duplicate receipt. On the materialization plane the claim also overstates: .NET String.Replace returns the original instance when no match exists, so on LF hosts (the Linux production case the claim itself concedes) both calls are allocation-free probe scans and the 'normalized form' is materialized zero extra times. Residual cost is two vectorized scans per statement — trivially small — and the proposed single-pass rewrite still pays a probe scan, saving at most one scan.

### K18 · ManifestEmitter pays each kind's (module, schema, table) coordinate strings twice across Tables and PredicateCoverage, and per attribute in columnProfiles
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ManifestEmitter.fs:380-382 — PredicateCoverage.compute re-derives the identical triple for the same kinds (`Module = Name.value m.Name; Schema = TableId.schemaText k.Physical; Table = TableId.tableText k.Physical`); and ManifestEmitter.fs:669 re-runs `TableId.qualifiedParts k.Physical` inside the per-ATTRIBUTE List.choose of columnProfiles though it is constant per kind
- **refutation:** Refuted on both lenses. Materiality/value plane: the 'coordinate derivation' is not a computation — TableId.schemaText/tableText are single-case-DU field unwraps (Coordinates.fs:314,317: SchemaName.value / TableName.value) and qualifiedParts is just the pair of them (fs:322); Name.value is likewise an unwrap. Every 'second payment' returns the SAME already-materialized string instances via a field read (plus one small tuple), including the per-attribute qualifiedParts at ManifestEmitter.fs:669 — there is no re-derivation of substance to pay once. Value identity is also shaky: the Tables entries (fs:637-655, with TableFile/IndexCount/ForeignKeyCount) and PredicateCoverage.Tables rows (fs:379-384, with Predicates) are genuinely different information products that merely share coordinate FIELDS. Consumer-need/churn: PredicateCoverage.compute is a standalone public module whose signature is documented as 'Per A18 amended: Catalog only' (fs:367-372); threading a precomputed KindCoordinates carrier into it churns that recorded contract to save a handful of pointer reads per kind — the trivially-small-cost-plus-contract-churn refutation applies squarely.

### K19 · TransferSpec normalizes CSV newlines with a two-Replace chain before Split copies the whole text a third time
- **plane:** representation  ·  **site:** sidecar/projection/src/Projection.Pipeline/TransferSpec.fs:165 — second and third whole-text passes on the same line: `.Replace("\r", "\n").Split('\n')` (Split alone, with a per-line trailing-'\r' trim as ConstraintFormatter.fs:515 already does, yields the same lines in one pass)
- **refutation:** Refuted on both lenses. Value identity: the three passes are a pipeline of successive transforms, each consuming a DIFFERENT value (raw csv → CRLF-normalized → CR-normalized → split) — not two receipts of one fact; and String.Replace returns the original instance when nothing matches, so on LF-only input (the common case for these files) zero copies occur, collapsing the 'three whole-text copies' framing. Consumer need: the second Replace("\r", "\n") is load-bearing — it splits lone-CR (classic-Mac/stray-CR) line endings into lines, which the suggested carrier (single Split('\n') + trailing-CR trim per ConstraintFormatter.fs:515) does NOT do ("a\rb" stays one unparseable line), so the fix changes parse behavior for CR-only operator CSVs. Cost is also trivially small (small operator-authored spec, once per run).

### K20 · SchemaComplexityPass builds the full ParallelSafe level grouping only to count the levels
- **plane:** materialization  ·  **site:** sidecar/projection/src/Projection.Core/Passes/SchemaComplexityPass.fs:98-100 — `let depth = let levelCount = TopologicalOrder.levels t \|> List.length in max 0 (levelCount - 1)` — only the count survives
- **refutation:** Receipts are accurate (SchemaComplexityPass.fs:99 takes only List.length), but the claim fails the consumer-need lens: the pass's real need is 'count of levels as levels defines them across ALL modes' — for Alphabetical/JunctionDeferred, levels returns singleton groups (TopologicalOrder.fs:296-298), so a fold-max 'depth' projection would have to duplicate the exact mode branch whose semantics already caused the documented card-P2 bug (self-FK flattening real chains into one level). The discarded work (Map.toList/groupBy/sortBy/member sorts after the dominant level fold) is trivially small, once per publish, and the fix mints a new public TopologicalOrder projection for a single consumer — churning the Core module contract against the repo's own 'primitives at the second consumer' law and the ':143 ONLY production mint' single-source discipline. Deriving depth from the one mint is the safe carrier.

### K21 · StagedMerge.renderStagedPhase1 re-derives the kind's PK column names inline though the caller's args already carry the row-identity columns and KindColumns.pkColumnNames names the same projection
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Targets.Data/StagedMerge.fs:124-127 — `let pkCols = k.Attributes \|> List.filter (fun a -> a.IsPrimaryKey) \|> List.map (fun a -> ColumnRealization.columnNameText a.Column)` inside renderStagedPhase1 for the same k
- **refutation:** Value identity fails between the paired receipts: args.PkColumns = KindColumns.matchColumnNames (KindColumns.fs:85-100) is ROW-IDENTITY — for a no-PK kind it falls back to writable-minus-identity-minus-deferred columns and is never empty — while the index site's inline filter (StagedMerge.fs:124-130) is the TRUE-PK list with the load-bearing '[] -> None' branch (no PK ⇒ no clustered #temp index; using the fallback columns would mint a bogus wide clustered index). The claim's own evidence concedes the divergence. The second computation is therefore a genuinely different fact the consumer requires, not a re-payment. The residual point (inline vs KindColumns.pkColumnNames) saves no computation — calling the named function re-executes the identical 3-line filter per kind — so it is cosmetic naming, not a pay-twice finding.

### K22 · EmissionSeam.apply computed twice per publish — the emitted (post-seam) catalog has no name outside projectFromChainWithState, so the dacpac arm re-derives it
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1465 — dacpac arm: `EmissionSeam.apply policy.Emission finalState.Catalog \|> DacpacEmitter.emit`
- **refutation:** Value identity holds (same policy.Emission, same finalState.Catalog reference feeds both Pipeline.fs:630 and :1465), but the claim fails the cost/contract test. EmissionSeam.apply folds exactly one rewrite (EmissionSeam.fs:51), and Policy.fs:772-773 makes it identity-BY-REFERENCE under the default IncludePlatformAutoIndexes=true — so in the default config the 'second computation' returns the same object, defeats no ConditionalWeakTable cache, and costs nothing. The rebuild only occurs under the conjunction of a non-default toggle AND the opt-in cfg.Emission.Dacpac arm, where it is one O(catalog) map plus the downstream ms-scale statements run that PERF_OPPORTUNITIES Sweep 2 item 5 already adjudicates as an accepted remaining compute ('SsdtDdlEmitter.statements (dacpac path — a different consumer shape, ms-scale)'). Fixing it means widening the public projectWithState*/projectFromChainWithState return contract (Outputs * ComposeState, Pipeline.fs:616/683/754) or the ComposeState record — contract churn for a cost that is zero by default and ms-scale in the rare combination. The F3 comment (Pipeline.fs:1462-1464) also shows the recompute-through-the-seam is the deliberate anti-drift binding for the conditional arm.

### K23 · RemediationEmitter builds three whole-catalog indexes eagerly before knowing any decision needs them, and the artifact is discarded post-fold under EmitDiagnostics=false
- **plane:** materialization  ·  **site:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:677-682 — `if policy.EmitDiagnostics then schemaGated else { schemaGated with RemediationSql = "" … }` (the gate clears the field AFTER the emit step computed it at :592-596); and RemediationEmitter.fs:225-257's loops need the indexes only for the matching-outcome decisions, which are frequently zero (`if count = 0 then …`, :259)
- **refutation:** Both halves fail. The discard half is load-bearing by design: the NM-02 comment (Pipeline.fs:662-668) states every EmitStep runs so the property-tested registered⇔executed invariant (RegisteredAllTransformsBidirectionalTests) holds, with gates clearing fields AFTER the fold — and EmitDiagnostics=false is non-default (EmissionPolicy default is diagnostics ON, Policy.fs ~790), so in the normal publish RemediationSql is a real shipped artifact, not discarded. The eager-index half is the codified house contract, not an oversight: the builders' own doc (RemediationEmitter.fs:56-60) cites 'DECISIONS 2026-05-19 (slice B.3.6b)' — cross-derivation lookup state lands as a precomputed index at construction time, not as repeated List.tryFind — and attrIndex is shared across all three loops (renderForeignKeyOrphans and renderUniqueIndexDuplicates both take it, :244/:254). The cost when decision sets are empty is three O(kinds) walks + Map.ofList per publish (low-ms at 300-kind scale, invisible next to the same publish's ScriptDom render), and lazifying it churns the documented index-at-construction discipline for that ms-scale case. Trivially small cost, deliberate design on both halves.

### K24 · ManifestEmitter re-derives the per-kind SSDT relative path that SsdtDdlEmitter already computed and carries on SsdtFile.RelativePath
- **plane:** representation  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ManifestEmitter.fs:644-652 — `TableFile = System.String.Concat("Modules/", Name.value m.Name, "/", schemaStr, ".", tableStr, ".sql")` inside buildWithTopology (invoked per publish via buildFull at Pipeline.fs:571-574, after the SSDT step already produced the SsdtFiles at :566)
- **refutation:** Consumer need refutes it: the manifest surface must derive TableFile from the Catalog alone. ManifestEmitter.build (Catalog-only) is invoked at seedOutputs (Pipeline.fs:554) BEFORE any SSDT emit step has produced SsdtFiles — at that site there is no ArtifactByKind<SsdtFile> to thread, so the String.Concat convention in buildWithTopology (ManifestEmitter.fs:644-652) is the load-bearing derivation for the whole build/buildWith/buildFull family, and buildFull (called at Pipeline.fs:571-574) shares the same entries code path. The per-kind cost is one String.Concat — trivially small — and the fix churns the public ManifestEmitter builder contracts (buildFull's Catalog-driven signature plus the Catalog-only build used by seedOutputs, which would need a divergent second implementation). The override-blindness observation cuts against the claim's own value-identity premise: under EmissionFolders overrides (Pipeline.fs:339-342) RelativePath and TableFile carry different semantic content (actual overridden path vs naming convention), i.e. they are not the same fact there — that is at most a separate correctness question about manifest accuracy under overrides, not a pay-twice defect.

### K25 · ManifestEmitter.buildWithTopology re-flattens the Modules→Kinds enumeration four times in one build (entries, columnProfiles, Coverage.compute, PredicateCoverage.compute)
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ManifestEmitter.fs:656-660 — `let columnProfiles = catalog.Modules \|> List.collect (fun m -> m.Kinds \|> List.collect …)`; third at :468-469 (`Coverage.compute`: `let allKinds = catalog.Modules \|> List.collect (fun m -> m.Kinds)` via :686) and fourth at :374-377 (`PredicateCoverage.compute`: `catalog.Modules \|> List.collect (fun m -> m.Kinds \|> List.map …)` via :687)
- **refutation:** Weak value identity and trivial cost with contract churn. The four walks (ManifestEmitter.fs:627, :656, :468 via :686, :374 via :687) compute four genuinely different projections (table entries, column-moment profiles, coverage counts, predicate satisfaction); the only shared fact is the List.collect flatten of ~hundreds of kinds — microseconds of list allocation per publish, in the same emit step whose ScriptDom render dominates by orders of magnitude, and nowhere near the tens-of-ms bar Sweep 2 already used to DECLINE the sibling 'manifest built twice per publish' item (PERF_OPPORTUNITIES.md:979-981). Fixing it requires threading a kind list into Coverage.compute and PredicateCoverage.compute — public [<RequireQualifiedAccess>] module APIs whose documented contract is explicitly 'Per A18 amended: Catalog only' (ManifestEmitter.fs:367-371, :463-465) with their own Bench scopes; changing their signatures is contract churn for a sub-ms win. Coverage.compute's doc even reserves the second-argument widening for a real forward trigger (an emit-set filter), not for this.

### K26 · emittedResidualCollector's full static-row per-cell walk rebuilt at every tolerance-residual consumer
- **plane:** derivation  ·  **site:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:2152 — storeLegFromConfig: `(emittedToleranceResidual (defaultArg cfg.Emission.Tolerance Tolerance.permissive) emitted)` → :513 `CanaryResidual.resolve configured (emittedResidualCollector catalog)`; also :2222 in recordLoad (`emittedToleranceResidual Tolerance.permissive emitted`)
- **refutation:** VALUE IDENTITY fails: the three collector runs are not over the same catalog value. fidelityOf folds over ctx.EmittedCatalog (Pipeline.fs:630) — the HYDRATED catalog (readAndHydrateConfigModel grafts live static rows onto Modality.Static, Pipeline.fs:1626-1685) after the full pass chain and EmissionSeam.apply — while storeLegFromConfig's emittedSchema (Pipeline.fs:1881-1885) is plain readConfigModel + applyRenames: unhydrated and pre-chain. The static populations the collector folds over therefore differ (live-grafted rows present in one, absent in the other); the docstring's 'same catalog' claim is about the schema-plane input, not the value the collector consumes. recordLoad additionally resolves against Tolerance.permissive instead of the operator's configured tolerance, by documented design (Pipeline.fs:2216-2221: 'config-less... resolves against Tolerance.permissive') — a semantically different resolution. The suggested carrier (threading Outputs.Fidelity.AcceptedDivergences) would change the recorded episodes' residual values, confirming these receipts are not the same fact.

### K27 · Dacpac arm rebuilds and re-renders every schema statement AST the SSDT bundle step just produced
- **plane:** materialization  ·  **site:** sidecar/projection/src/Projection.Pipeline/Pipeline.fs:1465-1466 — `EmissionSeam.apply policy.Emission finalState.Catalog \|> DacpacEmitter.emit` → DacpacEmitter.fs:204 `SsdtDdlEmitter.statements catalog` rebuilding the same statements (SsdtDdlEmitter.fs:913-932) and DacpacEmitter.fs:157 `statementToScript statement` re-rendering each
- **refutation:** VALUE IDENTITY fails: the two builds share the catalog but not the transform inputs, so they are not two receipts for the same ASTs. The bundle builds with the run's decision overlay (DecisionOverlay.ofComposeState) and ctx.EmitIdentityAnnotations (Pipeline.fs:565-566); the dacpac builds via SsdtDdlEmitter.statements = statementsWith DecisionOverlay.empty with identity annotations forced true (SsdtDdlEmitter.fs:938-944), in topological order with BatchSeparators, INCLUDING sequenceStatements (line 908) and EXCLUDING moduleSchemaPropertyStatements — a genuinely different statement set on any decision-bearing publish. CONSUMER NEED also refutes the text half: DacFx's TSqlModel.AddObjects requires standalone per-statement scripts (DacpacEmitter.fs:93-102, 153-159); the bundle's per-kind bodies are GO-framed and ConstraintFormatter-postprocessed (Render.toTextWith, SsdtDdlEmitter.fs:776-785) and cannot feed AddObjects. Consuming the A35 typed statement stream is the dacpac's registered contract (DacpacEmitter.fs:257-260 registeredMetadata: 'statement generation flows through SsdtDdlEmitter.statements').

### K28 · Per-statement Bench.scope stopwatch+disposable stacked under streamProbe in the >=100k-statement render fold
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/Render.fs:82 — `toSql` opens `use _ = Bench.scope "render.statement"` per element inside that same enumeration (Render.fs:151 `Seq.iter (toSql sb)`), re-paying a heap Stopwatch + disposable + lock per statement
- **refutation:** Both lenses refute. VALUE IDENTITY: the per-element scope and the streamProbe do not record the same fact — streamProbe yields ONE wall-time sample (which includes upstream production time between pulls) plus a count, while render.statement records the per-statement DISTRIBUTION (Count/Min/Mean/P50/P95/P99 per statement shape — its stated purpose, Render.fs:65-66), which is not derivable from the aggregate. CONSUMER NEED: the label is load-bearing in the active measurement program — PERF_HARNESS.md §2a names 'render.statement' as a scenario label with a recorded operator-reality figure ('render.statement ~143 ms / 2210 calls') and states the scenario 'exists to PROVE it stays cheap as the estate scales'; scripts/perf-gate.sh gates per-label TotalMs against the committed bench/baseline-canary.json. The suggested EOF-aggregate carrier changes the label's Count/Mean semantics, churning the perf-gate baseline — a contract whose re-record requires a DECISIONS amendment (CLAUDE.md survival rule 11). The ReadSide fusion is a design precedent the team may choose, not evidence the two receipts duplicate.

### K29 · Four nested per-row Bench scopes (plus fresh StringBuilder + generator) on every Phase-2 UPDATE row
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:131 — `use _ = Bench.scope (String.Concat(bench, ".renderUpdate"))` re-times the same body (scope #2); then ScriptDomGenerate.fs:173 `Bench.scope "scriptDom.renderDataBatch"` (scope #3, called per row via MergeRender.fs:158) and ScriptDomBuild.fs:1870 `Bench.scope "emit.scriptDom.build.update"` (scope #4)
- **refutation:** Refuted primarily on CONSUMER NEED, with a value-identity assist. The four scopes do not time 'the same body': scriptDom.renderDataBatch (ScriptDomGenerate.fs:173) and emit.scriptDom.build.update (ScriptDomBuild.fs:1870) are nested sub-span decompositions whose labels are shared with other consumers (the Phase-1 merge path also rides renderDataBatch), giving the build-vs-generate breakdown; nested rollup is the Bench design's stated intent (Bench.fs point 4: 'Scopes nest naturally... Stats roll up at every level'). The labels are load-bearing in the active harness program: PERF_HARNESS.md §3b names emit.staticSeeds.phase2Row and §2b names emit.scriptDom.build.*; the renderUpdate label is a documented deliberate continuity choice ('the labels are byte-for-byte what the per-emitter copies emitted', MergeRender.fs:13-16); perf-gate.sh gates per-label against the committed baseline, so fusing churns that contract. The cost premise is also weakened: DataStagingPolicy.auto (Threshold=1000, Policy.fs:159-169) routes >1000-row kinds through the staged set-based Phase-2, so the 20k-row per-row scenario requires operator-pinned Inline mode, and even there the ~4 scope allocations/locks per row are a small fraction of the per-row ScriptDom UpdateStatement build + Sql160ScriptGenerator render they time.

### K30 · Per-column double Bench scope in buildCreateTable: columns iterDo label wraps a body that is exactly columnDefinition's own scope
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ScriptDomBuild.fs:355-356 — `let private columnDefinition (c: ColumnDef) : ColumnDefinition = use _ = Bench.scope "emit.scriptDom.build.columnDefinition"` re-times the identical body
- **refutation:** VALUE IDENTITY fails globally: the two labels are not receipts for the same fact. `columnDefinition` (ScriptDomBuild.fs:356) fires at FOUR call sites — 673 (buildCreateTable), 1048 (staging-table build), 1540 (keymap table via keymapCol), 2346 (ALTER COLUMN) — while `emit.scriptDom.build.columns` (672) attributes only buildCreateTable's loop. The labels measure different populations; dropping the interior scope loses three other sites, dropping the iterDo (the suggested carrier) loses the createTable-specific attribution and the count difference between the two labels (which distinguishes created-table columns from staging/keymap/alter columns). CONSUMER NEED also holds: both labels are entries in the 51-label bench-fleet inventory (DECISIONS 2026-05-19) asserted by ComprehensiveCanaryTests.fs:96,98 (Assertion C, hard Assert.True at line 1119-1124) and present in every bench/canary/*.json baseline, and Bench.fs:152-156 documents the per-iteration overhead as ~µs and 'negligible at V2's scale'. Trivially small cost + fixing churns the canary label-inventory contract.

### K31 · Per-row Bench.iterDo scope inside the inline MERGE row loop, under the merge-level scope that already times it
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ScriptDomBuild.fs:949-953 — `args.Rows \|> Bench.iterDo "emit.scriptDom.build.merge.row" (fun row -> ... rv.ColumnValues.Add(buildSqlLiteral cell) ...)` re-wraps every row with its own scope allocation
- **refutation:** VALUE IDENTITY fails: the (merge-scope, renderMerge.rows) pair is not the same fact as the per-row samples. `emit.scriptDom.build.merge` (ScriptDomBuild.fs:927) times the WHOLE merge build (target refs, ON clause, WHEN arms) and also fires on the Staged path (RowSource=Staged, line 941-945) where the inline row loop never executes; `renderMerge.rows` (MergeRender.fs:42) records the count BEFORE the staging branch, so it too counts rows for staged merges, and it is per-lane (emit.staticSeeds.*/emit.migrationDeps.*) while merge.row is global — so 'total/count' is not a valid derivation of per-row time. The per-row label additionally carries a distribution (P95/P99 slow-tail rows), which Bench.fs:136-156 documents as the iterDo combinators' explicit operator-facing purpose ('per session-30 operator framing'). CONSUMER NEED: `emit.scriptDom.build.merge.row` is in the 51-label inventory asserted by ComprehensiveCanaryTests.fs:103 and the canary baselines, and the codebase's standing law (CLAUDE.md §6) is 'a hot loop without a label is unfinished code'. Per-element cost is the documented-negligible ~µs; removing the label churns the bench-fleet contract.

### K32 · ReadSide.read walks and rebuilds the reconstructed kind list four times through the attach* chain
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Adapters.Sql/ReadSide.fs:1926-1928 — `\|> List.map (attachComputed computed) \|> List.map (attachAnnotations triggers checks extProps) \|> List.map (attachIndexes indexes)` (three more walks over the same list)
- **refutation:** VALUE IDENTITY fails: the four passes (ReadSide.fs:1923-1928) are not receipts for the same fact — each List.map produces a genuinely different catalog value post-transform (defaults-attached, then computed-attached, then triggers/checks/extProps-attached, then indexes-attached), each attaching distinct recovered information from a distinct SQL read. The only shared duplication is the list spine (~3N cons cells for N kinds), a byproduct, and the claim's own suggested carrier (function composition in one List.map) would still perform all four Kind-record rebuilds and both interior attribute-list rebuilds (attachDefaults 1342-1352, attachComputed 1367-1378 run per kind regardless) — it saves only the 3 spines. That saving is trivially small on a verb-only readback path that immediately performs 8+ network round-trips (readDefaultConstraints, readComputedColumns, readTriggers, readCheckConstraints, readExtendedProperties, readIndexes, readSequences at 1906-1921, plus a per-kind readRows at 1940). The NM-30 symmetric-walk comment (1331-1337) further marks the uniform per-feature walks as a deliberate correctness shape.

### K33 · Diagnostics wrapper built per statement then immediately .Value-stripped in buildStatement's hot dispatch
- **plane:** materialization  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ScriptDomBuild.fs:2573 — `Some ((buildCreateTable table cols pk fks checks temporal).Value :> TSqlStatement)` strips it immediately (also 2575, 2585), per statement in the Render.toSql per-element dispatch (Render.fs:99)
- **refutation:** CONSUMER NEED holds: the Diagnostics<_>-bearing signature is a declared, tested typed contract, not a stray wrapper. ScriptDomBuild.fs:608-617 records it as the chapter 4.9 slice ζ canonical signature ('Today the entries list is empty by construction; callers explicitly drop via .Value at the call site — per the chapter 4.7 sibling-wrapper discipline'), and it is pinned by dedicated law-named tests: WithDiagnosticsBuildersTests.fs:28 ('Slice ζ: buildCreateTable returns Diagnostics with empty entries today'), :36 and :45 (buildSetExtendedProperty), plus external Diagnostics-form consumers of the same pattern (StagedMerge.fs:141, TransferCellShaping.fs:80, IndexIncludedColumnsTests.fs:134). The exact behavior the claim reports as a defect is the documented consumption discipline of that contract. The cost is one 2-field record allocation ({Value; Entries=[]}, the empty list being the shared singleton) per per-kind/per-extProp statement — trivially small next to the dozens of ScriptDom AST node allocations each arm performs — and the >=100k-statement render loop is dominated by the Merge/Update/InsertRow row-grain arms that already bypass. Trivial cost + fixing carves around the slice-ζ canonical-signature contract.

### K34 · Fresh Sql160ScriptGenerator constructed per rendered statement (and per Phase-2 row) after the options half was hoisted
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Targets.SSDT/ScriptDomGenerate.fs:129 — `let generator = Sql160ScriptGenerator(sharedPinnedOptions)` inside generateOne, executed per statement via Render.fs:101 and per row via ScriptDomGenerate.fs:183 (renderDataBatch)
- **refutation:** CONSUMER NEED holds (and the claim itself concedes it is contingent): the per-call generator is a recorded correctness decision, stated twice in the file — ScriptDomGenerate.fs:96-97 'The GENERATOR stays per-call (it holds real per-render state)' and :220-226 'ScriptDom's generator state is stateful so sharing across calls would alias'. Whether Sql160ScriptGenerator.GenerateScript fully resets internal per-render state is a property of the external Microsoft.SqlServer.TransactSql.ScriptDom package, unverifiable from this repo; the suggested ThreadLocal carrier is explicitly 'contingent on verifying GenerateScript resets per-render state' — the parser precedent (ScriptDomBuild.fs:347) does not transfer, since a parser's reusability says nothing about a generator's. The construction guards T1 byte-determinism (the module's stated law, verified by ScriptDomRoundTripTests), i.e., a fresh-instance verification/correctness purpose, and the ctor cost (one visitor object) is trivial against GenerateScript's full AST walk + string materialization per call. Uncertain library semantics + recorded load-bearing rationale ⇒ refuted.

### K35 · Bench.record re-resolves the loop-constant label's samples list under the global lock once per element
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Core/Bench.fs:163-166 / 179-183 — iterDo/iterMap apply `use _ = scope label` per element with the SAME label, so elements 2..N re-pay lock + TryGetValue for the identical binding; hot callers: OssysRowsetReader.fs:580 (per attribute row estate-wide), Render.fs:82 (per statement at >=100k)
- **refutation:** Both lenses refute. Value identity: storage is mutable shared state — Bench.reset() (Bench.fs:302) clears it for test isolation, so element N's TryGetValue is not a recompute of the same immutable fact; a cached ResizeArray/SampleSink would append to an orphaned list after a reset and silently lose samples. Consumer need: the per-record lock is the documented thread-safety contract (Bench.fs:44-47 codifies per-record lock granularity as adjudicated-negligible; snapshot at 269-272 promises concurrent safety), and Bench is Projection.Core's single named mutable-state exception whose justification is 'record is the only mutator; it's lock-protected' — handing loops the interior ResizeArray churns that codified contract while the saved cost (uncontended lock + one hash lookup, tens of ns/element vs ScriptDom rendering per statement) is trivially small.

### K36 · JsonEmitter/DistributionsEmitter time each kind twice: iterDo moduleKind label wraps a body that is exactly writeKind's own scope
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Targets.Json/JsonEmitter.fs:100 — `writeKind` opens `use _ = Bench.scope "emit.json.kind"` re-timing the identical body; DistributionsEmitter mirrors at DistributionsEmitter.fs:178 (`Bench.iterDo "emit.distributions.moduleKind" (writeKind w profile)`) and 148 (`Bench.scope "emit.distributions.kind"`)
- **refutation:** The 'two labels, one body' premise fails: emit.json.kind also fires from kindJsonNode (JsonEmitter.fs:158 via emitSlices, so it aggregates the per-kind slice path the moduleKind label never sees), and emit.json.moduleKind also fires from catalogJsonNode (JsonEmitter.fs:206) around a completely different body (slice DeepClone). DistributionsEmitter mirrors this exactly (kind label from kindJsonNode at 203/221; moduleKind also at 294). The two labels record different call-site populations — different scope, genuinely different information — and both are load-bearing operator surfaces: the suggested collapse would leave the slice path untimed or conflate document-path vs slice-path per-kind cost in the bench table.

### K37 · rowToCellValues allocates a `cellValue a` partial-application closure per cell in the InsertRow stream
- **plane:** tower  ·  **site:** sidecar/projection/src/Projection.Targets.Data/StaticPopulationEmitter.fs:84-86 — `k.Attributes \|> List.choose (fun a -> Map.tryFind a.Name row.Values \|> Option.map (cellValue a))` re-allocates the partial application per attribute, per row (loop at 112-113 `for row in rows do let values = rowToCellValues k row`)
- **refutation:** Value identity fails as cited: the first receipt (the module-level definition at StaticPopulationEmitter.fs:70) is code, not a value receipt, and each per-cell application at 84-86 binds a DIFFERENT attribute `a` within the loop — at the claimed per-cell grain no identical value is paid twice. The only genuine recurrence (same-attribute closure repeated across rows) is not deduplicated by the claim's own suggested carrier (the explicit match merely avoids allocation, it shares nothing). Cost is a ~30-byte gen0 closure per cell — on the order of 1-2% of the perf-gated emit.staticPopulation.statements.stream tier — and the claim itself concedes 'borderline; micro-allocation'; under the default-refute-when-uncertain rule this does not survive.


## Appendix C — the unadjudicated finding (hand-ruled IN, joins PL-3)

### U00 · Per-lane-constant Bench label rebuilt via String.Concat on every Phase-2 row
- **plane/grain/path:** derivation / per-row / publish-critical
- **fact:** The scope-label string `bench + ".renderUpdate"` (e.g. "emit.staticSeeds.renderUpdate") is a per-lane constant — MergeRender's own docstring says the prefix is fixed per lane — yet the concatenation is executed inside renderUpdate, i.e. once per row, allocating a fresh label string per row in the Phase-2 loop.
- **first receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:13-16 — the docstring pins the prefix as per-lane constant: "`bench` is the caller's per-lane Bench scope prefix (`\"emit.staticSeeds\"` / `\"emit.migrationDeps\"`)" — the concatenated label could be paid once at lane entry
- **second receipt:** sidecar/projection/src/Projection.Targets.Data/MergeRender.fs:131 — `use _ = Bench.scope (System.String.Concat(bench, ".renderUpdate"))` executes the concat per call, and the call site is per row (StaticSeedsEmitter.fs:177, MigrationDependenciesEmitter.fs:236)
- **suggested carrier:** renderUpdateLabel — a per-lane precomputed label binding threaded into the row loop (renderUpdateUsing form)
- **hand ruling:** the receipt is textually verifiable (the docstring pins the prefix per-lane-constant; the Concat sits inside the per-row scope); folded into PL-3's renderUpdateForKindUsing carrier.


## Appendix D — the critic's six gap receipts (verbatim, unverified)

- **[acquisition]** Config re-parse shape never hunted, and the entire Projection.Cli layer has zero findings (surviving or killed). Concrete receipt: within one migrate leg, projection.json is read+parsed up to FOUR times. (1) Dispatch parses it once: src/Projection.Cli/Program.fs:477 `discoverConfig ()` -> `ProjectionConfig.fromFile path` (Program.fs:341-346). (2) The tightening gate re-reads and re-parses the SAME file: src/Projection.Cli/Faces/Migrate.fs:334-335 `let configFile = RelaxationStore.configPath ()` / `RelaxationStore.read configFile`, where read -> rootObjectOf does `JsonNode.Parse(File.ReadAllText path)` (src/Projection.Cli/RelaxationStore.fs:34). (3+4) On Relax-ALWAYS, `RelaxationStore.persist` (Migrate.fs:359) internally pays the file read+parse TWICE more in one call: RelaxationStore.fs:73 `match rootObjectOf path with` and then line 77 `Set.union (read path) (Set.ofSeq keys)` — `read path` calls rootObjectOf again on the file it just parsed at line 73. Same LifecycleStore.load-4x shape (a survivor) in the CLI config store, never reported.
- **[representation]** Cross-verb sharing via a disk round-trip inside ONE process was never hunted for the slice verbs (survivors cover publish-then-load only). Concrete receipt: `SliceFlowRun.run` (src/Projection.Pipeline/SliceFlowRun.fs:25-36) chains extract -> apply through a temp file: `SliceExtractRun.extractSpec` builds the typed in-memory GoldenDataset (src/Projection.Pipeline/SliceExtractRun.fs:115 `let golden = GoldenDataset.ofClosure catalog state`) and serializes it to the temp path (line 119 `File.WriteAllText(outPath, GoldenCodec.serialize golden)`); then `SliceApplyRun.applyLive` in the same task immediately re-reads the bytes (src/Projection.Pipeline/SliceApplyRun.fs:134-135 `File.ReadAllText goldenPath`) and re-parses them back to the identical value (line 140 `GoldenCodec.deserialize json`). The whole closed row set is paid three times (typed -> JSON text -> disk -> JSON text -> typed) when the typed value was already in memory.
- **[derivation]** LogSink.fs (1,077 lines, the run's whole event egress) was never read by any finding. Concrete receipt: every envelope's NDJSON serialization is a pure function of the unchanged Envelope, paid twice per run. First payment at emit: src/Projection.Pipeline/LogSink.fs:635 `writer.Value.WriteLine(serializeEnvelope env)` (the string is written and discarded; the accumulator keeps only the typed Envelope, line 578 `s.Envelopes.Add env`). Second payment at run capture: LogSink.fs:719-720 `let serializedEnvelopes () : string list = snapshot () \|> List.map serializeEnvelope` re-serializes EVERY accumulated envelope, consumed by `Run.capture` (src/Projection.Pipeline/Run.fs:345 `Events = LogSink.serializedEnvelopes ()`). The entire event stream's serialization cost is duplicated on any run that persists a Run aggregate.
- **[materialization]** The Run store (Run.fs / RunHistory.fs / Inspect.fs — none touched by any finding) fully materializes artifacts whose consumers need only an index projection. Concrete receipt: `Run.list` (src/Projection.Pipeline/Run.fs:227-234) parses every run.json completely — including the `events` array of verbatim NDJSON envelope strings (Run.fs:146-149) and the `artifacts` map of whole content blobs (catalog JSON, SSDT bundle text; Run.fs:150-154). Paid-again consumers: `runInspectHistory` (src/Projection.Cli/Faces/Inspect.fs:93 `Run.list dir \|> List.sortByDescending ...`) whose view uses artifacts ONLY for their key names (Inspect.fs:50-55 `arts \|> List.map (fun (k, _) -> View.Field (k, "", ...))`) and events only for `List.length r.Events` (Inspect.fs:41); and `RunHistory.load`/`readiness` (src/Projection.Pipeline/RunHistory.fs:26,46-47) which projects each fully-parsed Run down to a `RunLedger.LedgerRecord` index row via `Run.toLedgerEntry`. Every stored run's full artifact tree is deserialized to answer index-grain questions.
- **[representation]** Targets.OperationalDiagnostics had only one (killed) finding — RemediationEmitter; its siblings SummaryFormatter/DecisionLogEmitter/SuggestConfigEmitter/ActionableDiagnostics were never read. Concrete receipt in SummaryFormatter: `format` builds the whole summary in a StringBuilder, copies it out with `sb.ToString()` and splits it into a line list with per-line \r-strip (src/Projection.Targets.OperationalDiagnostics/SummaryFormatter.fs:235-244 `text.Split([\| '\n' \|], ...) \|> Array.toList \|> List.map ... \|> List.filter ...`), and `formatText` (SummaryFormatter.fs:248-257) immediately re-joins those lines with `String.concat "\n"` for the manifest.summary.txt artifact — build -> split -> re-join copies of the same text, the exact split-then-rejoin shape the surviving ConstraintFormatter finding names, in a file no pass examined.
- **[tower]** All tower findings (all killed) were Bench/render shapes; the logging egress was never checked for the same per-element stack. Borderline concrete receipt: `LogSink.serializeEnvelope` allocates a fresh MemoryStream + Utf8JsonWriter per envelope, then copies the encoded bytes with `ms.ToArray()` and decodes them to a UTF-16 string (src/Projection.Pipeline/LogSink.fs:544-545 `use ms = new MemoryStream()` / `let jw = new Utf8JsonWriter(ms)`; line 570 `Encoding.UTF8.GetString(ms.ToArray())`) — which `emit` (LogSink.fs:620-637) runs under the global `lock lockObj` per envelope before `writer.Value.WriteLine` re-encodes the string back to the writer's encoding. Per-event cardinality: UTF-8 encode -> byte[] copy -> UTF-16 decode -> re-encode, plus a fresh stream/writer pair per element under a lock — the sibling of the surviving PinnedWriting `stream.ToArray()` finding and the killed `Bench.record ... under the global lock once per element` finding, in a file neither pass read.
