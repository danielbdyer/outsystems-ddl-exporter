---
name: retype-explicit
description: Use when the developer says "change the text field to a date", "store it as a number now", "make this Text into an Integer", "convert this column's type" where not every value converts — a value-reshaping, lossy cast.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/retype-explicit/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.py apply`; do not edit by hand).
