---
name: add-default
description: Use when the developer says "give this attribute a default value", "new rows should default to Active", "everything new should start as Pending" — adding a named DEFAULT constraint. Ships in place as a single schema change; it fills only NEW rows and never backfills existing ones.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/add-default/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.mjs apply`; do not edit by hand).
