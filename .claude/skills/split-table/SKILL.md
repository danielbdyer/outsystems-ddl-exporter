---
name: split-table
description: Use when the developer says "split Customer into Customer and CustomerAddress", "pull the address fields out into their own entity", "this entity is doing too much, break it up" — one entity becoming two, with data moving. SSDT destination = a multi-phase additive-CREATE / copy / cutover / drop-old-columns program.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/split-table/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/ssdt-agent/scripts/ssdt-agent-package.mjs apply`; do not edit by hand).
