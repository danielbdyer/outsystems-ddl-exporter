---
name: make-optional
description: Use when the developer says "make this attribute optional", "uncheck Mandatory", "let it be blank now", "it doesn't have to be filled anymore" — an existing column NOT NULL→NULL. A pure loosening; the risk is downstream, not at deploy.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/make-optional/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.py apply`; do not edit by hand).
