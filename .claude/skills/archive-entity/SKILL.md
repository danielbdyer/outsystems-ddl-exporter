---
name: archive-entity
description: Use when the developer says "archive old orders", "move the historical rows out to an archive table", "move records we don't need live anymore" — a data movement between tables, not a shape change.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/archive-entity/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/ssdt-agent/scripts/ssdt-agent-package.mjs apply`; do not edit by hand).
