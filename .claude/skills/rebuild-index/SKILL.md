---
name: rebuild-index
description: Use when the developer says "the index is fragmented, rebuild it", "reorganize the index to fix performance", "run index maintenance on this table", "make SSDT rebuild the index on deploy" — operational maintenance with no declarative destination. Refuse-and-route to the maintenance job.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/rebuild-index/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.mjs apply`; do not edit by hand).
