---
name: rename-entity
description: Use when the developer says "rename the Entity", "change the table name from Customer to Client", "I renamed it in Service Studio" — an existing table getting a new name. Identity must be carried by the refactorlog; a rename with no refactorlog entry loses the table's data.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/rename-entity/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.py apply`; do not edit by hand).
