---
name: modify-default
description: Use when the developer says "change the default value", "new orders should default to Shipped now, not Pending", "stop defaulting this attribute" — changing or removing a DEFAULT constraint. SSDT does a DROP-then-ADD; it ships in place as a single schema change and never backfills existing rows.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/modify-default/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.py apply`; do not edit by hand).
