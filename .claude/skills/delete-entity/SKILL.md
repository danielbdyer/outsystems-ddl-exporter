---
name: delete-entity
description: Use when the developer says "delete the Entity", "drop the table, we don't need it", "remove the old AuditLog" — removing a whole table. The risk is the irreversible data loss, not the number of releases; BlockOnPossibleDataLoss blocking the publish on a populated table is the safety proof.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/delete-entity/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.py apply`; do not edit by hand).
