---
name: toggle-trust
description: Use when the developer says "trust the constraint now that the data is clean", "turn the FK back on", "temporarily disable the check so this load can run" — toggling a constraint's enforcement/trust state. OPERATIONAL, NOT declarative; refuse-and-route to a script, prove it ends TRUSTED.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/toggle-trust/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/scripts/ssdt-agent-package.mjs apply`; do not edit by hand).
