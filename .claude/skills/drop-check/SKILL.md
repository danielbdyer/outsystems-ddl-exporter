---
name: drop-check
description: Use when the developer says "remove the rule that Total must be positive", "stop enforcing that status list", "drop the check so this load can go through" — removing a CHECK constraint. Always publishes clean (a drop validates nothing), but the rule stops being enforced and re-adding it later re-validates every row.
---

The canonical skill body lives in the tree, where its relative citations resolve:
**Read `sidecar/projection/ssdt-agent/skills/op/drop-check/SKILL.md` now and follow it exactly.** This package is the dispatch pointer
(generated by `sidecar/projection/ssdt-agent/scripts/ssdt-agent-package.mjs apply`; do not edit by hand).
